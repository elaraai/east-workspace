name: Test east

on:
  push:
    branches: [main]
    paths:
      - "libs/east/**"
      - ".github/workflows/test-east.yml"
  pull_request:
    branches: [main]
    paths:
      - "libs/east/**"
      - ".github/workflows/test-east.yml"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test east (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        # Git Bash on all OSes so the build/test steps share one shell.
        shell: bash
    env:
      # east's test script uses single-quoted globs + `&&`; pnpm runs package
      # scripts via cmd.exe on Windows by default, so force a POSIX shell.
      npm_config_script_shell: bash

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Build east
        run: pnpm --filter @elaraai/east run build

      # eslint is OS-independent; run it once on Linux.
      - name: Lint (Linux only)
        if: runner.os == 'Linux'
        run: pnpm --filter @elaraai/east run lint

      # The codegen round-trip legs (#628) read the exported compliance corpus
      # and examples (/tmp/east-test-ir, /tmp/east-examples-ir). The export
      # tooling is Unix-only, so the corpora are exported on Linux — where the
      # legs are REQUIRED — and the legs skip on the other OSes.
      - name: Export IR corpora (Linux only)
        if: runner.os == 'Linux'
        run: |
          pnpm --filter @elaraai/east run test:export
          pnpm --filter @elaraai/east run export:examples

      - name: Run tests
        env:
          EAST_CONFORMANCE_REQUIRED: ${{ runner.os == 'Linux' && '1' || '0' }}
        run: pnpm --filter @elaraai/east run test
