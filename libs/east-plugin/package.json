{
  "name": "@elaraai/east-plugin",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "bundle": "esbuild hooks/prompt-submit.ts hooks/session-start.ts hooks/subagent-start.ts hooks/pre-agent.ts hooks/pre-write.ts hooks/post-write.ts mcp/search-server.ts --bundle --format=esm --platform=node --outdir=.build --preserve-symlinks",
    "generate-index": "npm run build && node dist/scripts/generate-index.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "minisearch": "^7.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.18.1",
    "esbuild": "^0.27.3",
    "typescript": "~5.9.2"
  }
}
