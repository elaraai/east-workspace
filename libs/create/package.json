{
  "name": "@elaraai/create-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "East project scaffolding — scaffold-core, create-east, create-e3, and the template projects",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" run build",
    "test": "pnpm -r --filter \"./packages/*\" run test",
    "lint": "pnpm -r --filter \"./packages/*\" run lint"
  }
}
