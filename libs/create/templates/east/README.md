# __DISPLAY_NAME__

East project (AGPL-3.0), Node-only.

## Setup

```bash
npm install
```

## Commands

```bash
npm run build    # compile TypeScript
npm run test     # build and run tests
npm run lint     # lint sources
npm run clean    # remove build output and dependencies
```
