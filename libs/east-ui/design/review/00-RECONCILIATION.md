# Design Review — Reconciliation

Generated by re-checking every review section against the current renderer source.

## Headline

**All 77 review sections reconciled against the current source.** Status by section:

| Status | Count |
|---|---:|
| ✅ resolved — every real discrepancy fixed | 3 |
| ✅ clean — no real drift / verified clean | 2 |
| ↪ superseded — nav, implemented to spec (see `06`) | 2 |
| 🟡 partial — some discrepancies fixed, others open | 45 |
| 📋 open — untouched | 24 |
| 🏗 deferred-structural | 1 |
| ⛔ blocked | 0 |

- **Resolved:** `recipe:code`, `slot:editableChip`, `slot:pagination`.
- **Clean:** `slot:breadcrumb`, `slot:segmentedMeter`.
- **Superseded:** `slot:navList`, `component:navigation-deep` (see `06-nav-implemented-to-spec.md`).
- **Deferred-structural:** `slot:planner`.

The **partial** sections each had a discrepancy resolved by one of the 10 fixes (most commonly the systemic ink fix) while other discrepancies remain — see the per-section tables for exactly which.

**Discrepancies resolved vs open (across all sections):**

- Total resolved: **101**
- Total open: **288**

The single largest lever was foundation change #1 (`fg.DEFAULT`/`fg.default` → `brand.900` #111b22, plus `colors_and_type.css --fg-primary` → `var(--brand-900)`), which resolved the primary-ink discrepancy across ~30 components. Change #3 (matrix off-palette), #4 (table header), #6 (pagination), #7 (editableChip), #8 (codeBlock colours), and #9 (dialog/drawer backdrop scrim) account for most of the remaining resolutions.

## Reconciliation by section

### Doc 01 — Theme / tokens / foundation

| Item | Status | Resolved | Open | Key open items |
|---|---|---|---|---|
| theme-config | 🟡 | 1 | 5 | spec-internal duplicate-hex consolidation (`--info`/`--brand-d`, `--brand-dd`/`--ink-2`) and brandHeat empty-cell anchor; three documented intentional deviations stay |
| spec:spec-token-conflicts | 🟡 | 1 | 5 | off-4px-grid type/spacing reconciliation (tokens.ts:131 still integer-only), `--info`/`--brand-d` & `--brand-dd`/`--ink-2` dups, missing spec.css dark block, `--ink-5`/gray.400 unmapped fg role |
| spec:spec-internal-dupes | 📋 | 0 | 8 | dup `.btn.primary:hover` (717), `--brand-700` vs `--brand-dd` in `.evt-committed`, split `.eyebrow-row`, 3 unused selectors, dead `_dummy` rule, `!important`, mono-label dup |

### Doc 02 — Atom recipes

| Item | Status | Resolved | Open | Key open items |
|---|---|---|---|---|
| recipe:badge | 🟡 | 1 | 4 | brand-variant text (brand.800 vs brand.700/600) + border (brand.500 vs brand.600); warn-hue spec inconsistencies (stale #d4a74f) |
| recipe:button | 🟡 | 1 | 4 | default md font-size 14px vs spec 12.5px (major); commit tracking 0.12em vs 0.14em; commit padding 12/20 vs 14/22; base line-height 1 vs 1.15 |
| recipe:chip | 📋 | 0 | 7 | base/brand chip text wants brand.700 not brand.900 (fg fix made it brand.900); `more` tone drifts (mono 10.5px, brand.600 border, brand.700 color, gap, padding) |
| recipe:code | ✅ | 1 | 0 | none — flagged ink inconsistency resolved; rest intentional |
| recipe:heading | 🟡 | 1 | 4 | xs/h5 fontFamily DM Sans vs spec Inter Tight (major); xl line-height 1.1 vs 1.25; sm tracking -0.01em; no h6 tier |
| recipe:icon-button | 🟡 | 2 | 2 | xs glyph 12px vs 13px; base-radius spec inconsistency (.x-btn 4px vs .btn 6px) |
| recipe:input | 🟡 | 1 | 6 | font-size 14px vs 13px (major) + padding 8/12 vs 7/10 (major); resting-border spec ambiguity; _hover/radius-comment/focus-doc nits |
| recipe:kbd | 🟡 | 1 | 6 | paddingX 8px vs 6px (major); letterSpacing 0.02em vs 0.04em; paddingY 1px vs 2px; inert-size/non-spec-variant/line-height nits |
| recipe:link | 📋 | 0 | 2 | spec two-atom link conflict (inline `a` 16px/400 vs `.btn-link` 12.5px/500); renderer weight-500 hybrid; doubled hover |
| recipe:mark | 📋 | 0 | 2 | default text brand.800 vs spec brand.700 on brand-tint ground; je-verb padding gap |
| recipe:note | 🟡 | 1 | 6 | accent stripe colour (brand.500 vs brand.600) + width (2/4px overriding 3px), italic leak into callout/narrative, py 8px vs 12px (all major) |
| recipe:separator | 📋 | 0 | 1 | default variant 'strong' (gray.300) vs spec `.rule` gray.200 |
| recipe:skeleton | 📋 | 0 | 4 | dead/bypassed `elara-skeleton` recipe; linear-shimmer keyframe has no spec analog; pulse 0.4 floor + rect 20px-fallback nits |

### Doc 03 — Slot recipes

| Item | Status | Resolved | Open | Key open items |
|---|---|---|---|---|
| accordion | 🟡 | 1 | 5 | chevron `fg.muted` (gray.600) vs spec `--ink-4`; content padding 20/16 vs 18/14; trigger gap 8 vs 10; body line-height 1.55 vs 1.5 |
| actionBar | 🟡 | 1 | 9 | generic flex toolbar of outline Buttons (commit/commit-primary variants unused); no grid, no flush cells, no brand primary cell; ActionBarItemType has no primary flag (IR) |
| avatar | 🟡 | 1 | 3 | brand variant lacks `.mx-avatar` weight 700 + tracking 0.05em; no 24px/9px tier |
| barStrip | 📋 | 0 | 8 | untoned fill brand.600 vs spec `--brand` brand.500; track/fill `radii.full` pill caps vs spec 3px/1px; value 11px gray.600 (not fg) |
| breadcrumb | ✅ | 0 | 0 | none — only spec-internal/dead-code cleanups (legacy `.hd-crumb`, unused `breadcrumb` text-style) |
| carousel | 🟡 | 1 | 2 | prevTrigger/nextTrigger slots dead (4px squares declared, circular IconButtons rendered); inactive dot gray.300 vs spec `.dot --ink-4` |
| checkbox | 🟡 | 1 | 6 | indeterminate fill brand.600 teal vs spec `--ink-4` grey (major); 16px box vs 14px (major); radius 4 vs 3; border 1 vs 1.5; label 14 vs 13; size contract |
| codeBlock | 🟡 | 4 | 7 | recipe dead/unconsumed (hand-styled Box tree); body 14px vs 11.5px; uniform 12px pad vs 14/16; line-height 1.5 vs 1.6; plain title; dark border gray.700 vs gray.800; generic copy IconButton |
| collapsible | 🟡 | 1 | 2 | indicator chevron never rendered (East type has no chevron field — dead slots); trigger tracking 0.14em vs 0.18em |
| combobox | 🟡 | 1 | 5 | resting border `border.subtle` (gray.200) vs `--rule-strong` gray.300 (major); focus brand.500 vs brand.600; padding 8/12 vs 7/10; input 14 vs 13; item fontSize literal |
| commandPalette | 🟡 | 2 | 9 | recipe entirely unconsumed (index.tsx builds from ChakraDialog + inline Box/Input/Kbd/Text); group label body-font 12px/500/0.02em; unboxed Chakra Kbd |
| commitBar | 🟡 | 2 | 3 | Apply rest fill brand.700 vs spec `--brand-d` brand.600 (renders as the spec hover tone); hover brand.800 vs brand.700; danger-hover + disabled lack spec atoms |
| dataList | 🟡 | 1 | 4 | itemLabel `fg.muted` (gray.600 `--ink-3`) vs spec `--ink-4` (major); label tracking 0.12em vs 0.14em; label column 120 vs 116; gap 16 vs 14 |
| dialog | 🟡 | 1 | 6 | title color literal `gray.900` (not `fg`, fix #1 doesn't reach it); description/eyebrow color; eyebrow→title gap; radius spec-internal; dead header/footer/closeTrigger slots |
| drawer | 🟡 | 1 | 9 | no drawer atom in spec (floating slide-over vs inline briefing); title weight/size/line-height; divider colors; description/title literal (not fg) |
| editableChip | ✅ | 9 | 0 | none (trailing-glyph choice is intentional) |
| emptyState | 🟡 | 1 | 6 | description color (`fg.subtle` vs `fg.muted`); Root+Content padding stacks; missing `.checklist` sub-pattern; superseded recipe indicator/title values |
| eyebrowRow | 🟡 | 1 | 4 | meta flex-shrink still 0 (should be 1); lbl flex-shrink missing; meta overflow/ellipsis missing; root min-width missing |
| field | 📋 | 0 | 3 | label tracking 0.12em vs 0.18em; label color (`caption.eyebrow` `fg.muted`); compounding 4+4=8px label gap vs spec 6px |
| fileUpload | 🟡 | 1 | 7 | dropzone bg `bg.subtle` vs `bg.canvas`; prompt 14 vs 13.5; item filename body-13 vs mono-12; icon color `fg.muted` vs `fg.subtle`; icon mb |
| gantt | 📋 | 0 | 8 | recipe dead code (not consumed); bar fill defaults blue; axis mono/color; milestone color+border; missing now-line |
| hoverCard | 📋 | 0 | 4 | inline `padding="14px 16px"` override kills size variants; content.color literal gray.900 (not fg); radius/shadow spec-internal |
| matrix | 🟡 | 4 | 5 | recipe dead code (inline, not consumed); cell height 52px vs 44px; bars fill cell vs fixed 24px; colHeader body font; legend/avatar unwired; selection tint overlay recoloured not removed |
| menu | 📋 | 0 | 3 | destructive item variant absent (IR/deferred); item.color literal gray.900 (not fg); group/accelerator/icon unsupported (IR) |
| meter | 📋 | 0 | 2 | fill background brand.600 (`--brand-d`) vs spec `--brand` brand.500; border-radius full vs 3px |
| metricChip | 📋 | 0 | 8 | rendered font-size 14px vs 11.5px; brand text brand.700 vs brand.600; radius 2 vs 3; flat bg `bg.subtle` vs `bg.canvas` |
| navList | ↪ | 0 | 4 | nested sub-item + divider IR feature gaps (no renderer change until NavList IR supports them); eyebrow font-size spec inconsistency |
| optionList | 🟡 | 1 | 3 | itemDescription `fg.muted` → `fg.subtle`; padding 14/18 → 12/16; `_hover` `bg.canvas` → `bg.muted` code/comment conflict |
| pagination | ✅ | 10 | 0 | none (per change list ALL RESOLVED; a few one-step nits — resting text brand.900 vs gray.600, prev/next box, ellipsis — noted but treated resolved) |
| planner | 🏗 | 0 | 14 | whole-recipe dead code + structural Gantt-vs-CSS-grid model mismatch (renderer rebuild or recipe deletion); event states, now-line, conflict marker, capacity bar, cell states, bucketing, scenario chrome |
| popover | 🟡 | 1 | 4 | content radius 6 → 4; title tracking 0.14em → 0.18em; min/max width 240/360 vs 320–480 band; (content.color literal gray.900 — resolved-with-caveat) |
| progress | 📋 | 0 | 3 | inert `trackColor` override (var/token mismatch — no-ops); `caption.eyebrow` label tracking + color drift; indeterminate/striped/valueText/circular gaps |
| radioGroup | 🟡 | 1 | 4 | itemControl 16px → 14px; inner dot scale 0.5 (~8px) → fixed 6px; itemText 14 → 13; dead root.gap / 12px Stack gap |
| segmentedMeter | ✅ | 0 | 0 | flush-vs-gapped adopt-or-delete design call (gap, radius, inside-label typography) deferred; no off-palette colour drift |
| segmentGroup | 📋 | 0 | 6 | divider `border.strong` → `border.subtle`; active text brand.800 → brand.700; add `textTransform:uppercase`; tracking 0.06em vs 0.12em; md size |
| select | 🟡 | 1 | 3 | trigger border `border.subtle` → `border.strong`; caret/indicator `fg.muted` → `fg.subtle`; padding 8/12 vs 7/10; body-font default variant |
| showMore | 📋 | 0 | 1 | drop `_hover` `textDecoration:underline` to match colour-only `.btn-link:hover` |
| sliceEdit | 🟡 | 3 | 13 | close × (`fg`/near-black + 13px glyph vs `--ink-4` + 10px); muted roles want `--ink-2`/`--ink-4` but use `fg.muted`; grid-rounded gaps/spacings cluster |
| sliceFrame | 🟡 | 3 | 4 | frameFooter/legendValue `fg.muted` → `fg.subtle`; inline-row gap 12 → 16px; `.frame` radius 10-vs-4 spec-internal; searchKbd `.kbd` nits |
| slider | 📋 | 0 | 8 | range/fill + thumb-border brand.500 vs brand.600; thumb 12px vs 14px; stray console.log; dead marker/valueText slots |
| stat | 🟡 | 1 | 4 | `caption.eyebrow` label color `fg.muted` (spec `fg.subtle`/`--ink-4`); label tracking 0.12em vs 0.16em; wrong-atom docstring |
| status | 📋 | 0 | 5 | label color `fg.subtle` vs spec `fg.muted`; neutral indicator `fg.muted` vs `fg.subtle`; root gap 8 vs 6 (gray-role swaps, not fg-vs-ink) |
| switch | 🟡 | 1 | 1 | colorPalette plumbing — `_checked` hardcodes `brand.600`, threaded colorPalette prop dead for on-state |
| table | 🟡 | 2 | 3 | numeric cell right-align + mono (no align/numeric field in TableColumn IR); body cell flex-centered vs vertical-align:top; footer/total double-rule |
| tabs | 🟡 | 1 | 5 | inactive trigger `fg.muted` vs spec `--ink-4` (`fg.subtle`); trigger padding 12/12 vs 14/16; list gap 16 + no 20px inset; stale doc citations; focus ring |
| tag | 📋 | 0 | 7 | close-× `fg.muted` vs `fg.subtle`; dashed text `fg.muted` vs `fg.subtle`; brand text brand.800 vs brand.700; gap 4 vs 6 |
| toast | 📋 | 0 | 7 | `info.subtle` blue-grey tint clashes with teal `fg.info` border (major); radius 6 vs 4; padding 12/16; indicator 16px; no enter/exit motion |
| tooltip | 🟡 | 1 | 5 | missing fontWeight 600; missing letterSpacing 0.06em; fontSize 11 vs 10; radius 4 vs 2; padding 4/8 vs 3/6 |
| treeView | 🟡 | 1 | 6 | `_selected` bg `bg.brand.subtle` (#e8f6f7) violating principle-05 dirty-edit reservation (critical); hover paper-3 vs paper-2; `_selected` text brand.800 |

### Doc 04 — Component-level renderers

| Item | Status | Resolved | Open | Key open items |
|---|---|---|---|---|
| component:charts | 🟡 | 2 | 6 | Recharts axis-tick typography (sans, not mono/10px/ink-4); sparkline default stroke/width/fill; pie/composed/area-range fallbacks; brush traveller raw #666; gridline spec inconsistency |
| component:layout-primitives | 📋 | 0 | 4 | Card body paddingX 20px vs header/footer 18px; stale `card` layer-style vs 10px Frame; unstyled Splitter handle; chip-rail dot separator `inherit` fallback |
| component:slice-composites | 📋 | 0 | 7 | sliceFrame.root radius 4px vs `.frame` 10px; chip neutral/brand/more text too dark (fg→brand.900 but spec wants brand.700); eyebrow pills reuse generic `.chip` at 12px/500 |
| component:typography-extras | 📋 | 0 | 4 | List default disc marker not brand-teal (`.dotlist` dot); Numeric `mono.lg` no weight (spec `.num` 500); Mark/Highlight Chakra warm-yellow; standalone Code on host recipe |
| component:display-extras | 🟡 | 1 | 3 | no Avatar recipe encoding `.avatar` mono/600/ink-2 atom; icon colorPalette resolves raw `-500` shade; +N overflow chip omits size/borderColor |
| component:forms-composites | 🟡 | 1 | 6 | date/time segment digits in body font (not JetBrains Mono); range field shells round 6px / border gray.200 / pad 12-6 / focus brand.500; tags-input raw chips; radio-card fill; TimeScaleControl no-op variant |
| component:collections-inline | 🟡 | 5 | 2 | table pinned-column border hex fallbacks `#e2e8f0`/`#fafafa` still wrong; matrix legend/header text still literal gray.700 |
| component:overlays-inline | 🟡 | 3 | 7 | command-palette still ignores `commandPaletteSlotRecipe` (raw ChakraDialog; maxH 400, item 14px, body-font group labels); dialog eyebrow inline; dialog boxShadow `lg` vs xl; popover radius 6px; inline-duplicate-recipe nits |
| component:navigation-deep | ↪ | 2 | 3 | groupLabel section eyebrow 9.5px vs spec 10.5px; no nested sub-item path; per-item badge / gray.500 role / text-style-vs-recipe nits |
| component:buttons-extras | 📋 | 0 | 5 | `ButtonVariantType` subtle/plain have no recipe analog; Toggle has no canonical pressed paint; CopyButton icon-only solid fill; no closeButton recipe; commit-tracking 0.12em nit |
| component:feedback-extras | 🟡 | 1 | 7 | partial/info banner pairs slate bg with teal (`fg.info`) border+icon; description body `fg.muted` not `--ink`; change-banner border/icon too light; radius/padding/title-size; three-banner-system spec inconsistency |

## Still open — prioritised

### Quick wins (single-token / single-value renderer edits)

- **meter** — fill background `brand.600` → `brand.500` (both spec bar atoms use `--brand`), border-radius `full` → 3px. The doc comment still says "brand-d fill"; one clean fix.
- **barStrip** — untoned fill `brand.600` → `brand.500`; track/fill `radii.full` → 3px/1px caps; value color literal gray.600 → `--ink`.
- **separator** — flip `defaultVariants.variant` from 'strong' (gray.300) to the `.rule` gray.200 default.
- **showMore** — drop `_hover` `textDecoration:underline` + offset to match colour-only `.btn-link:hover`.
- **slider** — range/fill + thumb-border `brand.500` → `brand.600`; thumb 12px → 14px; remove stray `console.log` in `forms/slider/index.tsx:66`; prune dead marker/valueText slots.
- **commitBar** — Apply button rest fill `brand.700` → `brand.600` and hover `brand.800` → `brand.700` (today it renders one step too dark — the spec hover tone at rest).
- **treeView** — `_selected` background `bg.brand.subtle` (#e8f6f7) → `bg.subtle` (paper-3); this brand-tint is reserved for dirty-edit per principle 05 (critical), plus `_selected` text brand.800 → fill-only/brand.700.
- **The `fg.muted` vs `fg.subtle` cluster** (gray-role swaps, all single-token) — accordion chevron, dataList itemLabel, optionList itemDescription, fileUpload prompt icon, select caret/indicator, status label + neutral dot, tag close-× + dashed text, tabs inactive trigger, sliceEdit/sliceFrame muted roles, banner description, command-palette/dialog eyebrow.
- **The brand.800 → brand.700 text cluster** — recipe:chip, recipe:mark, segmentGroup active text, metricChip brand sentiment, tag brand variant, slice-composites chip tones (note: the foundation fg fix took these to brand.900, but spec wants brand.700 — they remain drift).
- **dialog / drawer / hoverCard / menu / popover literal `gray.900` titles** — foundation fix #1 did not reach hardcoded `{colors.gray.900}` literals; switch each to the `fg` role (or `brand.900`) to land at spec `--ink`.
- **progress** — fix the inert `style.trackColor` override (writes `--chakra-colors-bg-emphasized`, track reads literal `{colors.gray.100}` — silent no-op).
- **table** — zero `borderBottomWidth` on footer/total cell to remove the double rule; switch body cell to `vertical-align:top`.
- **component:collections-inline** — correct the table pinned-column border hex fallbacks `#e2e8f0`/`#fafafa`; make matrix legend/header use a semantic token instead of literal gray.700.
- **component:display-extras** — icon colorPalette should resolve the ink shade, not raw `-500` (brand → brand.500 instead of brand.700).

### Structural (IR change, renderer rebuild, recipe wiring, or shared chrome)

- **gantt** — slot recipe is dead code (no `useSlotRecipe`); bars default to colorPalette "blue", axis ticks body-font 14px (wrong family — mono/`fg.subtle`, so the ink remap is irrelevant), milestones default blue with no white border, no now-line. Wire the recipe + add status palette + now-line.
- **planner** (🏗 deferred-structural) — `plannerSlotRecipe` dead; `collections/planner` is the TanStack/SVG horizontal-Gantt model, not the spec CSS-grid scheduling Planner. The full event-state grammar, now-line, conflict marker, capacity bar, cell states, bucketing, scenario chrome are unimplemented — renderer rebuild or recipe deletion.
- **matrix size/shape** — recipe still inline (not consumed); live cell height 52px vs spec 44px; bars fill the cell vs spec fixed-24px centered; colHeader body font (not mono eyebrow); legend/avatar slots unwired; selection tint overlay recoloured but not removed.
- **commandPalette / overlays-inline** — recipe entirely unconsumed; `index.tsx` builds from `ChakraDialog` + inline Box/Input/Kbd/Text. Wire `commandPaletteSlotRecipe` (mono group labels, spec `.kbd` chips, list maxH 320, item 13px).
- **actionBar** — replace the gapped flex of outline Buttons with the `.commit-bar` grid of flush mono cells; needs an `ActionBarItemType` primary/role flag (IR) for the brand primary cell, plus commit/commit-primary variant wiring.
- **table numeric IR** — `TableColumn` has no `align`/`numeric` field; numeric cells can't right-align/use mono tabular figures until the IR adds it.
- **shared table / footer chrome** — footer pager button radius is ad-hoc inline (not the shared IconButton ghost recipe); column-resize and pinned-column treatments are inconsistent.
- **menu** — destructive/danger item variant, group/accelerator slots, and leading-icon support all require East `Menu.Item` IR fields (deferred-structural).
- **navList / navigation-deep** (↪ superseded by `06-nav-implemented-to-spec.md`) — remaining items are IR feature gaps: nested sub-item and divider item-types (no renderer change until the NavList IR supports them).
- **codeBlock** — recipe dead/unconsumed (hand-styled Box tree); body fontSize 14px (vs 11.5px), uniform 12px padding, line-height 1.5, plain-Text title, generic copy IconButton. Adopt the recipe or pin the rendered chrome.
- **buttons-extras** — reconcile East `ButtonVariantType` (solid/subtle/outline/ghost/plain) with `buttonRecipe` variants (subtle/plain fall through to Chakra); add Toggle recipe-level pressed paint; register a `closeButton` recipe matching `.x-btn`.

### Spec consolidation (spec.css / token-side, no renderer change)

- **spec:spec-token-conflicts** — reconcile off-4px-grid type/spacing values (spec uses 8.5/9.5/…px and 5/6/10/14/18/22px padding) against the strict-4px-grid `tokens.ts:131` claim; collapse `--info`==`--brand-d` and `--brand-dd`==`--ink-2` duplicates; add a dark block to `spec.css` (or declare it light-only); give `--ink-5`/gray.400 a named fg role.
- **spec:spec-internal-dupes** — delete the duplicate `.btn.primary:hover` (717) and dead `._dummy_btn_primary_anchor_` (718-720); change `.evt-committed` from raw-scale `var(--brand-700)` to `--brand-dd`; fold the split `.eyebrow-row` (4077-4093) into the 386-410 atom; remove unused `.evt-span` / `.planner-capacity-bar` / `.planner-now.thead`; drop `!important` in `.evt-scenario-both`; extract a `.mono-label` utility for the repeated mono-uppercase cluster.
- **theme-config** — the same duplicate-hex consolidation (`--info`/`--brand-d`, `--brand-dd`/`--ink-2`) plus a `brandHeat` empty/resting-cell anchor (#f1f5f5) so resting empties don't route through `bg.muted`.
- **feedback-extras** — consolidate the three overlapping `.banner`/`.bn` rule blocks with conflicting radii/paddings (spec-internal); add slate background token so partial/info banner stops pairing slate bg with a teal border+icon.