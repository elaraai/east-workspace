## Single-part recipes (theme/recipes/*)

Conformance review of each single-part Chakra recipe against the canonical `design/` visual spec (`spec.css`, `colors_and_type.css`, `slice.html`). Discrepancies keep EXACT px/hex values and are sorted by severity.

---

### `recipe:badge`  — minor-drift
> 🟡 Partial — outline/plain text now reads spec `--ink` (foundation `fg` → brand.900) ✅; still open: brand-variant text `{colors.brand.800}` vs spec brand.700/600, brand border `border.brand` (brand.500) vs spec brand.600, and the two spec-internal warn-hue inconsistencies (stale #d4a74f). See `00-STATUS.md`.
- **Spec anchors:** spec.css:245-255 .pattern-anchor · spec.css:1124-1134 .stakes-tag / .stakes-tag.{low,mid,high,crit} · spec.css:1197-1206 .diff-pill / .diff-pill.{ok,block,warn} · spec.css:22-29 :root --brand/--brand-d/--brand-dd/--brand-tint/--pos/--neg/--warn/--ink · colors_and_type.css crosswalk · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/badge.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/display/badge/index.tsx · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts:33-114 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts:36-119
- **Summary:** Re-checked every discrepancy against actual spec.css and badge.ts. The recipe is largely faithful to the tag/pill family, confirmed by token resolution and side-by-side PNG (Display__display_badge.png vs decide__pattern__stakes-tag.png): mono / 10px / 700 / uppercase / 0.16em / 3px radius / 1px coloured border / faint per-hue tint. ok and danger variants match their spec backgrounds (rgba(47,122,91,0.06) and rgba(184,90,74,0.06)) and ink (--pos #2f7a5b, --neg #b85a4a) exactly. CORRECTION TO FIRST PASS: the first pass mis-resolved the brand variant — it does NOT match --brand. The brand (and stakesMid) variant hardcodes color {colors.brand.800} (#1f363d), darker than both spec brand-chip hues (.pattern-anchor brand.600 #3a7780, .stakes-tag.mid brand.700 #2b4b55), and bypasses the dedicated brand.fg role that would match .stakes-tag.mid exactly — promoted to a major renderer-drift the first pass missed. Its border is brand.500 (#488e97) vs .stakes-tag.mid's brand.600 (#3a7780) — added as minor drift. The outline text fg-vs--ink drift (gray.900 #1a2626 vs brand.900 #111b22) is confirmed real (minor). The two warn issues are confirmed spec-internal-inconsistency: .stakes-tag.high / .diff-pill.warn hardcode the stale #d4a74f / rgb(212,167,79) at 0.10 contradicting the spec's own --warn #b8862d; the renderer correctly uses #b8862d but at 0.14 opacity.
- **Confirmed aligned:** base typography (mono / 10px / 700 / uppercase / 3px radius / 1px border / lineHeight 1.2), ok variant (rgba(47,122,91,0.06) + #2f7a5b), danger variant (rgba(184,90,74,0.06) + #b85a4a), tag-family status-rule check (tinted pills, not dot+word), visual confirmation against decide__pattern__stakes-tag.png, index.tsx adds no inline overrides.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| brand variant text colour (color) | .pattern-anchor color = --brand-d #3a7780 (brand.600); .stakes-tag.mid color = --brand-dd #2b4b55 (brand.700, = brand.fg role) | color: {colors.brand.800} = #1f363d | renderer-drift | 🟠 major | Renderer side (badge.ts:53,83): change brand and stakesMid color from {colors.brand.800} to brand.fg (= brand.700 #2b4b55) to match .stakes-tag.mid, or {colors.brand.600} (#3a7780) to match .pattern-anchor. Pick one spec source — .stakes-tag.mid is the closer analog for a bordered chip. |
| brand variant border colour | .pattern-anchor has NO border (background-tint only); .stakes-tag.mid border-color = --brand-d #3a7780 (brand.600) | borderColor: border.brand = brand.500 #488e97 | renderer-drift | 🟡 minor | Renderer side (badge.ts:52,82): change brand/stakesMid borderColor from border.brand (brand.500) to {colors.brand.600} (#3a7780) to match .stakes-tag.mid border-color. |
| outline variant text colour (color: fg) | .diff-pill default text / cool primary ink = --ink #111b22 (brand.900) | color: fg = fg.DEFAULT = gray.900 #1a2626 | renderer-drift | 🟡 minor | Renderer side (badge.ts:59,89): set outline + plain variant color to {colors.brand.900} (#111b22) to match --ink; OR remap fg.DEFAULT to brand.900 workspace-wide (semantic-tokens.ts:73,79). |
| warn variant background tint (hue + opacity) | .stakes-tag.high / .diff-pill.warn background = rgba(212,167,79,0.10) (stale hue #d4a74f) | warning.subtle.strong = status.warnSubtleStrong = rgba(184,134,45,0.14) (canonical --warn #b8862d, opacity 0.14) | spec-internal-inconsistency | 🟡 minor | Spec side (spec.css:1133,1206): replace the rgba(212,167,79,0.1) backgrounds with rgba(184,134,45,0.1) and use var(--warn) instead of the stale #d4a74f. Then align renderer warning.subtle.strong opacity to 0.10 (tokens.ts:115), OR point the warn variant at warning.subtle (0.08) — either way both sides should agree on hue #b8862d and one opacity. |
| warn variant border + text colour | .stakes-tag.high border-color #d4a74f (stale), color #8a6420 (warn-dd, two-tone); .diff-pill.warn border + color var(--warn) #b8862d | borderColor + color both fg.warning = status.warn #b8862d | spec-internal-inconsistency | 🟡 minor | Spec side (spec.css:1133): collapse .stakes-tag.high to border-color/color: var(--warn) (matching .diff-pill.warn) so all warn atoms agree on #b8862d. No renderer change for hue. |
| base letterSpacing | .pattern-anchor 0.16em (spec.css:249); .stakes-tag 0.14em (spec.css:1126); .diff-pill 0.04em (spec.css:1201) | letterSpacings.wider2 = 0.16em (from .pattern-anchor) | intentional-deviation (intentional) | ⚪ nit | No change required; if exact .stakes-tag fidelity is wanted later, add a 0.14em option. |
| base paddingY / fontWeight blend across source atoms | .stakes-tag padding 3px 8px, weight 700; .pattern-anchor padding 2px 8px, weight 600 | size sm: paddingY 2px, paddingX {spacing.2}=8px; base fontWeight bold(700) | intentional-deviation (intentional) | ⚪ nit | No change required; optionally bump size sm paddingY to 3px to match .stakes-tag exactly if .stakes-tag is the canonical badge target. |
| count / callout variant — no literal spec atom | No dedicated .count-pill / .callout-pill atom in spec; recipe cites informal 'Spec Badge & progress' (paper-3 count, brand-d callout, radius-full) | count: bg gray.100 (#f1f5f5 = --paper-3) / color brand.700 (#2b4b55 = --brand-dd) / radius full / semibold / 11px / paddingX 7px; callout: bg brand.600 (#3a7780 = --brand-d) / white / radius full / semibold / 11px / paddingX 7px | spec-internal-inconsistency (intentional) | ⚪ nit | Spec side: add an explicit .count-pill / .callout-pill atom to spec.css (near .diff-pill) so these variants have a citable source. |

---

### `recipe:button`  — minor-drift
> 🟡 Partial — outline/ghost-hover text now reads spec `--ink` (foundation `fg` → brand.900) ✅; still open: default md font-size 14px vs spec 12.5px (major), commit letter-spacing 0.12em vs 0.14em, commit padding 12/20px vs 14/22px, and base line-height 1 vs 1.15. See `00-STATUS.md`.
- **Spec anchors:** spec.css:413-460 (.btn, .btn:hover, .btn.primary, .btn.primary:hover, .btn.ghost, .btn.ghost:hover, .btn.danger, .btn.danger:hover, .btn.compact, .btn.commit-mono, .btn-link) · spec.css:696-721 (.commit-bar .btn, .commit-bar .btn:hover, .commit-bar .btn.primary, .commit-bar .btn.primary:hover) · spec.css:12-16 (:root --ink #111b22, --ink-2 #2b4b55, --ink-3 #4a5f5f, --ink-4 #6b8080) · colors_and_type.css :root (--rule #e2e8e8, --rule-strong #cbd5d5, --brand-d #3a7780, --brand-dd #2b4b55, --neg #b85a4a, --paper #ffffff, --paper-2 #f8fafa, --paper-3 #f1f5f5) · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/button.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/buttons/button/index.tsx
- **Summary:** The button recipe is structurally faithful to spec .btn with one deliberate re-roling: the renderer DEFAULT variant is `solid` (brand.600 fill) whereas spec's base `.btn` is the white outline button. The recipe maps spec .btn -> `outline`, .btn.primary -> `solid`, .btn.ghost -> `ghost`, .btn.danger -> `danger`, .btn.commit-mono/.commit-bar .btn -> `commit`/`commit-primary`, plus a renderer-only `ink` dual-CTA. Per-counterpart the fills/borders/hovers resolve to the correct spec hexes (brand.600 #3a7780 fill, brand.700 #2b4b55 hover, border.strong #cbd5d5 outline, neg #b85a4a danger, rule #e2e8e8 commit divider, paper-2 #f8fafa commit hover). I re-read both sides and confirmed every first-pass discrepancy is REAL with correct crosswalk resolution and well-calibrated severity. Real drifts: (1) MAJOR - default md label size is 14px (md->fontSizes.sm) vs spec 12.5px; no tier hits 12.5px; (2) MINOR - outline (and ghost-hover) text color 'fg' resolves to gray.900 #1a2626 vs spec --ink brand.900 #111b22 (the crosswalk whole-renderer ink mismatch); (3) MINOR - commit letter-spacing 0.12em (widest) vs spec 0.14em; (4) MINOR - commit padding rounded to 12/20px vs spec off-grid 14/22px; (5) NIT - base line-height 1 vs 1.15. Documented intentional deviations (nit): outline hover bg-tint instead of border-darken (attached-group rationale, recipe lines 100-106), ghost omits the transparent border, and fixed hit-target heights instead of spec's padding-derived height. Spec gaps: no .btn:disabled / :active atom though the renderer defines both, and no spec atom for the renderer `ink` dual-CTA variant.
- **Confirmed aligned:** radius md 6px, fontFamily body (Inter Tight), outline weight 500, outline bg white #ffffff, outline border 1px #cbd5d5, outline paddingX 12px, solid fill brand.600 #3a7780, solid hover brand.700 #2b4b55, solid white + semibold 600, ghost color #4a5f5f, ghost hover bg #f1f5f5, danger color #b85a4a, danger hover border #b85a4a, commit mono/uppercase/600/11.5px, commit borderLeft 1px #e2e8e8, commit bg white + #4a5f5f, commit hover bg #f8fafa, commit-primary brand.600/brand.700, compact 11.5px, focus ring (3px brand).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| base/default font-size | .btn 12.5px (default) | size md fontSize = fontSizes.sm = 14px (no base fontSize; sm tier=12px, xs/compact=11.5px) | renderer-drift | 🟠 major | Renderer side: set md fontSize to a 12.5px literal so the canonical button reads at spec size (keep height 32px); or add a 12.5px tier. Confirm against snapshot. |
| outline variant text color (and ghost-hover text color) | --ink #111b22 (brand.900) | color 'fg' -> fg.DEFAULT = gray.900 = #1a2626 | renderer-drift | 🟡 minor | Renderer side: add an fg.ink role mapped to {colors.brand.900} and use it for the outline color (and ghost _hover color), or re-point fg.DEFAULT to brand.900. Consolidate the ink decision once at the token layer rather than per-recipe. |
| commit / commit-primary letter-spacing | 0.14em (.btn.commit-mono and .commit-bar .btn letter-spacing:0.14em) | letterSpacings.widest = 0.12em | renderer-drift | 🟡 minor | Renderer side: use a 0.14em literal for commit/commit-primary letterSpacing, or add a 0.14em token tier; OR spec side: consolidate commit tracking to 0.12em if the 0.14/0.16/0.18 spread is unintentional. |
| commit / commit-primary padding | .commit-bar .btn padding 14px 22px (off-grid) | paddingY spacing.3 = 12px, paddingX spacing.5 = 20px | renderer-drift | 🟡 minor | Renderer side: use literal 14px/22px for the commit variants if commit-bar pixel-fidelity matters; OR spec side: accept grid rounding and update .commit-bar .btn to 12px 20px to consolidate onto the grid. |
| base line-height | .btn line-height 1.15 | base lineHeight '1' | renderer-drift | ⚪ nit | Renderer side: set base lineHeight to 1.15 to match spec, or leave as-is given fixed-height centering (cosmetic). |
| vertical sizing model: padding vs fixed height | .btn padding 6px 12px (no fixed height; height derived from padding + 12.5px/1.15 line box) | no paddingY; size md height fixed 32px (sm 28px, xs 26px, lg 40px), text vertically centered | intentional-deviation (intentional) | ⚪ nit | None required. Record only; the fixed-height model is an intentional hit-target choice. |
| outline variant hover cue | .btn:hover border-color var(--ink-3) #4a5f5f (border darkens, bg stays white) | _hover background bg.subtle gray.100 #f1f5f5 (bg tints, border unchanged) | intentional-deviation (intentional) | ⚪ nit | None - documented intentional deviation. Record only. |
| ghost variant border | .btn base border 1px solid --rule-strong, .btn.ghost border-color transparent (border present but invisible) | ghost variant has no borderWidth (no border box at all) | intentional-deviation (intentional) | ⚪ nit | None visible. Optionally add borderWidth 1px + borderColor transparent to ghost for exact box-model parity, but not visually required. |
| _disabled and _active states | spec has no .btn:disabled or .btn:active rule (no documented disabled/active style) | _disabled background gray.200 #e2e8e8 + color fg.muted #4a5f5f + cursor not-allowed; _active transform scale(0.98) + variant-specific darker fills | spec-internal-inconsistency | ⚪ nit | Spec side: add .btn:disabled (e.g. background var(--rule) #e2e8e8, color var(--ink-4), cursor not-allowed) and an optional :active press, then confirm renderer matches. |
| ink variant (dual-CTA partner) | no spec atom (spec has only .btn.primary as the filled CTA) | background brand.900 #111b22 + white + hover brand.800 #1f363d + active brand.700 | spec-internal-inconsistency | ⚪ nit | Spec side: add a .btn.ink atom (background var(--ink), color #fff, hover var(--brand-d-darker)) if the dual-CTA pattern is canonical, so this renderer variant has a spec counterpart. |

> 📸 Visual verify: `make east-ui-examples-html-buttons/button` then compare the dist-examples PNG to the dist-design spec PNG.

---

### `recipe:chip`  — minor-drift
> 📋 Open — chip targets `--ink-2`/`--brand-dd` (brand.700), not `--ink`, so the foundation `fg`→brand.900 change does NOT resolve base color (now brand.900 vs spec brand.700) — still open along with brand-variant brand.800 vs brand.700 and the five `more`-tone drifts (mono 10.5px, brand.600 border, brand.700 color, gap, padding). See `00-STATUS.md`.
- **Spec anchors:** design/spec.css:463-490 (.chip / .chip.brand / .chip.dashed / .chip.compact / .chip .x / .chip .caret / .chip .num) · design/spec.css:11-31 (:root --ink-2 #2b4b55, --ink-4 #6b8080, --paper #ffffff, --rule-strong #cbd5d5, --brand #488e97, --brand-d #3a7780, --brand-dd #2b4b55, --brand-tint #e8f6f7) · design/slice.html:121-130 (.more-chip — slice-local, NOT in spec.css: gap 5px, padding 4px 9px 4px 11px, border --brand-d, radius 9999px, mono 10.5px, color --brand-dd, weight 700) · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/chip.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/slice/filter/index.tsx · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/slice/breakdown/index.tsx
- **Summary:** Re-checked every discrepancy against the actual spec.css rules (.chip 463-490, :root 11-31), the slice.html .more-chip block (121-130), and the resolved renderer tokens. All seven first-pass discrepancies are REAL after re-reading both sides; I added one the first pass missed (more-tone gap 5px vs inherited 6px). Headline drift is ink/colour: (1) base `color:"fg"` resolves to gray.900 #1a2626 (tokens.ts:57) but spec .chip text is --ink-2 #2b4b55 brand.700 — the documented fg/gray.900-vs-brand-ink trap, a colder/darker neutral label on every neutral chip (major); (2) brand variant uses brand.800 #1f363d where spec --brand-dd is brand.700 #2b4b55, one step too dark (major). The `more` tone (only consumed by slice/filter/index.tsx:153; the breakdown +N more is a separate inline dashed Box, NOT this tone) drifts on five axes vs the slice-local .more-chip: font-size 12px-inherited vs mono 10.5px (major), border brand.500 vs --brand-d brand.600 (minor), color brand.800 vs brand.700 (minor), gap 6px vs 5px (nit), and asymmetric padding (nit). Structure, sizing, base border, background, dashed variant, brand fill/border, compact sizing, numeric mono and pill shape all match exact-hex via the crosswalk.
- **Confirmed aligned:** base font-family body (Inter Tight), base 12px, base weight 500, base padding 4px 10px, base radius 4px, base border 1px #cbd5d5, base bg #ffffff, base gap 6px, base line-height 1, white-space nowrap, brand bg #e8f6f7, brand border #488e97, dashed border + #6b8080, compact 3px 8px / 11.5px, numeric mono, more weight 700, more bg #ffffff, more radius 9999px, pill shape 9999px.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| base color | --ink-2 #2b4b55 (brand.700) | color:"fg" = fg.DEFAULT = gray.900 #1a2626 | renderer-drift | 🟠 major | Renderer: chip.ts:34 change base `color: "fg"` to `color: "brand.fg"` (= brand.700 #2b4b55 per semantic-tokens.ts:36) so the neutral chip text resolves to spec --ink-2. |
| brand variant color | --brand-dd #2b4b55 (brand.700) | {colors.brand.800} #1f363d | renderer-drift | 🟠 major | Renderer: chip.ts:42 change `color: "{colors.brand.800}"` to `color: "{colors.brand.700}"` (or `"brand.fg"`) to match --brand-dd. |
| more variant font-size | .more-chip font-mono 10.5px (slice.html:126) | inherits base 12px (consumer slice/filter:153 passes numeric:true → mono family, shape:pill — but NOT size:sm, so font-size stays 12px) | renderer-drift | 🟠 major | Renderer: add `fontSize: "10.5px", fontFamily: "mono"` to the `more` variant in chip.ts:45 so the pill is self-sufficient and matches .more-chip without relying on the consumer passing numeric. |
| more variant border color | .more-chip border --brand-d #3a7780 (brand.600) (slice.html:124) | border.brand = brand.500 #488e97 | renderer-drift | 🟡 minor | Renderer: chip.ts:45 set the `more` variant borderColor to `{colors.brand.600}` to match .more-chip's --brand-d. |
| more variant color | .more-chip color --brand-dd #2b4b55 (brand.700) (slice.html:127) | {colors.brand.800} #1f363d | renderer-drift | 🟡 minor | Renderer: chip.ts:45 change `more` variant `color: "{colors.brand.800}"` to `"{colors.brand.700}"` (or `"brand.fg"`). |
| more variant gap | .more-chip gap 5px (slice.html:122) | inherits base gap 6px (chip.ts:23) | renderer-drift | ⚪ nit | Renderer: optionally add `gap: "5px"` to the `more` variant in chip.ts:45. Low priority. |
| more variant padding (asymmetry) | .more-chip padding 4px 9px 4px 11px (slice.html:123 — asymmetric, optical balance for the trailing caret) | inherits base symmetric paddingInline 10px / paddingBlock 4px | renderer-drift | ⚪ nit | Renderer: optionally set `paddingInlineStart: "11px", paddingInlineEnd: "9px"` on the `more` variant. Low priority. |
| more tone source atom (scoping) | .more-chip is defined in slice.html:121-130 (component-local), NOT in spec.css | chip.ts:44 comment says `more` is `spec .more-chip`; recipe scope is the spec.css `.chip` atom only | spec-internal-inconsistency | ⚪ nit | Spec: promote `.more-chip` into spec.css as a named atom (mono 10.5px / 9999px radius / --brand-d border / --brand-dd color / weight 700 / gap 5px / padding 4px 9px 4px 11px) so the recipe `more` variant cites a canonical source. Renderer: optionally narrow the chip.ts:44 comment to note .more-chip is currently slice-local. |

> 📸 Visual verify: `make east-ui-examples-html-display/chip` then compare the dist-examples PNG to the dist-design spec PNG.

---

### `recipe:code`  — match
> ✅ Resolved/clean — the one flagged spec-internal inconsistency (`--fg-primary` #1a2626 vs spec `--ink` #111b22) is now reconciled: colors_and_type `--fg-primary` = `var(--brand-900)` and renderer `fg` = brand.900, both #111b22; the inline-code chip is otherwise a pixel match. Remaining items are intentional (context-override font-size, surface≈subtle, additive/size variants). See `00-STATUS.md`.
- **Spec anchors:** colors_and_type.css:173 (.code, code — mono, 0.92em, bg --bg-tertiary, padding 1px 5px, radius --r-sm, color --fg-primary) · colors_and_type.css:51 (--bg-tertiary: var(--gray-100)) · colors_and_type.css:49,113 (--bg-primary:#fff; --r-sm: 4px) · colors_and_type.css:54 (--fg-primary: var(--gray-900)) · colors_and_type.css:37 (--gray-900: #1a2626) · colors_and_type.css:30 (gray-100 #f1f5f5) · spec.css:174 (.conventions code { font-size: 11px } — context override) · spec.css:12 (--ink: #111b22 = brand.900 — NOT what inline code uses) · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/code.ts:17-40 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/code/index.tsx:22-69 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts:47,54,72-79 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts:41,43,49,57 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/index.ts:116
- **Summary:** Confirmed the first pass: the code recipe (theme/recipes/code.ts base + typography/code/index.tsx) is a pixel-exact match to the inline-code spec atom at colors_and_type.css:173. Verified each property by reading both sides and resolving tokens via tokens.ts/semantic-tokens.ts: mono font (JetBrains Mono), fontSize 0.92em (off-grid relative value preserved, NOT rounded), background {colors.gray.100}=#f1f5f5 (== --bg-tertiary), color "fg" -> fg.DEFAULT={colors.gray.900}=#1a2626 (== --fg-primary, the inline-code anchor), radius {radii.sm}=4px (== --r-sm), padding 1px/5px (off-grid preserved). Snapshot Typography__typography_code.png confirms the rendered chip (light-gray bg, dark mono text, 4px radius, tight padding). No renderer drift. Adjustments to the first pass: kept the fg-vs-ink item as spec-internal-inconsistency/nit (the inline-code anchor uses --fg-primary #1a2626, so color:"fg" matches exactly and the crosswalk warning about spec.css --ink #111b22 does not bite here); the .conventions 11px is a documented context override, base stays 0.92em; ADDED an observation the first pass missed — variants.surface (bg.muted) resolves to gray.100 in light mode, making it visually identical to subtle (they only diverge in dark mode), confirmed in the snapshot. Verdict remains match.
- **Confirmed aligned:** font-family mono (JetBrains Mono), font-size 0.92em (off-grid preserved), background #f1f5f5 (--bg-tertiary), color #1a2626 (--fg-primary, exact match to the inline-code anchor), radius 4px (--r-sm), padding 1px 5px (off-grid preserved), registered as theme slot 'code' with defaultVariant 'subtle', snapshot Typography__typography_code.png confirms the chip.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| color / 'primary ink' source | colors_and_type.css:54 --fg-primary = gray.900 = #1a2626 (the inline-code anchor) vs spec.css:12 --ink = brand.900 = #111b22 (spec.css's declared primary ink) | color:"fg" -> fg.DEFAULT base = {colors.gray.900} = #1a2626 | spec-internal-inconsistency | ⚪ nit | Spec-side consolidation only: reconcile colors_and_type --fg-primary (#1a2626) with spec.css --ink (#111b22) so 'primary ink' has one canonical value. If brand.900 is the intended ink, retarget colors_and_type:173 (and the renderer color) to brand.900; otherwise leave the renderer as-is — it correctly follows the current anchor. |
| font-size (context override) | spec.css:174 .conventions code { font-size: 11px } (absolute, only inside .conventions) | 0.92em (base recipe; no .conventions-context override) | spec-internal-inconsistency (intentional) | ⚪ nit | No renderer change. If a docs/conventions surface needs the 11px treatment, set fontSize at that callsite. |
| surface variant resolves identical to subtle | spec inline code has a single subtle/default look (bg --bg-tertiary = gray.100) | variants.surface background:"bg.muted" resolves to {colors.gray.100} = #f1f5f5, identical to subtle's {colors.gray.100}; in light mode surface == subtle | intentional-deviation (intentional) | ⚪ nit | None required. If 'surface' is meant to read distinctly from 'subtle' in light mode, give it a different token; otherwise the overlap is harmless. |
| additive variants (outline/plain/solid) | spec inline code defines only the single subtle/default look | recipe adds outline (transparent + 1px border.strong=gray.300 #cbd5d5), plain (transparent), solid (bg {colors.brand.700}=#2b4b55, color white) | intentional-deviation (intentional) | ⚪ nit | None required. Values are all on-token if these ever become spec'd. |
| size variants | no size variants defined for inline code in spec | recipe declares no size variants; toChakraCode forwards a size prop, so xs/sm/md/lg fall through to Chakra's default Code size scale | intentional-deviation (intentional) | ⚪ nit | None for conformance. If exact size px must be governed, declare explicit size variants in code.ts so they don't fall back to Chakra defaults. |

---

### `recipe:heading`  — minor-drift
> 🟡 Partial — heading ink is reconciled (colors_and_type `.h1-.h6` now `var(--brand-900)`, renderer `fg` = brand.900, all #111b22) ✅; still open: xs/h5 uses DM Sans vs spec Inter Tight (major), xl line-height 1.1 vs 1.25, sm tracking -0.01em not in spec, and no h6 tier. See `00-STATUS.md`.
- **Spec anchors:** colors_and_type.css:161-166 (.h1-.h6 semantic type scale; .h5/.h6 switch to --font-body) · colors_and_type.css:54 (--fg-primary = var(--gray-900) = #1a2626) · colors_and_type.css:70-72,75-96 (--font-brand/--font-body, --fs-*, --fw-*, --lh-*) · spec.css:112-120 (.frontmatter h1 — 44px/700/-0.02em/lh 1.1/--ink) · spec.css:196-204 (.mode h2 — 32px/700/-0.02em/lh 1.15/--ink) · tokens.ts:182-214 (fontSizes 2xl=24/xl=20/4xl=36/3xl=30/5xl=48; lineHeights tight=1.25 snug=1.375 normal=1.5; letterSpacings tighter=-0.02 tight=-0.015 snug=-0.01) · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/heading.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/heading/index.tsx · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/text-styles.ts:27-70 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/text-styles.ts:305-339
- **Summary:** Re-checked all five first-pass discrepancies against actual heading.ts, text-styles.ts, colors_and_type.css and spec.css — all REAL, correctly directed, token crosswalk resolved correctly (gray.900=#1a2626, brand.900=#111b22), no false positives, severities calibrated. The heading recipe maps size xl/lg/md/sm/xs onto spec .h1-.h5: lg(h2) and md(h3) are EXACT full matches (size/weight/line-height/tracking), and xl/sm/xs match on size, weight and line-height. Confirmed issues — renderer-drift: (1) xl lh 1.1 vs colors_and_type .h1 1.25 (recipe borrows the 44px .frontmatter-h1 leading for the 48px tier; its own display.xl=1.1 disagrees with display-xl alias=1.25); (2) sm/h4 adds snug -0.01em tracking spec .h4 doesn't declare; (3) MAJOR: xs (20px h5 tier) inherits base DM Sans but spec .h5/.h6 use the body font (Inter Tight) — only h1-h4 are DM Sans; (4) no tier maps to spec .h6 (18px/body/1.5), a coverage gap. Spec-internal-inconsistency: colors_and_type .h1-.h6 ink = --fg-primary/gray.900 #1a2626 (which the renderer matches) vs spec.css real usages (.frontmatter h1, .mode h2) hardcoding --ink/brand.900 #111b22.
- **Confirmed aligned:** xl size 48px (.h1 --fs-5xl), xl weight bold 700, xl tracking -0.02em, lg full match (36px/700/lh 1.25/-0.015em = .h2), md full match (30px/600/lh 1.375/-0.01em = .h3), sm 24px/600/lh 1.375 (= .h4), xs 20px/600/lh 1.375 (= .h5), base fontFamily heading DM Sans (for xl/lg/md/sm), base color #1a2626 (--fg-primary).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| size=xs fontFamily | colors_and_type .h5 (colors_and_type.css:165) font-family: var(--font-body) (Inter Tight) | heading.ts xs (:51-54) sets no fontFamily, so inherits base "heading" (DM Sans) from heading.ts:22 | renderer-drift | 🟠 major | Add fontFamily:"body" to the heading.ts xs variant (and to text-styles display.xs) so the h5 tier matches spec .h5's Inter Tight; mirror for any future h6 tier. |
| size=xl line-height | colors_and_type .h1 (colors_and_type.css:161) line-height: var(--lh-tight) = 1.25 | heading.ts:32 xl lineHeight: "1.1" | renderer-drift | 🟡 minor | Pick one source of truth for 48px h1 leading. To match colors_and_type .h1, set heading.ts xl lineHeight to "{lineHeights.tight}" (1.25) and change text-styles display.xl to 1.25; OR if 1.1 is the intended display leading, set colors_and_type .h1 + display-xl alias to 1.1 so all three agree. |
| size=sm letterSpacing | colors_and_type .h4 (colors_and_type.css:164) — no letter-spacing declared (inherits normal/0) | heading.ts:49 sm letterSpacing: "{letterSpacings.snug}" = -0.01em | renderer-drift | 🟡 minor | Remove letterSpacing from heading.ts sm (and from text-styles display.sm / display-sm) to match spec .h4 which has none; OR add letter-spacing:-0.01em to colors_and_type .h4 if the tighter tracking is the intended h4 spec. |
| h6 tier coverage | colors_and_type .h6 (colors_and_type.css:166): --font-body (Inter Tight) / --fs-lg 18px / --fw-semibold 600 / --lh-normal 1.5 | heading.ts sizes are xl,lg,md,sm,xs (h1..h5 only); no size value maps to spec .h6 | renderer-drift | 🟡 minor | Add a smaller tier {fontSize {fontSizes.lg} 18px, fontFamily body, fontWeight semibold, lineHeight {lineHeights.normal} 1.5} to match spec .h6, OR document that h6 is out of the Heading recipe's scope and route it through Text. |
| heading ink colour | spec.css heading USAGES use color: var(--ink) = brand.900 #111b22 (.frontmatter h1 spec.css:119, .mode h2 spec.css:203); colors_and_type .h1-.h6 use color: var(--fg-primary) = gray.900 #1a2626 | heading.ts:23 base color: "fg" = gray.900 #1a2626 (follows colors_and_type --fg-primary) | spec-internal-inconsistency | 🟡 minor | Spec side: give heading ink one source. Either change colors_and_type .h1-.h6 to color: var(--ink) (#111b22) AND retarget the renderer base to brand.900; OR change spec.css .frontmatter h1 / .mode h2 to var(--fg-primary) so the deep-ink usages match the token base the renderer already implements. |

> 📸 Visual verify: `make east-ui-examples-html-typography/heading` then compare the dist-examples PNG to the dist-design spec PNG.

---

### `recipe:icon-button`  — minor-drift
> 🟡 Partial — ink-variant fill and ghost-hover glyph now read spec `--ink` (foundation `fg`/`fg.default` → brand.900) ✅; still open: xs glyph 12px vs .x-btn 13px, and the .x-btn(4px)/.btn(6px) base-radius spec inconsistency. See `00-STATUS.md`.
- **Spec anchors:** spec.css:1234-1241 .diff-row .x-btn (width/height 26px, inline-flex centred, border 1px var(--rule-strong), border-radius 4px, background var(--paper), color var(--ink-3), font-size 13px, cursor pointer) · spec.css:1271 .diff-foot .btn-apply (border 1px var(--ink), background var(--ink), color var(--paper)) — closest dark/ink-filled trigger analog · spec.css:1268 .diff-foot .btn-discard · spec.css:1255-1258 .btn · spec.css:1264 .btn:hover · spec.css:1259-1262 .btn.primary · spec.css:1265-1266 .btn.ghost · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/icon-button.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/button.ts:87-94,153-158 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/buttons/icon-button/index.tsx · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts:73,79 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts:38-44,49-58
- **Summary:** icon-button recipe is structurally faithful: xs 26x26 matches .x-btn; ghost resting (transparent bg / fg.muted=--ink-3 color / bg.subtle=--paper-3 hover bg), outline (border.strong=--rule-strong border, fg.muted=--ink-3 hover border, bg.surface=--paper), and solid (brand.600=--brand-d / brand.700=--brand-dd) all resolve to exact spec hex. Snapshot (Buttons__buttons_icon-button.png) confirms the ghost close affordance, the grey-toned ink fill, and a hex-escape blue reactive counter button. Drift is minor and mostly the ink-family issue, now confirmed as an internal-recipe inconsistency: (1) ink variant fill uses fg.default=gray.900(#1a2626) where the spec --ink and the SIBLING button recipe both use brand.900(#111b22) — fix on the renderer to {colors.brand.900}; (2) ghost hover color shares the same fg=gray.900 vs --ink=brand.900 drift; (3) the 26x26 square uses a 12px glyph vs .x-btn's 13px. Corrections to the first pass: the base radius is re-labeled spec-internal-inconsistency (renderer 4px is an EXACT match to the .x-btn atom it cites; the .btn atom's 6px disagrees with .x-btn — fix on the spec side); and the ink-hover brand.800 is re-labeled intentional-deviation because it is identical to the paired button recipe's ink hover. Focus ring (3px brand) is the documented intentional Button/IconButton focus model. No critical/major issues.
- **Confirmed aligned:** xs square 26x26 (.x-btn), ghost resting color #4a5f5f + transparent bg, ghost hover bg #f1f5f5, outline border #cbd5d5 / hover border #4a5f5f / bg white, solid brand.600 #3a7780 / hover brand.700 #2b4b55 / white, inline-flex centring, focus ring 0 0 0 3px rgba(72,142,151,0.35), transition 120ms ease-out, ink hover brand.800 #1f363d (matches sibling button recipe).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| ink variant background (solid dark fill) | var(--ink) = brand.900 = #111b22 (.diff-foot .btn-apply background, spec.css:1271) | fg.default = gray.900 = #1a2626 (icon-button.ts:38) | renderer-drift | 🟡 minor | Renderer side: change icon-button.ts ink variant background from fg.default to {colors.brand.900} to match the button recipe ink variant and spec --ink. Keep color bg.surface (=--paper). |
| ghost hover color (text/icon glyph) | var(--ink) = brand.900 = #111b22 (.btn.ghost:hover, spec.css:1266) | fg (fg.DEFAULT) = gray.900 = #1a2626 (icon-button.ts:35) | renderer-drift | 🟡 minor | Renderer side: set ghost _hover color to {colors.brand.900}, or add an ink role mapped to brand.900 and use it. A global remap of fg.DEFAULT/fg.default to brand.900 would fix this and the ink-variant drift together, but is higher-risk. |
| icon glyph font-size at the 26x26 square (xs) | .x-btn font-size 13px at 26x26 (spec.css:1235,1239) | xs fontSize 12px at 26x26 (icon-button.ts:42) | renderer-drift | 🟡 minor | Renderer side: set xs fontSize to 13px to match the .x-btn glyph at the 26x26 size, OR document that icon-button intentionally decouples glyph size from .x-btn. |
| base border-radius | .x-btn 4px (spec.css:1236) vs .btn 6px (spec.css:1258) — the two spec atoms disagree | {radii.sm} = 4px (icon-button.ts:24) | spec-internal-inconsistency | 🟡 minor | Spec side: decide the canonical icon-trigger radius and make .btn and .x-btn agree (most likely lift .x-btn to 6px to match .btn / the button recipe, then change icon-button.ts to {radii.md}). If 4px for icon triggers is deliberate, document the .btn(6) vs .x-btn(4) split in the spec. |
| ink variant hover background | no dedicated dark-fill hover atom; .diff-foot .btn-apply (spec.css:1271) defines no :hover | brand.800 = #1f363d (icon-button.ts:38) | intentional-deviation (intentional) | ⚪ nit | No code change. If desired, add a spec hover for a dark/ink-filled trigger (lighten toward brand.800/700) so the convention is documented. |
| ghost resting color role | --ink-3 = gray.600 = #4a5f5f (.btn.ghost / .x-btn, spec.css:1266,1240) | fg.muted = gray.600 = #4a5f5f (icon-button.ts:35) | intentional-deviation (intentional) | ⚪ nit | No change. |

---

### `recipe:input`  — minor-drift
> 🟡 Partial — input text ink now reads spec `--ink` (foundation `fg` → brand.900) ✅; still open: font-size 14px vs spec 13px (major), padding 8/12px vs 7/10px (major), resting-border spec ambiguity, plus _hover/radius-comment/focus-doc nits. See `00-STATUS.md`.
- **Spec anchors:** spec.css:1881-1888 (.lib-search / .lib-search::placeholder — toolbar freeform search, radius 2px, 12.5px body, 5/10 padding, --rule border, --ink color) · spec.css:1595-1622 (.param-row .p-input input/select — canonical parameter-form input: 4px radius, 13px mono, 7/10 padding, --rule-strong border, --ink color, focus = --brand-d border + 3px ring) · spec.css:1558-1580 (.input-band / .ib-cell / .ib-val — display knobs, not editable inputs) · spec.css:12 (--ink #111b22 = brand.900) · spec.css:21 (--rule-strong #cbd5d5 = gray.300) · spec.css:23 (--brand-d #3a7780 = brand.600) · colors_and_type.css:126 (--shadow-focus 0 0 0 3px rgba(72,142,151,0.35)) · configure.html:715 (<input class="lib-search">) · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/input.ts:22-97 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/forms/input/index.tsx:49-105 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts:123-124 (radii.sm=4px, radii.md=6px // 'buttons, inputs') · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts:73 (fg.DEFAULT=gray.900) · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/global-css.ts:126-131 (input focus = border-colour only, no ring)
- **Summary:** Re-verified against both spec input atoms and the renderer source. The first pass made a load-bearing error: it matched the recipe only against .lib-search (a 2px-radius toolbar search field) and missed the canonical parameter-form input .param-row .p-input input/select (spec.css:1595-1622), which is the real general-input analog. That atom uses border-radius 4px — IDENTICAL to the renderer (radii.sm) — so the first pass's headline 'major' radius drift (2px vs 4px) is a FALSE POSITIVE and is dropped; the renderer radius is correct. The remaining real drift: (1) font-size 14px vs spec 13px (form) / 12.5px (search) — major, renderer rounds up off the spec's deliberate sub-14px sizes; (2) padding 8/12px vs spec 7/10px (form) / 5/10px (search) — major, renderer is chunkier on both axes; (3) text ink fg=gray.900 #1a2626 vs spec --ink brand.900 #111b22 — minor systemic. Both font and padding drift are confirmed in the snapshots (rendered field is visibly larger than the design lib-search). NEW findings the first pass missed: spec-internal-inconsistency on resting border (form=gray.300, search=gray.200; renderer picks gray.200), the spec form input focus still encoding a brand.600 border + 3px ring contradicting the renderer's border-only brand.500 focus model, and a stale tokens.ts:124 comment. Root cause is spec-side: there is NO single canonical .input atom.
- **Confirmed aligned:** borderWidth 1px, background white #ffffff (--paper), default fontFamily body (Inter Tight, == .lib-search), placeholder #6b8080 (--ink-4), border-radius 4px (matches the .param-row form-input atom; the first pass's 2px drift was a false positive), _invalid border #b85a4a (--neg), _disabled bg #f1f5f5 (--paper-3), numeric variant right-align + tabular-nums.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| font-size (default md) | 13px (.param-row .p-input input, spec.css:1602) / 12.5px (.lib-search, spec.css:1886) — both off-4px-grid and below 14 | fontSizes.sm = 14px (input.ts:26,89) | renderer-drift | 🟠 major | Set md fontSize to a literal 13px to match the canonical .param-row form input (fontSizes.xs=12px is short; a 13px literal or new token is needed). Alternatively codify 14px as the intended general-input baseline in a consolidated spec .input atom and document the divergence; do not leave the renderer silently larger than both existing atoms. |
| padding | 7px 10px (.param-row .p-input input, spec.css:1606) / 5px 10px (.lib-search, spec.css:1883) | paddingY {spacing.2}=8px, paddingX {spacing.3}=12px for md (input.ts:32-33,89) | renderer-drift | 🟠 major | Align md padding to the canonical .param-row form input: paddingY 7px, paddingX 10px (off-grid literals, not spacing tokens). Report exact px both sides — do not round to the 4px grid. |
| resting border-color | AMBIGUOUS in spec: .param-row .p-input input border = --rule-strong = gray.300 = #cbd5d5 (spec.css:1604); .lib-search border = --rule = gray.200 = #e2e8e8 (spec.css:1884) | border.subtle = gray.200 = #e2e8e8 (input.ts:31) | spec-internal-inconsistency | 🟡 minor | Spec-side: pick one canonical input border. If the form input is the reference, the renderer should resolve resting border to border.strong (gray.300); if .lib-search is the reference, keep gray.200. Consolidate spec.css onto a single .input atom so the renderer has one target. |
| color (text ink) | --ink = #111b22 = brand.900 (both .lib-search:1886 and .param-row .p-input input:1603) | color: fg = fg.DEFAULT = gray.900 = #1a2626 (input.ts:28; semantic-tokens.ts:73) | renderer-drift | 🟡 minor | System-level: remap fg.DEFAULT to brand.900 (#111b22) in semantic-tokens.ts so all input/body ink matches spec --ink, or formally accept #1a2626 as the renderer ink and document it. No per-recipe override warranted. |
| _focusVisible (border colour + ring) | .param-row .p-input input:focus = border-color var(--brand-d)=brand.600=#3a7780 PLUS box-shadow var(--shadow-focus)=0 0 0 3px rgba(72,142,151,0.35) — a 3px focus RING (spec.css:1616-1619) | borderColor {colors.brand.500}=#488e97, boxShadow none (input.ts:40-43) | intentional-deviation (intentional) | ⚪ nit | Update spec.css .param-row .p-input input:focus to match the current renderer focus model (brand-colour border change, no ring) so the canonical input atom stops contradicting global-css.ts. If a brand.600 focus border is intended, change input.ts _focusVisible borderColor to {colors.brand.600}; otherwise update the spec atom to brand.500. |
| _hover border-color | (none) — neither .lib-search nor .param-row input defines a :hover state; .param-row input's RESTING border is already --rule-strong (gray.300) | borderColor: border.strong = gray.300 = #cbd5d5 (input.ts:39) | renderer-drift | ⚪ nit | Either add a :hover rule to the consolidated spec .input atom (border-color: var(--rule-strong)) to legitimise it, or drop _hover from the recipe if the spec intends a static border. |
| radius token comment vs usage | n/a (renderer-internal) | tokens.ts:124 comments radii.md=6px '// buttons, inputs' implying inputs use 6px, but input.ts:29 uses radii.sm=4px | renderer-drift | ⚪ nit | Update the tokens.ts:124 comment to '// buttons' (drop 'inputs'), since the canonical input radius is radii.sm=4px per the spec form atom. |
| focus doc-comment rot | focus = border-colour change only, no ring (global-css.ts:126-131; the current renderer focus model) | code: boxShadow 'none' on _focusVisible (input.ts:42) — CORRECT; but module doc input.ts:10 still says 'Focus: brand-500 border + 3 px brand-tinted box-shadow' | renderer-drift | ⚪ nit | Update input.ts:9-11 doc to describe focus as a brand-500 border-colour change with no box-shadow, matching the code and global-css.ts. |

---

### `recipe:kbd`  — minor-drift
> 🟡 Partial — kbd text ink (recipe + bare-`<kbd>` global rule, both `color:'fg'`) now reads spec `--ink` (foundation `fg` → brand.900) ✅; still open: paddingX 8px vs spec 6px (major), letterSpacing 0.02em vs 0.04em, paddingY 1px vs 2px, plus inert-size/non-spec-variant/line-height nits. See `00-STATUS.md`.
- **Spec anchors:** spec.css .kbd L323-333 · colors_and_type.css :root --ink (#111b22) / --paper-3 (#f1f5f5) / --rule-strong (#cbd5d5) · tokens.ts letterSpacings L202-212 (wide=0.02em, wider=0.06em; no 0.04em step) + spacing L130-144 (strict 4px grid, spacing.2=8px) · tokens.ts brandScale L33-44 (900=#111b22) + grayScale L47-59 (900=#1a2626, 100=#f1f5f5, 300=#cbd5d5) · semantic-tokens.ts fg.DEFAULT L73 (gray.900) + bg.subtle L54 (gray.100) + border.strong L91 (gray.300) · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/kbd.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/global-css.ts:100-112 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/display/kbd/index.tsx · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/index.ts:115
- **Summary:** Adversarial re-check of the kbd recipe (kbd.ts) and the bare-<kbd> rule (global-css.ts). Both agree with each other and the spec on the structural look (mono 10px/600, 3px radius, 1px gray.300 border = spec --rule-strong, gray.100 fill = spec --paper-3, flat no-shadow default) — verified against tokens.ts hexes and the Display__display_kbd.png snapshot (flat chips, raised opt-in). Confirmed drifts (all renderer-side, all present in BOTH the recipe and the global-css rule): (1) color uses fg=gray.900 #1a2626 vs spec --ink brand.900 #111b22 (minor; shared fg-vs-ink class); (2) letterSpacing 0.02em (wide) vs spec 0.04em (minor) — CORRECTED the first-pass error: wider=0.06em not 0.04em, and there is NO 0.04em token; (3) paddingX 8px (spacing.2, grid-rounded) vs spec off-grid 6px (major, visibly wider chip); (4) paddingY 1px vs spec 2px (minor). Nits: an inert size prop, undocumented outline/subtle/raised variants (re-labelled to renderer-drift), and a recipe-vs-global-css lineHeight inconsistency added by this pass. Priority fixes: paddingX 6px and letterSpacing 0.04em; the color drift likely warrants a system-level fg=brand.900 decision rather than a per-recipe patch.
- **Confirmed aligned:** font-family mono (JetBrains Mono), font-size 10px, font-weight 600, border-radius 3px, border 1px #cbd5d5 (--rule-strong), flat bg #f1f5f5 (--paper-3), no drop shadow on flat default, kbdRecipe registered at recipes.kbd (replaces Chakra's built-in).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| padding-inline (paddingX) | 6px (spec padding: 2px 6px, L331) | {spacing.2} = 8px — recipe kbd.ts L29 base + global-css.ts L109 | renderer-drift | 🟠 major | Renderer side: set paddingX to "6px" (deliberately off-grid, as the OFF-GRID note permits) in both kbd.ts base L29 and global-css.ts kbd L109. |
| color (text ink) | var(--ink) = #111b22 (brand.900) | fg = fg.DEFAULT = gray.900 = #1a2626 (recipe flat/outline/subtle/raised all set color:"fg" L40/46/51/57; global-css L111 color:"fg") | renderer-drift | 🟡 minor | Renderer side: change every variant color from "fg" to a token resolving to brand.900 (spec --ink) in kbd.ts, and the global-css.ts kbd block; OR consolidate the fg role to brand.900 system-wide if the whole system should track spec --ink (preferred, since this drift is shared across atoms). |
| letter-spacing | 0.04em (spec .kbd L327) | 0.02em — {letterSpacings.wide} in recipe kbd.ts L27 and global-css.ts L104 | renderer-drift | 🟡 minor | Renderer side: add a 0.04em letterSpacings step to tokens.ts (none exists — wide=0.02, wider=0.06) and reference it, or hardcode "0.04em" in both kbd.ts L27 and global-css.ts L104. |
| padding-block (paddingY) | 2px (spec padding: 2px 6px, L331) | 1px — recipe kbd.ts L30 base + global-css.ts L110 | renderer-drift | 🟡 minor | Renderer side: set paddingY to "2px" in both kbd.ts base L30 and global-css.ts kbd L110. |
| size variants (inert size prop) | spec .kbd defines a single size (10px / 2px 6px); no size variants | component index.tsx L47 forwards a size prop to <ChakraKbd>, but kbdRecipe defines NO size variant group and fully replaces Chakra's built-in recipe — so the size prop has zero visual effect | renderer-drift | ⚪ nit | Renderer side: drop the size plumbing in index.tsx L32/L47 (spec documents one size, so dropping is preferred over adding a size variant group). |
| non-spec variants (outline/subtle/raised) | spec .kbd is a single filled atom (bg --paper-3, 1px --rule-strong, no shadow); no variants documented | kbd.ts L42-59 defines outline (transparent bg), subtle (no border), raised (0 2px 0 border.strong drop shadow); default is flat which matches spec | renderer-drift | ⚪ nit | Either document the variant set in spec.css .kbd (so renderer surface is intentional) or accept as documented renderer-only extensions. No pixel change to the flat default needed. |
| line-height (renderer-internal inconsistency) | spec .kbd sets no line-height (L323-333) | recipe kbd.ts L28 sets lineHeight:"1"; global-css.ts kbd block (L100-112) sets NO lineHeight | renderer-drift | ⚪ nit | Renderer side: add lineHeight:"1" to the global-css.ts kbd block (L100-112) to match the recipe, OR drop it from kbd.ts L28 — pick one for consistency. |

---

### `recipe:link`  — minor-drift
> 📋 Open — link colours use brand.600/brand.700 explicitly (not `fg`), so the foundation ink change does not apply here; still open: spec-internal two-atom conflict (inline `a` 16px/400/hover-underline vs `.btn-link` 12.5px/500/no-underline) leaving the renderer a weight-500 hybrid, plus the doubled hover (recipe + component). See `00-STATUS.md`.
- **Spec anchors:** colors_and_type.css:174-175 (a: color --link #3a7780, text-decoration none; a:hover: color --link-hover #2b4b55, underline, text-underline-offset 2px — inline link inherits body 16px / weight 400) · colors_and_type.css:66-67 (--link: --brand-600; --link-hover: --brand-700) · colors_and_type.css:22-23 (--brand-600 #3a7780; --brand-700 #2b4b55) · colors_and_type.css:88 (--fw-medium: 500); :87 (--fw-normal: 400) · colors_and_type.css:77 (--fs-md 16px body) · spec.css:454-460 (.btn-link: font-size 12.5px, color --brand-d #3a7780, text-decoration none, font-weight 500; :hover color --brand-dd #2b4b55, NO underline) · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/link.ts:17-36 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/link/index.tsx:23-77 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts:116-120 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts:33-55,164-174 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/index.ts:44,117
- **Summary:** Re-checked against the actual spec.css/colors_and_type.css rules and renderer source. Colors are pixel-exact: recipe rest color resolves brand.600=#3a7780 (==spec --link/--brand-d) and hover brand.700=#2b4b55 (==--link-hover/--brand-dd), no rest underline, hover underline with 2px offset matching the inline `a` atom; dark-theme link tokens match too. Recipe is registered as theme recipes.link (index.ts:117) so it applies to ChakraLink, and the snapshot confirms teal links, no rest underline, and an inline link rendering at the surrounding body size. The first pass was well-calibrated; all three discrepancies confirmed real. Core issue (minor) is a SPEC-INTERNAL INCONSISTENCY: the spec carries two conflicting link atoms — inline `a` (inherited 16px / weight 400 / hover underline) and `.btn-link` (12.5px / weight 500 / no hover underline) — and the renderer is a hybrid (inherited size + hover underline from `a`, but font-weight 500 from `.btn-link`), so a body-prose link renders at weight 500 vs the `a` atom's 400.
- **Confirmed aligned:** rest color brand.600 #3a7780 (== --link / --brand-d), hover color brand.700 #2b4b55 (== --link-hover / --brand-dd), rest text-decoration none, hover underline + 2px offset (== inline `a`), font-weight 500 (== .btn-link), registered as recipes.link (overrides Chakra), dark theme link.DEFAULT brand.300 / hover brand.200.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| font-size / font-weight (spec has two conflicting link atoms; renderer is a hybrid) | inline `a`: inherits body 16px, weight 400, hover underline+2px offset (colors_and_type.css:174-175). `.btn-link`: 12.5px, weight 500, NO hover underline (spec.css:454-460). | recipe base sets no fontSize (inherits surrounding text size) + fontWeight:"medium"=500 + _hover underline 2px offset. Result: a link in body prose renders at ~16px / weight 500. | spec-internal-inconsistency | 🟡 minor | Consolidate the spec to one canonical link. If the renderer link represents inline `a`, drop fontWeight:"medium" from link.ts:25 so it inherits 400 like spec `a`. If it represents the compact action `.btn-link`, add fontSize "12.5px" and remove the _hover underline (link.ts:32-33). Right now it is a hybrid of both. |
| _hover defined in both recipe and component | single hover definition (a:hover underline / link.hover) | hover defined twice: recipe link.ts:30-34 AND component index.tsx:38-43 (css['&:hover']) for the default no-variant/no-color path; index.tsx:52 also re-defaults color to "link". | renderer-drift | ⚪ nit | Remove the default-path hover block (index.tsx:38-43) and the `?? "link"` color default (index.tsx:52); let the registered linkRecipe own rest color + hover. Keep only the explicit per-instance overrides (hoverColor/visitedColor/color from style). |
| transition | spec a/.btn-link declare no transition (instant color/underline change) | recipe transitionProperty: color, text-decoration-color; duration {durations.fast}=120ms; timingFunction {easings.out}=cubic-bezier(0.16,1,0.3,1) | intentional-deviation (intentional) | ⚪ nit | No action required. If strict parity is desired, document the standard 120ms fast/ease-out transition for links in the spec so both sides agree explicitly. |

---

### `recipe:mark`  — no-spec-analog
> 📋 Open — mark text uses `{colors.brand.800}` (#1f363d), not `fg`, so the foundation ink change does not apply; still open: spec's invariant brand-tint text is `--brand-dd` brand.700 #2b4b55 (one step lighter), plus the je-verb padding gap (1px 5px vs renderer 2px-X). See `00-STATUS.md`.
- **Spec anchors:** spec.css:2241 (.je-verb — the only inline brand-tint pill: mono 11px/700, color var(--brand-dd) #2b4b55, bg var(--brand-tint) #e8f6f7, border-radius 2px, padding 1px 5px, margin-right 6px) · spec.css:24,26 (:root --brand-dd #2b4b55, --brand-tint #e8f6f7) · spec.css:1132 (.stakes-tag.mid), 1534 (.annotate-tag.active), 1902 (.lib-seg-btn.active), plus 1167/1231/1389/1580/2097/2347/2551/2576/2611/2625/2812/2947 — every brand-tint ground in the spec pairs with var(--brand-dd) text · colors_and_type.css / tokens.ts:41-42 brandScale (700 #2b4b55, 800 #1f363d, 900 #111b22); tokens.ts:76 brandTint #e8f6f7 · spec grep for /\.mark|\.highlight|<mark/ returns nothing — no generic highlight atom exists · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/mark.ts:19-24 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/mark/index.tsx · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts:59,36,39 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts:41-42,76
- **Summary:** Verdict no-spec-analog: spec.css has NO generic highlight/<mark> atom (grep confirmed). The closest analog is .je-verb (spec.css:2241), the one inline brand-tint pill. Re-verified both sides. Default background bg.brand.subtle resolves to brandTint #e8f6f7 (semantic-tokens.ts:59 → tokens.ts:76), an exact match to spec --brand-tint, and the 2px borderRadius matches .je-verb — both real matches, first pass correct. One real drift: recipe default text colour is brand.800 (#1f363d, tokens.ts:42), whereas every brand-tint ground in the spec pairs the wash with --brand-dd / brand.700 (#2b4b55) — confirmed ~15 occurrences, zero counterexamples. Minor severity (close teal hexes on a pale ground). Padding (2px X, no Y) differs from .je-verb's 1px 5px, but since .je-verb is a typed verb pill not a generic highlight, that is a nit signalling a spec gap. Colored/solid variants in Typography__typography_mark.png are author colorPalette overrides passed through toChakraMark, out of scope for the base recipe.
- **Confirmed aligned:** background bg.brand.subtle → brandTint #e8f6f7 (== --brand-tint, exact), borderRadius 2px (== .je-verb), inline-span structure (bg + color + radius + small paddingX, inherits surrounding type), colorPalette/variant overrides pass-through (author-driven), snapshot markBasic confirms faint brand-tint wash + dark teal ink + tight radius.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| color (default text on brand-tint) | var(--brand-dd) = brand.700 = #2b4b55 (the spec's invariant text colour on a --brand-tint ground: .je-verb 2241, .stakes-tag.mid 1132, .annotate-tag.active 1534, .lib-seg-btn.active 1902, and ~12 more all use --brand-dd) | {colors.brand.800} = #1f363d (mark.ts:21) | renderer-drift | 🟡 minor | Renderer side: change mark.ts:21 base color from "{colors.brand.800}" to semantic "brand.fg" (resolves to brand.700 #2b4b55 per semantic-tokens.ts:36) to match the spec's brand-tint text token. Verified brand.fg/brand.emphasized both land on #2b4b55. |
| padding | .je-verb padding: 1px 5px (~1px vertical, 5px horizontal) — the only inline brand-tint pill the spec defines | paddingX: 0.5 = 2px horizontal, no vertical padding (mark.ts:23) | spec-internal-inconsistency | ⚪ nit | Spec side (preferred): add a generic .mark / .highlight atom to spec.css (bg --brand-tint, color --brand-dd, radius 2px, explicit inline padding e.g. 0 4px) so the renderer has a real anchor; then align mark.ts paddingX to it. No renderer change strictly required if a light generic highlight is intended. |

---

### `recipe:note`  — major-drift
> 🟡 Partial — callout body text now reads spec `--ink` (foundation `fg` → brand.900) ✅; still open (mostly major): accent stripe brand.500 vs --brand-d brand.600, stripe width 2/4px overriding 3px, base italic leaking into callout/narrative, py 8px vs 12px, ps/pe grid-rounding, narrative-vs-.je-rationale, and dead recipe-base values. See `00-STATUS.md`.
- **Spec anchors:** spec.css .ho-q 2381-2385 (font 14px var(--ink), line-height 1.5, padding 12px 14px, background var(--paper-2), border-left 3px solid var(--brand-d), border-radius 2px) — the canonical upright brand left-accent callout · spec.css .je-rationale 2243-2247 (font-size 12.5px, color var(--ink-3), font-style italic, border-left 2px solid var(--rule-strong), padding 2px 0 2px 10px, NO background) — the narrative/rationale block · spec.css .banner 724-744 (top+bottom 1px dashed var(--rule-strong), padding 12px 18px, font 13px var(--ink-2), background var(--paper)) — a DIFFERENT full-width top/bottom-dashed atom, NOT a left-border note · spec.css .bn 2333-2353 / .banner.* 1649-1661 — full-surround status banners; separate atom · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/note.ts:17-42 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/note/index.tsx:36-58,120-150 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts:96-99,71-74 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts:39-43,130-144 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui/src/typography/note/index.ts:85-96 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/dist-examples/Typography__typography_note.png
- **Summary:** Re-verified against spec.css and renderer source. The note recipe's true analogs are .ho-q (callout: 3px solid --brand-d left rule, --paper-2 bg, 14px UPRIGHT --ink, padding 12px 14px, radius 2px) and .je-rationale (narrative/quote: 12.5px italic --ink-3, 2px solid --rule-strong left rule, NO background). The recipe header's .banner citation is a wrong anchor — .banner is a top/bottom-dashed full-width atom, not a left-border note. Confirmed matches: callout/quote background gray.50 (=--paper-2), line-height 1.5, callout fontSize 14px. Confirmed major drifts (all visible in Typography__typography_note.png): (1) accent stripe uses border.brand=brand.500 #488e97 vs spec --brand-d=brand.600 #3a7780; (2) stripe width 2px default/4px strong overrides the recipe's correct 3px (never renders); (3) recipe base fontStyle:italic LEAKS into callout AND narrative though only quote/.je-rationale should be italic; (4) vertical padding py=8px vs spec 12px (-4px tightening). Minor: horizontal ps/pe land at 12/16px on the 4px grid vs spec off-grid 14px (±2px); callout body fg=gray.900 #1a2626 vs --ink #111b22. NEW finding: the narrative variant deviates from its .je-rationale analog three ways. Recipe-internal inconsistency: note.ts base borderLeftWidth/paddingX/paddingY are dead (overridden by index.tsx) while fontStyle leaks.
- **Confirmed aligned:** callout/quote background gray.50 #f8fafa (== .ho-q --paper-2), lineHeight 1.5 (== .ho-q), callout fontSize 14px (== .ho-q; quote/.je-rationale analog is 12.5px so 14px is a size lift there but .ho-q parity holds), quote variant italic + body fg.muted #4a5f5f (== --ink-3).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| borderLeftColor (brand accent stripe) | .ho-q border-left color = var(--brand-d) = brand.600 = #3a7780 | border.brand = brand.500 = #488e97 (note.ts:21 base + accent.brand :32; VARIANT_DEFAULTS callout/quote accentColor 'border.brand' index.tsx:45,52) | renderer-drift | 🔴 major | Renderer: change the accent.brand variant (and callout/quote VARIANT_DEFAULTS accentColor) from 'border.brand' to 'link.DEFAULT' or 'brand.solid' (both = brand.600 = --brand-d). Leave the base default to the brand accent so it inherits the same value. |
| borderLeftWidth (default stripe) | .ho-q border-left = 3px (recipe base note.ts:20 also 3px — correct intent) | 2px default / 4px when emphasis='strong' (index.tsx:120, applied borderLeftWidth :128); recipe base 3px is overridden and never renders | renderer-drift | 🟠 major | Renderer: make the default branch '3px' (index.tsx:120 → emphasis==='strong' ? '4px' : '3px'), or drop the explicit borderLeftWidth and let the recipe base 3px apply. |
| fontStyle (callout + narrative leak italic) | .ho-q is upright (no font-style); only .je-rationale (narrative/rationale) and the quote block are italic | recipe base fontStyle:'italic' (note.ts:25) applied to ALL variants via css; component fontStyle={d.fontStyle} is undefined for callout/narrative (index.tsx:135) so it does NOT reset the base | renderer-drift | 🟠 major | Renderer: remove fontStyle:'italic' from the note.ts recipe base; apply italic only via the quote variant default (already VARIANT_DEFAULTS.quote.fontStyle) and a narrative default if the .je-rationale italic is desired there. Then callout renders upright per .ho-q. |
| padding (vertical) | .ho-q padding 12px 14px → 12px top/bottom (recipe base paddingY {spacing.3}=12px, correct intent) | py='2' = 8px (index.tsx:138); overrides recipe base 12px | renderer-drift | 🟠 major | Renderer: set py='3' (12px) to match .ho-q, or drop the explicit py and rely on the recipe base paddingY {spacing.3}. |
| padding (horizontal / paddingStart) | .ho-q padding 12px 14px → 14px left/right | ps='3'=12px (callout/narrative), ps='4'=16px (quote), pe='3'=12px; 4px-grid has no 14px stop | renderer-drift | 🟡 minor | Renderer: either accept the 4px-grid rounding as an intentional grid-snap (then document), or add an off-grid 14px padding (matching spec.css's deliberate off-grid 14px) on ps/pe for the .ho-q parity. Pick one and note it. |
| narrative variant vs .je-rationale (border + background) | .je-rationale: border-left 2px SOLID var(--rule-strong)=gray.300 #cbd5d5, NO background (transparent), padding 2px 0 2px 10px | narrative: borderLeftStyle 'dashed', accentColor 'border.muted'=gray.200 #e2e8e8, 2px width, PLUS recipe base background 'bg.canvas'=gray.50 #f8fafa fill | renderer-drift | 🟡 minor | Renderer: for narrative set borderLeftStyle 'solid', accentColor 'border.strong' (gray.300=--rule-strong), and drop the bg.canvas fill (set background 'transparent' for narrative) to match .je-rationale. If a dashed muted treatment is intended, document it as a deliberate narrative style distinct from .je-rationale. |
| body text color (callout) | .ho-q color var(--ink) = brand.900 = #111b22 | callout bodyColor 'fg' = fg.DEFAULT = gray.900 = #1a2626 (index.tsx:46; semantic-tokens.ts:71) | renderer-drift | 🟡 minor | Foundation-level reconciliation (semantic-tokens.ts fg.DEFAULT). Track under the global fg=gray.900 vs spec --ink=brand.900 question, not as a note-specific fix; if addressing locally, point callout bodyColor at a brand.900-backed token. |
| recipe base padding/borderLeftWidth/fontStyle vs component overrides (dead values) | recipe note.ts base owns borderLeftWidth 3px, paddingX {spacing.4}=16px, paddingY {spacing.3}=12px | index.tsx overrides all three with explicit Box props (borderLeftWidth 2/4px, py='2', ps/pe='3'/'4'); fontStyle:italic base leaks because the component cannot clear it. The recipe's own padding/width never render. | spec-internal-inconsistency | 🟡 minor | Code-level: pick one source of truth. Either drive padding+stripe width+font-style from the recipe (add an emphasis variant to note.ts, drop the index.tsx explicit borderLeftWidth/py/ps/pe/fontStyle) or remove the unused base values from note.ts. |
| recipe accent variant set vs reachable IR variants | IR exposes exactly narrative/callout/quote (east-ui note index.ts:85-89) | recipe accent variants brand/warning/danger/success/muted (note.ts:31-37) but VARIANT_TO_ACCENT only maps to muted/brand/brand (index.tsx:73-77) — warning/danger/success are unreachable | spec-internal-inconsistency (intentional) | ⚪ nit | No conformance action. Optionally trim unused accent variants from note.ts until the IR exposes them. |

---

### `recipe:separator`  — minor-drift
> 📋 Open — separator colours are gray-ladder tokens (no `fg`), so the foundation ink change does not apply; still open: defaultVariants.variant='strong' → border.strong gray.300 for a styleless separator vs spec .rule gray.200 (one step darker). Strong/vertical extensions remain intentional. See `00-STATUS.md`.
- **Spec anchors:** spec.css:308-318 (.rule / .rule.dashed / .rule.brand) · spec.css:943-946 (.rule-hairline — border:none; border-top:1px solid var(--rule); margin:0) · colors_and_type.css :root --rule #e2e8e8 (gray.200) / --rule-strong #cbd5d5 (gray.300) / --brand #488e97 (brand.500) · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/separator.ts:17-39 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/separator/index.tsx:28-34 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts:89-98 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/global-css.ts:115-120 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui/src/layout/separator/index.ts:125-140 · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui/test/layout/separator.examples.ts:8-55
- **Summary:** Re-verified the first-pass finding against spec.css (308-318, 943-946), the resolved crosswalk, and the actual renderer/IR sources — it holds up, with no false positives and no missed pixel discrepancies. The separator recipe is a faithful port of spec .rule / .rule.dashed / .rule.brand: base border:0 + 1px top hairline + margin:0; subtle=gray.200 (#e2e8e8 == --rule), dashed=gray.300 dashed (#cbd5d5 == --rule-strong), brand=brand.500 (#488e97 == --brand). global-css hr.dashed mirrors the dashed variant exactly. One genuine renderer-drift (minor): defaultVariants.variant="strong", so a styleless or orientation-only Separator resolves variant:none → undefined → Chakra default 'strong' = gray.300 #cbd5d5, one gray step darker than spec .rule's gray.200 #e2e8e8; the example set even ships a separate 'separatorSubtle' calling itself the rule-default, so the two disagree. Two extension variants (strong, vertical) have no spec atom but are token-consistent — intentional-deviation nits with optional spec consolidation.
- **Confirmed aligned:** base border:0 + borderTop 1px solid + margin:0 (== .rule / .rule-hairline), subtle gray.200 #e2e8e8 (== --rule), dashed gray.300 #cbd5d5 dashed (== --rule-strong), brand brand.500 #488e97 (== --brand), 1px thickness, global-css hr.dashed mirror, snapshot separatorBrand (teal solid) + separatorDashed (gray dashed).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| default variant / borderTopColor when no style.variant is set | spec .rule base = 1px solid var(--rule) = gray.200 #e2e8e8 (the canonical section-divider hairline; .rule-hairline at spec.css:943-946 confirms --rule is the plain default) | defaultVariants.variant = "strong" (separator.ts:38) → border.strong = gray.300 #cbd5d5 (one gray step darker) | renderer-drift | 🟡 minor | Renderer side: change separator.ts:38 defaultVariants from variant:"strong" to variant:"subtle" so a styleless/orientation-only separator renders the spec .rule gray.200 hairline. Keep the explicit "strong" variant for callers who want gray.300. |
| strong variant — spec atom | no .rule.strong atom exists in spec.css; spec only defines .rule (gray.200), .rule.dashed (gray.300 stroke), .rule.brand (brand.500) | strong variant (separator.ts:29): borderTopColor border.strong = gray.300 #cbd5d5 | intentional-deviation (intentional) | ⚪ nit | Spec side (optional consolidation): add .rule.strong { border-top: 1px solid var(--rule-strong); } to spec.css so the renderer's strong variant has a documented anchor. No renderer change. |
| vertical orientation — spec atom | no vertical .rule atom in spec.css (only horizontal border-top divider documented) | vertical (separator.ts:35): borderTopWidth 0 + borderLeftWidth 1px solid border.subtle (gray.200 #e2e8e8) | intentional-deviation (intentional) | ⚪ nit | None required; optionally document a vertical .rule in spec.css for completeness. |

---

### `recipe:skeleton`  — minor-drift
> 📋 Open — skeleton uses bg.subtle/bg.emphasized gray tokens (no `fg`), so the foundation ink change does not apply; still open: the registered `elara-skeleton` recipe is dead/bypassed (Chakra built-in drives the look), its linear-shimmer keyframe has no spec analog, plus elara-pulse 0.4 floor and rect 20px-fallback nits. See `00-STATUS.md`.
- **Spec anchors:** spec.css:366-369 @keyframes pulse (0/100% opacity 1, 50% opacity 0.5; ease-in-out) · spec.css:358-362 .dot.live (pulse 2.4s) / .dot.run (pulse 1.6s) · spec.css:3375-3379 @keyframes sch-pulse (box-shadow ring, 1.4s ease-out) · spec.css:1169 @keyframes blink (50% opacity 0) · colors_and_type.css --paper-3 #f1f5f5 (gray.100 = bg.subtle), --rule #e2e8e8 (gray.200 = bg.emphasized) · no dedicated skeleton / loading-placeholder atom exists in spec.css · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/skeleton.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/feedback/skeleton/index.tsx · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/keyframes.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/index.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/global-css.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts
- **Summary:** Skeleton has NO spec atom, so this is judged against foundation tokens + spec's loading-motion vocabulary. Adversarial re-check confirms the first pass's core structural finding and corrects its framing of two items. CONFIRMED: skeletonRecipe (className 'elara-skeleton') is registered at theme/index.ts:123 but grep shows 'elara-skeleton' appears only in its own definition file — it is never applied, because Chakra v3's <Skeleton>/<SkeletonText>/<SkeletonCircle> are standalone components not wired to a top-level recipes.skeleton entry. The live look comes from Chakra's built-in skeleton, conditionally re-coloured by feedback/skeleton/index.tsx:40-42 (raw --chakra-colors-bg / -bg-emphasized vars, set ONLY when the East value supplies background/shimmerColor) — both resolve to bg.subtle gray.100 (#f1f5f5 / --paper-3) -> bg.emphasized gray.200 (#e2e8e8 / --rule), confirmed on-palette, with 4px radius. CORRECTIONS to the first pass: the 'linear horizontal shimmer foreign to spec' lives in the DEAD recipe and does NOT render — the motion mismatch is latent, not visible; elara-pulse bottoming at opacity 0.4 vs spec pulse 0.5 is a renderer-side keyframe value (renderer-drift not spec-vs-tokens), and it is unused; the renderer's CSS-var overrides are conditional, not unconditional. Net: one minor load-bearing issue (dead recipe duplicating the component path) plus two latent nits and one code-clarity nit. No critical/major issues; palette and radius are on-spec.
- **Confirmed aligned:** resting/sweep colours on the cool-gray ladder (bg.subtle gray.100 #f1f5f5 / --paper-3 and bg.emphasized gray.200 #e2e8e8 / --rule), borderRadius 4px, CSS-var overrides conditional (Chakra defaults drive the no-override look), prefers-reduced-motion handled globally (animation/transition 0.001ms), spec-pulse-live/run keyframes mirror .dot.live/.dot.run depths, snapshot confirms cool-gray rounded 4px placeholders.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| skeleton recipe is dead / bypassed (registered but never applied) | n/a — spec has no skeleton atom; the renderer is expected to drive the visual, single-sourced | skeletonRecipe (className 'elara-skeleton', gradient base + line/circle/block variants) is registered at theme/index.ts:123 under recipes.skeleton, but a grep of src/ shows 'elara-skeleton' appears ONLY in skeleton.ts itself — never applied via className or useRecipe. feedback/skeleton/index.tsx renders Chakra's built-in <Skeleton>/<SkeletonText>/<SkeletonCircle> and (conditionally) overrides colours via raw CSS vars. | renderer-drift | 🟡 minor | Either (a) delete skeletonRecipe and document the gray.100->gray.200 + 4px-radius contract on the CSS-var override path, or (b) make feedback/skeleton/index.tsx actually consume it (className='elara-skeleton' / useRecipe + variant props) so radius, variant heights and animation are single-sourced. Renderer/theme side changes. |
| animation motion vocabulary (shimmer keyframe with no spec analog) | spec's only loading-motion vocabulary is opacity-pulse (@keyframes pulse 1->0.5 ease-in-out, spec.css:366-369) and a box-shadow ring (sch-pulse ease-out, spec.css:3375-3379); spec has NO background-position shimmer anywhere | skeletonRecipe.base declares 'animation: elara-shimmer 1.6s linear infinite' (skeleton.ts:21) with a linear-gradient + 200% backgroundSize horizontal sweep. Because the recipe is dead, this shimmer never renders — the live animation is Chakra v3's built-in Skeleton default; the snapshot shows a static cool-gray gradient. | renderer-drift | 🟡 minor | When resolving the dead-recipe issue: either document a shimmer as the intended skeleton motion in spec.css/text-styles, or align the recipe to spec by using an opacity pulse ('elara-pulse'/spec-style 1->0.5 ease-in-out) over a flat bg.subtle. Renderer/theme side, only bites once the recipe is actually consumed. |
| elara-pulse opacity floor vs spec pulse | spec @keyframes pulse 50% opacity 0.5 (spec.css:368); .dot.live/.dot.run mirror at 0.5/0.45 (faithfully copied as spec-pulse-live/spec-pulse-run in keyframes.ts:37-44) | keyframes.ts:25-28 elara-pulse 50% opacity 0.4 — deeper than every spec pulse | renderer-drift | ⚪ nit | Raise elara-pulse 50% to opacity 0.5 in keyframes.ts to match spec @keyframes pulse, or document the deeper 0.4 floor as intentional for skeletons. Renderer/theme side. |
| rect fallback default height vs recipe line height | n/a (no spec skeleton); spec sizing is on a 4px grid (with deliberate off-grid only for type/padding) | renderOne rect fallback height '20px' (index.tsx:72) coexists with recipe line variant height = fontSizes.md = 16px (skeleton.ts:25). circle default size '10' = 40px. Because the recipe is bypassed, the 20px fallback is what actually applies for a rect with no explicit height. | renderer-drift | ⚪ nit | Once recipe consumption is resolved, settle on a single default height; if keeping the renderer path, leave 20px and remove the unused recipe variant height. Renderer/theme side. |
