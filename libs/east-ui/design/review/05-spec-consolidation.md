## spec.css internal drift & consolidation

These findings concern the design spec itself (`spec.css` + `colors_and_type.css`) needing fixes and consolidation, not the renderer.

### `spec:spec-token-conflicts` — spec-needs-consolidation
> 🟡 Partial — primary-ink conflict RESOLVED (colors_and_type.css `--fg-primary` → `var(--brand-900)` and semantic-tokens.ts `fg.DEFAULT` → brand.900, so both spec `--ink` #111b22 and the renderer now agree on brand.900). Still OPEN: spec.css off-4px-grid px values vs tokens.ts strict-grid claim, `--info`==`--brand-d` dup, `--brand-dd`==`--ink-2` dup, missing dark block in spec.css, and `--ink-5` having no semantic fg role. See `00-STATUS.md`.

- **Spec anchors:** spec.css:11-31 (:root token block) · spec.css:12 --ink #111b22 · spec.css:13 --ink-2 #2b4b55 · spec.css:23 --brand-d #3a7780 · spec.css:24 --brand-dd #2b4b55 · spec.css:30 --info #3a7780 · spec.css:39 color: var(--ink) (body) · colors_and_type.css:14-134 (:root) · colors_and_type.css:25 --brand-900 #111b22 · colors_and_type.css:37 --gray-900 #1a2626 · colors_and_type.css:54 --fg-primary: var(--gray-900) · colors_and_type.css:136-156 (dark theme block) · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts · /home/crambelsoupy/src/east-workspace/libs/east-ui/design/spec.css · /home/crambelsoupy/src/east-workspace/libs/east-ui/design/colors_and_type.css
- **Summary:** spec.css :root (lines 11-31) is light-only and largely agrees with colors_and_type.css + tokens.ts, but six consolidation issues remain. (1) MAJOR token conflict: --ink #111b22 (brand.900) is the spec's primary body ink (spec.css:39), but colors_and_type.css:54 routes --fg-primary through gray.900 #1a2626 and the renderer's fg.DEFAULT (semantic-tokens.ts:73) resolves to gray.900 #1a2626 — two different canonical inks on the most-used text color; pick one (recommend brand.900). (2) MAJOR grid conflict: spec.css pervasively uses off-4px values (font 8.5/9.5/10.5/11.5/12.5/13.5px; padding 5/6/10/14/18/22px) while tokens.ts:131 forbids exactly 5/7/10/14/18/22 and ships integer-only font sizes — the renderer cannot express these without inline literals, so it rounds and drifts; reconcile by adding the spec's half-px/odd tokens to tokens.ts as the single source. (3) MINOR: --info #3a7780 is identical to --brand-d / brand.600 — info status is indistinguishable from brand; give info its own hue or document the alias. (4) NIT: --brand-dd #2b4b55 duplicates --ink-2 (brand.700) — alias one to the other so a retune touches one line. (5) MINOR: spec.css has no dark block while colors_and_type.css:136-156 and semantic-tokens.ts ship full dark themes — dark mode is unspecifiable against the pattern spec; add a dark remap or declare spec light-only. (6) NIT: --ink-5 gray.400 has no named fg role in semantic-tokens.ts. Net recommendation: make tokens.ts the single consolidated token source (carrying the spec's deliberate off-grid + brand.900 ink), and have spec.css/colors_and_type.css alias into it rather than restating divergent values. All values read directly from source; no rendering verification needed for a token-level audit.
- **Confirmed aligned:** spec.css --ink-3 #4a5f5f == colors_and_type --gray-600 == tokens.ts gray.600 == semantic fg.muted, spec.css --ink-4 #6b8080 == --gray-500 == tokens.ts gray.500 == semantic fg.subtle, spec.css --paper #ffffff / --paper-2 #f8fafa / --paper-3 #f1f5f5 == gray.50/100 + white in tokens.ts (bg.surface/canvas/muted), spec.css --rule #e2e8e8 / --rule-strong #cbd5d5 == gray.200/300 == border.subtle/strong, spec.css --brand #488e97 == brand.500, --brand-d #3a7780 == brand.600, --brand-dd #2b4b55 == brand.700, --brand-l #94f9f9 == brand.200, --brand-tint #e8f6f7 == brandTint, spec.css --pos #2f7a5b / --neg #b85a4a / --warn #b8862d / --info #3a7780 == tokens.ts status.pos/neg/warn/info exactly, Fonts: spec --font-body Inter Tight / --font-brand DM Sans / --font-mono JetBrains Mono == tokens.ts FONT_BODY/FONT_BRAND/FONT_MONO families, Radii sm4/md6/lg8/xl12/2xl16/full9999 identical across spec implied + colors_and_type --r-* + tokens.ts radii, Shadows: spec/colors_and_type rgba(17,27,34,*) ladder == tokens.ts shadows xs..xl + focus 3px rgba(72,142,151,0.35), Motion: --ease-out / --ease-in-out / --dur-fast 120ms / base 200ms / slow 360ms == tokens.ts easings + durations.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| primary ink color (--ink vs --fg-primary) | spec.css:12 --ink: #111b22 (brand.900); body text uses var(--ink) at spec.css:39 | colors_and_type.css:54 --fg-primary: var(--gray-900) = #1a2626; semantic-tokens.ts:73 fg.DEFAULT = {colors.gray.900} = #1a2626 | spec-vs-tokens-conflict | 🟠 major | Pick ONE canonical primary ink. Recommended: make --ink the law and have colors_and_type.css + renderer agree. Either (a) change colors_and_type.css:54 to `--fg-primary: var(--brand-900);` and change semantic-tokens.ts:73+79 fg.DEFAULT/default base to `{colors.brand.900}`, OR (b) if gray.900 is intended for body, change spec.css:12 to `--ink: #1a2626;`. Document the choice in one place. Given spec.css frontmatter treats --ink as the brand ink, prefer (a). |
| off-4px-grid px values throughout spec vs strict 4px-grid claim | spec.css uses font-size 8.5/9.5/10.5/11.5/12.5/13.5px and padding 5/6/10/14/18/22px (e.g. spec.css:390 padding 12px 18px; :571 14px 18px; :700 14px 22px; :956 18px 22px; :404 font-size 10.5px; :415 12.5px; :268 13.5px; :446 11.5px; :577 9.5px; :4011 8.5px) | tokens.ts:131 spacing comment '4-pixel grid — multiples of 4 only. No 5/7/10/14/18/22.'; tokens.ts:182-195 fontSizes are integer-only (xs 12 / sm 14 / md 16 / ...) with no half-px tiers | spec-vs-tokens-conflict (intentional) | 🟠 major | Reconcile the grid policy in ONE place. Either (a) add the spec's half-px type tiers and odd spacings as named tokens (e.g. fontSizes xs2:11.5px, xs1:12.5px; spacing 4.5/2.5 or explicit '14px'/'18px'/'22px' keys) and soften the tokens.ts:131 comment, OR (b) ratify a strict grid and retune spec.css off-grid values to the nearest token (then spec stops being the literal law for those atoms). Recommend (a): make tokens.ts the consolidated source and let it carry the spec's deliberate off-grid values rather than denying them. |
| --info duplicates --brand-d (redundant token) | spec.css:30 --info: #3a7780  AND spec.css:23 --brand-d: #3a7780 (identical hex) | tokens.ts:111 status.info #3a7780 == tokens.ts:40 brand.600 #3a7780 (same hex, two roles) | spec-internal-inconsistency | 🟡 minor | Decide intent. If info should read distinct from brand, give --info its own hue (e.g. a desaturated blue) in spec.css:30 and tokens.ts status.info. If the alias is intentional, add a one-line comment at spec.css:30 noting `--info aliases --brand-d (#3a7780) by design — no separate info hue`. |
| dark-theme token block missing from spec.css | spec.css:11-31 :root has NO dark-theme override block (no [data-theme="dark"] / .dark / prefers-color-scheme — grep returned empty) | colors_and_type.css:136-156 defines a full [data-theme="dark"], .dark block; semantic-tokens.ts defines _dark variants for nearly every role (bg/fg/border/brand/link) | spec-internal-inconsistency | 🟡 minor | Add a dark block to spec.css mirroring colors_and_type.css:136-156, remapping the spec vars (--ink/--ink-2..--paper-3/--rule*/--brand*) to the dark scale so the atoms in spec.css respond to dark mode. Or explicitly state at spec.css:11 that the pattern spec is light-only and dark is defined solely in colors_and_type.css + semantic-tokens.ts (single source for dark). |
| --brand-dd duplicates --ink-2 (redundant token) | spec.css:24 --brand-dd: #2b4b55  AND spec.css:13 --ink-2: #2b4b55 (identical hex = brand.700) | semantic-tokens.ts:36/39 brand.fg/brand.emphasized = {colors.brand.700} #2b4b55; spec uses --ink-2 for emphasised body, --brand-dd for link.hover — both #2b4b55 | spec-internal-inconsistency (intentional) | ⚪ nit | Keep one as the source and alias the other in spec.css, e.g. `--ink-2: #2b4b55;` then `--brand-dd: var(--ink-2);` (or vice-versa), so a future retune touches a single line. No visual change. |
| --ink-5 #9bb0b0 (gray.400) has no semantic role token | spec.css:16 --ink-5: #9bb0b0 (used for faintest text / '.muted-2') | tokens.ts:52 gray.400 #9bb0b0 exists, but semantic-tokens.ts defines NO fg role at gray.400 (fg.subtle stops at gray.500); no fg.faint / muted-2 role | spec-vs-tokens-conflict | ⚪ nit | Add a `fg.faint` (or `fg.disabled`) semantic role mapped to {colors.gray.400} in semantic-tokens.ts so the spec's --ink-5 has a named renderer counterpart; document it as the '.muted-2' tier. |

### `spec:spec-internal-dupes` — spec-needs-consolidation
> 📋 Open — pure spec.css-internal consolidation; none of the renderer edits touch these. Still OPEN: duplicate `.btn.primary:hover` (717), `.evt-committed` using raw-scale `var(--brand-700)` instead of `--brand-dd` (3754/3756), split `.eyebrow-row` atom (386-410 + 4077-4093), unused `.evt-span` / `.planner-capacity-bar` / `.planner-now.thead`, dead `._dummy_btn_primary_anchor_` (718-720), `!important` in `.evt-scenario-both` (4057-4060), and the repeated mono-uppercase-label cluster. See `00-STATUS.md`.

- **Spec anchors:** spec.css:386-410 .eyebrow-row (atom def) · spec.css:412-437 .btn / .btn.primary / .btn.primary:hover · spec.css:711-721 .commit-bar .btn.primary + dead/dup rules · spec.css:3729-3806 Collections.Planner .evt / .evt-committed / .evt-span · spec.css:3808-3906 .planner-now / .planner-now.thead / .planner-now-hint / .planner-conflict / .planner-capacity-bar · spec.css:3908-4016 .planner-grid / .planner-row / .planner-rh / .planner-cell / .planner-bucket / .evt.selected · spec.css:4018-4075 .scenario-pill / .scenario-mode-toggle / .evt-scenario-* · spec.css:4077-4093 second .eyebrow-row block · colors_and_type.css:16-25 --brand-50..--brand-900 scale · **Renderer:** /home/crambelsoupy/src/east-workspace/libs/east-ui/design/spec.css · /home/crambelsoupy/src/east-workspace/libs/east-ui/design/colors_and_type.css
- **Summary:** Audited all of spec.css (4093 lines) plus the 366-line uncommitted append (git diff HEAD, lines 3725-4093: Collections.Planner + Scenario chrome + eyebrow-row). Confirmed every item the task flagged. (a) Dead rule ._dummy_btn_primary_anchor_ (718-720): 0 HTML usages, delete it. (b) .btn.primary:hover is duplicated byte-for-byte at 437 (canonical atom) and 717 (orphaned, unscoped, inside the commit-bar section right before the correctly-scoped .commit-bar .btn.primary:hover at 721) — delete 717. (c) .eyebrow-row now has TWO definitions: the atom (386-410) and an additive block at 4081-4092 — non-conflicting but should be folded back into the atom (and its stale §1.5b comment dropped). (d) The appended planner/scenario block is purely additive at EOF and does not overwrite earlier atoms, so no value conflicts; but it introduces a tokens-naming conflict: .evt-committed (3754/3756) uses var(--brand-700) — a colors_and_type.css raw-scale var NOT present in spec.css's own :root (which uses semantic --brand-d/--brand-dd) and, unlike the pre-existing hedge at 3098, omits the #2b4b55 fallback. It only renders correctly because every HTML links colors_and_type.css before spec.css. Change to var(--brand-dd). (e) Unused selectors with no consumer in any design/*.html: .evt-span (3795-3806), .planner-capacity-bar (3892-3906), .planner-now.thead (3828-3838) — add a consumer or delete. Plus: !important in .evt-scenario-both (4057-4060) is a spec-atom specificity smell (reorder/raise specificity instead); and the mono-uppercase-label property cluster repeats across dozens of selectors (62 text-transform:uppercase rules) — a single .mono-label utility would consolidate it. No renderer-drift: this is a pure spec-internal review. Net: 1 nit dead rule, 1 minor dup, 1 minor tokens-conflict var rename, 1 minor split-atom, 3 unused atoms, plus 2 nit cleanups.
- **Confirmed aligned:** The recently-appended 366-line block (spec.css:3725-4093, git diff HEAD) is purely additive at EOF and does NOT redefine any earlier atom with conflicting values — .btn, .frame, .chip, .status etc. are untouched., .eyebrow-row atom (386-410) and the appended .eyebrow-row block (4081-4092) do not conflict on shared properties: the atom sets layout/padding/border/background + .lbl/.meta typography; the append only ADDS flex-wrap/min-width/overflow ellipsis. Cascade is additive, not contradictory., New planner/scenario hexes that are spelled out (rgba(184,90,74,*) for --neg #b85a4a, rgba(72,142,151,*) for --brand #488e97) resolve to the correct token families., border-radius:2px on .evt/.evt-span/badges matches the pre-existing chip micro-radius already used at spec.css:781 — consistent within the spec's chip vocabulary even though 2px is below the sm=4px token., .evt-committed background var(--brand-700) resolves at runtime to #2b4b55 because every design/*.html links colors_and_type.css BEFORE spec.css (configure.html:6-7, index.html:6-7) — so the rendered pixel is correct (= --brand-dd = --ink-2).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| .btn.primary:hover (exact duplicate) | spec.css:437 .btn.primary:hover { background: var(--brand-dd); border-color: var(--brand-dd); } (canonical, inside the buttons atom block) | spec.css:717 .btn.primary:hover { background: var(--brand-dd); border-color: var(--brand-dd); } — byte-identical selector+declaration, sitting inside the .commit-bar section (after .commit-bar .btn.primary at 711-716) where it is UNSCOPED, so it re-declares the global button hover from inside an unrelated section. | spec-internal-inconsistency | 🟡 minor | Delete spec.css line 717 (keep the canonical atom rule at 437 and the scoped commit-bar override at 721). |
| .evt-committed background/border var name (scale var vs spec semantic var) | spec.css :root (11-30) defines ONLY semantic brand names: --brand-d (#3a7780) and --brand-dd (#2b4b55). Every other brand reference in spec.css uses these (e.g. 432,435,437,712,715). brand.700 = #2b4b55 = --brand-dd. | spec.css:3754 background: var(--brand-700); and 3756 border-color: var(--brand-700); — uses the colors_and_type.css raw-scale token --brand-700, which is NOT declared in spec.css's own :root. Unlike the pre-existing hedge at spec.css:3098 (var(--brand-700, #2b4b55)) it also omits the literal fallback. | spec-vs-tokens-conflict | 🟡 minor | In spec.css change lines 3754 and 3756 from var(--brand-700) to var(--brand-dd) to match the spec's semantic naming (same hex #2b4b55). |
| .eyebrow-row split across two definitions | spec.css:386-410 atom block fully defines .eyebrow-row + .lbl + .meta + .meta .sep (layout, 12px 18px padding, border-bottom, --paper-2 bg, mono uppercase typography). | spec.css:4081-4092 adds a SECOND .eyebrow-row { flex-wrap:nowrap; min-width:0; } plus .eyebrow-row .lbl/.meta { min-width:0; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; } 3700 lines away from the atom. | spec-internal-inconsistency | 🟡 minor | Fold the four declarations at spec.css:4082-4092 into the atom block: add flex-wrap:nowrap; min-width:0 to the .eyebrow-row rule at 386-393, add min-width/white-space/overflow/text-overflow + flex-shrink to .lbl (394-401) and .meta (402-409); then delete the 4077-4093 block including its §-ref comment. |
| .evt-span — defined, never used | spec.css:3795-3806 defines .evt-span (block, height 22px, line-height 22px, mono 11px) + .evt-span.evt-committed + .evt-span.evt-proposed. | grep 'evt-span' across all design/*.html = 0 occurrences. No Planner Span example consumes it. | spec-internal-inconsistency | 🟡 minor | Either add an .evt-span usage to the Planner Span article in configure.html, or delete spec.css:3795-3806 until a consumer exists. |
| .planner-capacity-bar — defined, never used | spec.css:3892-3906 defines .planner-capacity-bar + .fill + .near .fill (--warn) + .over .fill (--neg). | grep 'planner-capacity-bar' across all design/*.html = 0 occurrences. | spec-internal-inconsistency | 🟡 minor | Add a row-header capacity bar to the Planner example in configure.html, or delete spec.css:3892-3906. |
| ._dummy_btn_primary_anchor_ (dead rule) | spec.css:718-720 defines ._dummy_btn_primary_anchor_ { border-left: 1px solid var(--brand-d); } | 0 occurrences of the class in any design/*.html (grep across all 8 HTML files returns 0) | spec-internal-inconsistency | ⚪ nit | Delete spec.css lines 718-720 entirely. |
| .planner-now.thead (header now-marker) — defined, never used | spec.css:3828-3838 defines .planner-now.thead + ::before pip; comment (3824) says 'Use class .planner-now.thead to opt in to the label'. | grep for 'planner-now thead' / 'planner-now.thead' multi-class usage in design/*.html = 0 (plain .planner-now is used 10x, but never combined with .thead). | spec-internal-inconsistency | ⚪ nit | Drop the unused .planner-now.thead rule (3828-3838) and collapse the two redundant comment blocks (3823-3827) into one, OR add the .thead marker to the Planner header in configure.html. |
| !important inside spec atoms | Spec atoms elsewhere never use !important to win the cascade (they rely on specificity/order). | spec.css:4056-4061 .evt-scenario-both sets background, color, and border-style with !important. | spec-internal-inconsistency | ⚪ nit | Remove the three !important flags at spec.css:4057-4060 and instead place .evt-scenario-both after the base .evt-committed/.evt-proposed rules (or raise selector specificity, e.g. .evt.evt-scenario-both) so the cascade wins naturally. |
| Repeated mono-uppercase-label property cluster | The cluster {font-family:var(--font-mono); font-weight:600/700; letter-spacing:~0.06–0.18em; text-transform:uppercase} recurs across .eyebrow-row .lbl (394-400), .status (372-382), .scenario-mode-toggle .mode (4039-4046), .planner-row.group-head .planner-rh (3931-3936), .planner-bucket-label (4010-4016) and many more (62 text-transform:uppercase rules, 208 font-mono rules total). | Each site re-declares the same four-property mono-uppercase-label recipe inline rather than extending a shared class. | spec-internal-inconsistency | ⚪ nit | Introduce one .mono-label atom near the typography atoms (~spec.css:371) carrying font-family/font-weight/text-transform/default letter-spacing; have the new planner/scenario label selectors (3932, 4011, 4041) and the eyebrow .lbl/.meta compose it, overriding only font-size/letter-spacing. Lower priority than the dup/dead/var-name fixes. |
