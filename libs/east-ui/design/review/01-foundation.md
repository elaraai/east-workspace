## Foundation — theme config vs design tokens

A token-level crosswalk between the canonical design sources (`spec.css`, `colors_and_type.css`) and the Chakra theme renderer, covering the theme config and the spec-internal token conflicts it surfaces.

### `theme-config` — minor-drift
> 🟡 Resolved: primary ink fg.DEFAULT/fg.default now brand.900 #111b22 (matches spec --ink; colors_and_type --fg-primary also retargeted to var(--brand-900)). Still open: --info==--brand-d / --brand-dd==--ink-2 duplicate-hex consolidation, brandHeat lacking the gray.100 empty-cell anchor; intentional (kept): 11px caption.eyebrow, frame/nav/header border.strong lift, 10px frame radius. See `00-STATUS.md`.
- **Spec anchors:** spec.css:11-31 (:root tokens) · spec.css:36-42 (html/body) · spec.css:343-369 (.dot + @keyframes pulse) · spec.css:371-382 (.status) · spec.css:776-788 (.heat heatmap scale) · colors_and_type.css:14-134 (:root scales/type/spacing/radii/shadows/motion) · colors_and_type.css:159-176 (html/body + .h1-.h6/.eyebrow/.code/a semantic type) · **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts`, `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts`, `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/text-styles.ts`, `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/layer-styles.ts`, `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/global-css.ts`, `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/keyframes.ts`, `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/index.ts`
- **Summary:** The theme foundation layer is a near-exact reproduction of the canonical design tokens. Colour scales (brand, gray, brandTint, brandHeat, accent, overlay, status), radii, the 4px spacing grid, all cool-ink shadows + focus ring, durations/easings, fontWeights/fontSizes/lineHeights/letterSpacings, fonts, the html/body baseline, table tnum, focus-visible reset, and the three pulse/blink keyframes all match spec.css and colors_and_type.css to the pixel. text-styles tiers map cleanly onto the spec .h1-.h6/.eyebrow/.mode-id/.cell ladder. One real foundation-level discrepancy: fg.DEFAULT/fg.default resolve to gray.900 #1a2626, but the spec.css visual law uses --ink = brand.900 #111b22 for all primary text — a genuine green-gray-vs-teal-ink shift across every body string. Critically this is a spec-vs-tokens CONFLICT, not a clean renderer bug: colors_and_type.css line 54 sets --fg-primary to gray.900 (agreeing with the renderer) while spec.css :root --ink says brand.900 — the two design sources disagree and must be consolidated. Two consolidation nits (spec duplicate hexes --brand-d==--info, --brand-dd==--ink-2 are faithfully mirrored; brandHeat lacks the spec's gray.100 empty-cell anchor) and three documented intentional deviations (frame/nav/header border.strong lift, 11px caption.eyebrow, 10px frame radius) round it out. No visual snapshot needed — this is token-level and fully resolvable by reading the files.
- **Confirmed aligned:** Brand scale (tokens.ts:33-44) identical to spec/colors_and_type --brand-* (50 #f0fffe … 900 #111b22), Gray scale (tokens.ts:47-58) identical to colors_and_type --gray-* (50 #f8fafa … 900 #1a2626), brandTint #e8f6f7 == spec --brand-tint, brandHeat 0-4 (#e8f0f0/#c0dadc/#88b8bd/#4d8e95/#2b4b55) == spec .heat.l1-l5, status hues pos #2f7a5b / neg #b85a4a / warn #b8862d / info #3a7780 == spec, radii sm 4/md 6/lg 8/xl 12/2xl 16/full 9999 == colors_and_type --r-*, 4px spacing grid == colors_and_type --sp-*, shadows xs/sm/md/lg/xl + focus rgba(72,142,151,0.35) == colors_and_type --shadow-*, durations fast 120/normal 200/slow 360 + easings out/inOut == colors_and_type --dur-*/--ease-*, fontWeights/fontSizes (xs 12 .. 6xl 60)/lineHeights == colors_and_type --fw-*/--fs-*/--lh-*, fonts heading=DM Sans / body=Inter Tight / mono=JetBrains Mono == spec, global-css html/body + table tnum + focus-visible + code/pre mono align with spec, keyframes spec-pulse-live/run + spec-blink mirror spec @keyframes pulse/blink, border.focus/focusRing/border.brand = brand.500 #488e97 == spec --brand/--border-focus, link.DEFAULT = brand.600 #3a7780 / link.hover = brand.700 #2b4b55 == colors_and_type --link/--link-hover.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| fg.DEFAULT / fg.default (primary text ink) | --ink #111b22 (brand.900) — spec.css:12; colors_and_type body color resolves to --fg-primary | gray.900 #1a2626 (semantic-tokens.ts:73,79) | spec-vs-tokens-conflict | 🟡 minor | Decide one source of truth for primary ink. If spec.css --ink #111b22 wins (it is the stated visual law and the html/body color), change semantic-tokens fg.DEFAULT + fg.default base to `{colors.brand.900}` and update colors_and_type --fg-primary to var(--brand-900). If gray.900 is the deliberate choice, change spec.css --ink to #1a2626 to consolidate. Resolve the spec-vs-tokens conflict rather than leaving both. |
| brand semantic role mapping vs spec eyebrow/link ink (consolidation note) | spec --brand-d #3a7780 == --info #3a7780 (two names, one hex); --brand-dd #2b4b55 == --ink-2 #2b4b55 (two names, one hex) | status.info #3a7780 == brand.solid (brand.600 #3a7780); brand.fg/brand.emphasized = brand.700 #2b4b55 == ink-2 equivalent (tokens.ts:111, semantic-tokens.ts:34-39) | spec-internal-inconsistency | ⚪ nit | No renderer change. Optionally, in spec.css, define `--info: var(--brand-d)` and `--ink-2: var(--brand-dd)` so the duplicate hexes are expressed as aliases rather than independently-maintained literals. |
| heatmap resting/empty cell base | .heat base (no level) = --paper-3 #f1f5f5 (spec.css:782); .heat.l1 = #e8f0f0 | brandHeat scale starts at '0' = #e8f0f0 (tokens.ts:80); no separate empty/resting token | renderer-drift | ⚪ nit | If any heat renderer needs the spec empty cell, use bg.muted (gray.100 #f1f5f5) for the resting state and reserve brandHeat 0-4 for active levels; or add a brandHeat empty/base entry = #f1f5f5. No change needed if renderers already route empties through gray.100. |
| caption.eyebrow / cell-label font-size | .cell .lbl / .cap-eyebrow / .sc-eyebrow 9.5-10px (spec) | caption.eyebrow 11px (text-styles.ts:188-198); label-sm 10px; tag.kv.k 10px | intentional-deviation (intentional) | ⚪ nit | None. Keep as documented (text-styles.ts:181-187 — 11px chosen so JetBrains Mono cap-tops stay crisp under sub-pixel hinting). |
| frame / nav.panel / header.bar border colour | spec .frame / .nav / header use --rule gray.200 #e2e8e8 | layer-styles frame/header.bar/nav.panel use border.strong gray.300 #cbd5d5 (layer-styles.ts:31,148,162) | intentional-deviation (intentional) | ⚪ nit | None. Keep as documented (layer-styles.ts:24-25,138-139,154-156 — deliberate +1 step contrast lift so the 1px rule renders against the gray.50 canvas). |
| frame border-radius | spec .frame radius 10px (spec frontmatter / .frame) | layerStyles.frame borderRadius 10px (layer-styles.ts:30) | intentional-deviation (intentional) | ⚪ nit | None — correct as-is. 10px is off the radii scale (4/6/8/12/16) but matches the spec .frame exactly, so the renderer rightly hardcodes 10px rather than snapping to lg(8) or xl(12). |

### `spec:spec-token-conflicts` — spec-needs-consolidation
> 🟡 Resolved: the MAJOR primary-ink conflict — fg.DEFAULT and colors_and_type --fg-primary now both resolve to brand.900 #111b22, agreeing with spec.css:12 --ink. Still open: off-4px-grid type/spacing values (tokens.ts:131 grid policy unchanged), --info==--brand-d and --brand-dd==--ink-2 duplicates, spec.css missing a dark block, and gray.400/--ink-5 having no fg.faint role. See `00-STATUS.md`.
- **Spec anchors:** spec.css:11-31 (:root token block) · spec.css:12 --ink #111b22 · spec.css:13 --ink-2 #2b4b55 · spec.css:23 --brand-d #3a7780 · spec.css:24 --brand-dd #2b4b55 · spec.css:30 --info #3a7780 · spec.css:39 color: var(--ink) (body) · colors_and_type.css:14-134 (:root) · colors_and_type.css:25 --brand-900 #111b22 · colors_and_type.css:37 --gray-900 #1a2626 · colors_and_type.css:54 --fg-primary: var(--gray-900) · colors_and_type.css:136-156 (dark theme block) · **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts`, `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts`, `/home/crambelsoupy/src/east-workspace/libs/east-ui/design/spec.css`, `/home/crambelsoupy/src/east-workspace/libs/east-ui/design/colors_and_type.css`
- **Summary:** spec.css :root (lines 11-31) is light-only and largely agrees with colors_and_type.css + tokens.ts, but six consolidation issues remain. (1) MAJOR token conflict: --ink #111b22 (brand.900) is the spec's primary body ink (spec.css:39), but colors_and_type.css:54 routes --fg-primary through gray.900 #1a2626 and the renderer's fg.DEFAULT (semantic-tokens.ts:73) resolves to gray.900 #1a2626 — two different canonical inks on the most-used text color; pick one (recommend brand.900). (2) MAJOR grid conflict: spec.css pervasively uses off-4px values (font 8.5/9.5/10.5/11.5/12.5/13.5px; padding 5/6/10/14/18/22px) while tokens.ts:131 forbids exactly 5/7/10/14/18/22 and ships integer-only font sizes — the renderer cannot express these without inline literals, so it rounds and drifts; reconcile by adding the spec's half-px/odd tokens to tokens.ts as the single source. (3) MINOR: --info #3a7780 is identical to --brand-d / brand.600 — info status is indistinguishable from brand; give info its own hue or document the alias. (4) NIT: --brand-dd #2b4b55 duplicates --ink-2 (brand.700) — alias one to the other so a retune touches one line. (5) MINOR: spec.css has no dark block while colors_and_type.css:136-156 and semantic-tokens.ts ship full dark themes — dark mode is unspecifiable against the pattern spec; add a dark remap or declare spec light-only. (6) NIT: --ink-5 gray.400 has no named fg role in semantic-tokens.ts. Net recommendation: make tokens.ts the single consolidated token source (carrying the spec's deliberate off-grid + brand.900 ink), and have spec.css/colors_and_type.css alias into it rather than restating divergent values. All values read directly from source; no rendering verification needed for a token-level audit.
- **Confirmed aligned:** spec --ink-3 #4a5f5f == gray.600 == fg.muted, spec --ink-4 #6b8080 == gray.500 == fg.subtle, spec --paper #ffffff / --paper-2 #f8fafa / --paper-3 #f1f5f5 == gray.50/100 + white (bg.surface/canvas/muted), spec --rule #e2e8e8 / --rule-strong #cbd5d5 == gray.200/300 == border.subtle/strong, spec --brand #488e97 == brand.500 / --brand-d #3a7780 == brand.600 / --brand-dd #2b4b55 == brand.700 / --brand-l #94f9f9 == brand.200 / --brand-tint #e8f6f7 == brandTint, spec --pos #2f7a5b / --neg #b85a4a / --warn #b8862d / --info #3a7780 == status.pos/neg/warn/info, fonts --font-body Inter Tight / --font-brand DM Sans / --font-mono JetBrains Mono == FONT_BODY/FONT_BRAND/FONT_MONO, radii sm4/md6/lg8/xl12/2xl16/full9999 identical across all three, shadows rgba(17,27,34,*) ladder == shadows xs..xl + focus 3px rgba(72,142,151,0.35), motion --ease-out/--ease-in-out/--dur-fast 120ms/base 200ms/slow 360ms == easings + durations.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| primary ink color (--ink vs --fg-primary) | spec.css:12 --ink: #111b22 (brand.900); body text uses var(--ink) at spec.css:39 | colors_and_type.css:54 --fg-primary: var(--gray-900) = #1a2626; semantic-tokens.ts:73 fg.DEFAULT = {colors.gray.900} = #1a2626 | spec-vs-tokens-conflict | 🟠 major | Pick ONE canonical primary ink. Recommended: make --ink the law and have colors_and_type.css + renderer agree. Either (a) change colors_and_type.css:54 to `--fg-primary: var(--brand-900);` and change semantic-tokens.ts:73+79 fg.DEFAULT/default base to `{colors.brand.900}`, OR (b) if gray.900 is intended for body, change spec.css:12 to `--ink: #1a2626;`. Document the choice in one place. Given spec.css frontmatter treats --ink as the brand ink, prefer (a). |
| off-4px-grid px values throughout spec vs strict 4px-grid claim | spec.css uses font-size 8.5/9.5/10.5/11.5/12.5/13.5px and padding 5/6/10/14/18/22px (e.g. spec.css:390 padding 12px 18px; :571 14px 18px; :700 14px 22px; :956 18px 22px; :404 font-size 10.5px; :415 12.5px; :268 13.5px; :446 11.5px; :577 9.5px; :4011 8.5px) | tokens.ts:131 spacing comment '4-pixel grid — multiples of 4 only. No 5/7/10/14/18/22.'; tokens.ts:182-195 fontSizes integer-only (xs 12 / sm 14 / md 16 / ...) with no half-px tiers | spec-vs-tokens-conflict (intentional) | 🟠 major | Reconcile the grid policy in ONE place. Either (a) add the spec's half-px type tiers and odd spacings as named tokens (e.g. fontSizes xs2:11.5px, xs1:12.5px; spacing 4.5/2.5 or explicit '14px'/'18px'/'22px' keys) and soften the tokens.ts:131 comment, OR (b) ratify a strict grid and retune spec.css off-grid values to the nearest token (then spec stops being the literal law for those atoms). Recommend (a): make tokens.ts the consolidated source and let it carry the spec's deliberate off-grid values rather than denying them. |
| --info duplicates --brand-d (redundant token) | spec.css:30 --info: #3a7780 AND spec.css:23 --brand-d: #3a7780 (identical hex) | tokens.ts:111 status.info #3a7780 == tokens.ts:40 brand.600 #3a7780 (same hex, two roles) | spec-internal-inconsistency | 🟡 minor | Decide intent. If info should read distinct from brand, give --info its own hue (e.g. a desaturated blue) in spec.css:30 and tokens.ts status.info. If the alias is intentional, add a one-line comment at spec.css:30 noting `--info aliases --brand-d (#3a7780) by design — no separate info hue`. |
| dark-theme token block missing from spec.css | spec.css:11-31 :root has NO dark-theme override block (no [data-theme="dark"] / .dark / prefers-color-scheme — grep returned empty) | colors_and_type.css:136-156 defines a full [data-theme="dark"], .dark block; semantic-tokens.ts defines _dark variants for nearly every role (bg/fg/border/brand/link) | spec-internal-inconsistency | 🟡 minor | Add a dark block to spec.css mirroring colors_and_type.css:136-156, remapping the spec vars (--ink/--ink-2..--paper-3/--rule*/--brand*) to the dark scale so the atoms in spec.css respond to dark mode. Or explicitly state at spec.css:11 that the pattern spec is light-only and dark is defined solely in colors_and_type.css + semantic-tokens.ts (single source for dark). |
| --brand-dd duplicates --ink-2 (redundant token) | spec.css:24 --brand-dd: #2b4b55 AND spec.css:13 --ink-2: #2b4b55 (identical hex = brand.700) | semantic-tokens.ts:36/39 brand.fg/brand.emphasized = {colors.brand.700} #2b4b55; spec uses --ink-2 for emphasised body, --brand-dd for link.hover — both #2b4b55 | spec-internal-inconsistency (intentional) | ⚪ nit | Keep one as the source and alias the other in spec.css, e.g. `--ink-2: #2b4b55;` then `--brand-dd: var(--ink-2);` (or vice-versa), so a future retune touches a single line. No visual change. |
| --ink-5 #9bb0b0 (gray.400) has no semantic role token | spec.css:16 --ink-5: #9bb0b0 (used for faintest text / '.muted-2') | tokens.ts:52 gray.400 #9bb0b0 exists, but semantic-tokens.ts defines NO fg role at gray.400 (fg.subtle stops at gray.500); no fg.faint / muted-2 role | spec-vs-tokens-conflict | ⚪ nit | Add a `fg.faint` (or `fg.disabled`) semantic role mapped to {colors.gray.400} in semantic-tokens.ts so the spec's --ink-5 has a named renderer counterpart; document it as the '.muted-2' tier. |