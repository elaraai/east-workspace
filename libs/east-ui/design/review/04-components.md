## Recipe-less / bespoke components

This document reviews bespoke and recipe-less components against the canonical design spec (`design/*.html`, `spec.css`, `colors_and_type.css`) and the theme token sources, flagging each renderer's drift from the spec atoms.

### `component:charts`  — major-drift
> 🟡 Partial — series palette now consumes the brand-first `accent` scale at source (`charts/utils.ts`: `SERIES_COLOR_PALETTE` = accent keys, lead series = brand teal #488e97; `getPivotColorToken` maps slate→gray); still open: Recharts axis ticks not JetBrains Mono/10px/ink-4, sparkline default stroke=currentColor / strokeWidth 2 / fill 0.2, pie `chart.N` fallback, composed/area-range fallbacks, brush traveller raw #666, and the gridline spec-internal inconsistency. See `00-STATUS.md`.
- **Spec anchors:** spec.css:613-620 (.spark — height 28px, stroke brand 1.5, area fill brand-tint stroke brand-d) · spec.css:33-51 (charts.js baseAxis — axisLine rule, axisLabel ink-4 JetBrains Mono 10px 500, splitLine rule dashed; base textStyle mono) · spec.css:824-843 (.tband threshold bands — good #e3f2ec / accept #f3efe1 / bad #f4e4e0, mono 10.5px uppercase ink-2) · design/charts.js:18-31 (C palette — brand #488e97, brandD #3a7780, brandTint #e8f6f7, ink4 #6b8080, rule #e2e8e8) · design/charts.js:231-244 (spark — stroke brandD width 1.5, area fill brandTint opacity 0.7) · design/charts-visx.js:22-36 (C palette — brand #3a7780, brandTint #e8f6f7, rule #e2e8e8, ink4 #6b8080) · design/charts-visx.js:51-73 (renderSpark — strokeWidth 1.6, area fillOpacity 0.10) · tokens.ts:85-95 (accent scale — canonical order brand/purple/orange/blue/teal/yellow/pink/slate)
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/utils.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/sparkline/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/area/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/bar/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/line/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/pie/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/scatter/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/radar/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/composed/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/charts/spec/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts`
- **Summary:** The visx-based ChartSpec renderer (charts/spec/index.tsx) is a faithful match: it resolves all chrome (mono font, 10px ink-4 labels, brand-tint fills, dotRadius 2.6, barRadius 2, spec tooltip) through the design tokens. The Recharts family (sparkline, area, bar, line, pie, scatter, radar, composed) drifts on two systemic axes. (1) Palette: the canonical accent scale in tokens.ts (brand-first order brand/purple/orange/blue/teal/yellow/pink/slate, #488e97 first) is DEAD — no Recharts renderer references accent.*; instead utils.ts SERIES_COLOR_PALETTE hard-codes [blue,purple,teal,orange,pink,cyan,green,red,yellow,indigo] mapped onto Chakra's vibrant default scales (blue.solid #3b82f6 first), pie uses the undefined chart.N family, composed defaults to teal.solid, area-range to gray.solid. So the default first series is vibrant blue/teal, never the muted spec brand, and the order/hues/source all diverge (critical). (2) Axis typography: the Recharts charts never set tick font/size/colour, so ticks render in the body sans font at Chakra defaults instead of JetBrains Mono 10px ink-4 tabular-nums (major; snapshots confirm). Plus sparkline-specific drift: strokeWidth 2 vs spec 1.5, default currentColor stroke instead of brand, area fill 0.2-of-stroke instead of fixed brand-tint. One spec-internal inconsistency: charts.js gridlines use --rule #e2e8e8 while the visx spec source uses --rule-strong #cbd5d5 for the same gridlines. Brush travellers use an off-palette raw #666. PNG snapshots exist and confirm the vibrant non-brand series colours and sans-serif axis labels.
- **Confirmed aligned:** spec/index.tsx visx ChartSpec resolves all chrome through tokens (mono, 10px fg.muted, areaOpacity 0.16, dotRadius 2.6, scatterRadius 3.4, barRadius 2, spec tooltip), toRechartsCartesianGrid defaults to border.muted (=--rule #e2e8e8), Area default fillOpacity 0.2 / AreaRange 0.4 within spec range, Radar fillOpacity 0.3 and Pie outerRadius 80 reasonable neutral defaults.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| palette · default series colour scale (all Recharts charts) | accent scale, canonical order brand #488e97, purple #8b5cf6, orange #f97316, blue #3b82f6, teal #14b8a6, yellow #eab308, pink #ec4899, slate #6b8080 (tokens.ts:86-95). Spec charts.js/charts-visx anchor series to brand #488e97 / brand-d #3a7780 first. | utils.ts:85-96 SERIES_COLOR_PALETTE = [blue, purple, teal, orange, pink, cyan, green, red, yellow, indigo] resolved via getDefaultSeriesColorToken as `<name>.solid` (Chakra built-in vibrant scales). First default series = blue.solid #3b82f6, NOT brand teal. Order, hues, and source scale all differ from accent. | renderer-drift | 🔴 critical | Renderer: replace SERIES_COLOR_PALETTE in utils.ts with the accent token order [accent.brand, accent.purple, accent.orange, accent.blue, accent.teal, accent.yellow, accent.pink, accent.slate] and resolve via chart.color('accent.brand') etc., so the first auto series is brand #488e97 and the order matches the canonical accent scale. |
| sparkline · stroke-width | .spark stroke-width 1.5 (spec.css:617); charts.js spark width 1.5; charts-visx renderSpark strokeWidth 1.6 | sparkline/index.tsx:152 strokeWidth={2} | renderer-drift | 🟠 major | Renderer: set strokeWidth={1.5} on the sparkline `<path>` in sparkline/index.tsx. |
| sparkline · default stroke colour | .spark stroke var(--brand) #488e97 (spec.css:616); area variant stroke var(--brand-d) #3a7780 (spec.css:620) | sparkline/index.tsx:41,120-126 default color = 'currentColor' (inherits text colour); examples then pick blue.solid #3b82f6 (line) / green / red — none default to brand. No brand fallback. | renderer-drift | 🟠 major | Renderer: default color to 'brand.solid' (#3a7780, = --brand-d) instead of 'currentColor' in toChakraSparkline, or set the IR default; for the area variant stroke to brand-d and fill brand-tint per spec.css:620. |
| axis · tick label typography (all Recharts charts) | charts.js baseAxis.axisLabel: JetBrains Mono, 10px, weight 500, colour ink-4 #6b8080 (charts.js:36-41); base textStyle fontFamily mono (charts.js:48); charts-visx ticks mono 9.5-10px ink-4 | No renderer sets axis tick font/size/colour — XAxis/YAxis props in utils.ts (toRechartsXAxis/toRechartsYAxis lines 258-349) carry dataKey/hide/axisLine/tickLine/label/tickMargin/orientation only; tick typography is left to @chakra-ui/charts defaults (sans body font, theme fg). Charts__charts_*.png confirm sans-serif non-mono axis ticks. | renderer-drift | 🟠 major | Renderer: set tick prop on XAxis/YAxis to { fontFamily: 'var(--font-mono)'/mono, fontSize: 10, fill: fg.muted } (and fontVariantNumeric tabular-nums) in toRechartsXAxis/toRechartsYAxis, mirroring spec/index.tsx labelSize/labelColor. |
| pie · slice colour palette | accent scale (tokens.ts:86-95); charts-visx renderPie default [brand #3a7780, pos #2f7a5b, ink4 #6b8080, warn #b8862d] | pie/index.tsx:133 fill = chart.color(entry.color ?? `chart.${index+1}`) — uses Chakra's built-in chart.1..chart.N palette, not accent.* and not the brand-first spec palette. Snapshot pieBasic shows vibrant blue/orange/magenta/green. | renderer-drift | 🟠 major | Renderer: replace `chart.${index+1}` fallback with the accent-ordered token getDefaultSeriesColorToken(index) once that is switched to the accent scale, so pie slices share the canonical chart palette. |
| composed · default series colour | accent scale brand-first (#488e97); spec composed (charts.js / visx) uses brand bars + pos line | composed/index.tsx default color 'teal.solid' for every series type (lines 86,112,138,158,184) = Chakra teal #14b8a6 | renderer-drift | 🟡 minor | Renderer: default composed series colour to accent.brand (#488e97) / the shared accent palette, matching the other chart renderers' default series colour. |
| area-range · default series colour | accent scale brand-first / brand-tint band | area/index.tsx:288 default color 'gray.solid' for area-range series | renderer-drift | 🟡 minor | Renderer: default the area-range series colour to brand.solid / accent.brand so the band reads as a brand-tinted confidence envelope per spec. |
| sparkline · area fill opacity | charts.js spark areaStyle opacity 0.7 on brand-tint; .spark.area fill solid var(--brand-tint) (spec.css:620); charts-visx area fillOpacity 0.10 | sparkline/index.tsx:144 fillOpacity={0.2} of the line colour (not brand-tint) | renderer-drift | 🟡 minor | Renderer: fill the area path with bg.brand.subtle (brand-tint #e8f6f7) at full or ~0.7 opacity rather than 0.2 of the stroke colour, to match .spark.area. |
| gridline stroke colour — spec internal inconsistency | charts.js baseAxis splitLine color var(--rule) #e2e8e8 (gray.200); BUT spec/index.tsx visx Frame gridStroke = border.strong = var(--rule-strong) #cbd5d5 (gray.300), comment 'matching the spec diagrams' | toRechartsCartesianGrid default stroke border.muted = gray.200 #e2e8e8 (utils.ts:378) | spec-internal-inconsistency | 🟡 minor | Spec: pick one gridline token for charts (recommend --rule #e2e8e8 to keep gridlines below axis lines) and align charts-visx.js gridStroke + spec/index.tsx gridStroke + Recharts default to it; otherwise document the heavier visx gridline as intentional. |
| brush · traveller fill | No chart-brush atom in spec.css; foundation expects brand/border tokens for interactive chrome (brand #488e97, border.strong #cbd5d5) | area/bar/line/composed brush traveller fill='#666' (raw hex, e.g. area/index.tsx:189) and Tooltip cursor crosshair default; raw grey #666 is off-palette (nearest token gray.500 #6b8080 / ink-4) | renderer-drift | 🟡 minor | Renderer: replace fill='#666' on the four Brush travellers with a resolved token (border.strong #cbd5d5 or fg.subtle #6b8080), or the brand for active affordance. |

> 📸 Visual verify: `make east-ui-examples-html-charts` then compare the dist-examples PNG to the dist-design spec PNG.

### `component:layout-primitives`  — minor-drift
> 📋 Open — no layout-primitive edits landed: Card body paddingX still 20px (header/footer 18px), the stale `card` layer-style (6px/pad-4) still contradicts Card's 10px Frame shape, the Splitter handle is still unstyled Chakra default, and the chip-rail middle-dot separator still falls back to `inherit`. All real but minor / spec-internal. See `00-STATUS.md`.
- **Spec anchors:** spec.css:298-305 (.frame: --paper bg, 1px --rule border, 10px radius, overflow hidden) · spec.css:386-410 (.eyebrow-row: padding 12px 18px, paper-2 bg, bottom 1px --rule; .lbl mono 11px 600 ls .18em uppercase; .meta mono 10.5px) · spec.css:604-610 (.rail: grid, top+bottom 1px --rule, --paper bg) · spec.css:307-318 (.rule / .rule.dashed / .rule.brand) · tokens.ts:98-100 (overlay.scrollThumb rgba(17,27,34,0.30)) · tokens.ts:122-128 (radii sm4/md6/lg8/xl12 — comment: lg=cards) · layer-styles.ts:62-89 (card / card.flat / card.elevated: border.subtle, radius md 6px, padding 4) · east-ui card/index.ts:154 + card/types.ts:24 (Card = bsys Frame: 1px rule, 10px radius, paper)
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/container/card/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/scroll-area/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/splitter/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/chip-rail/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/box/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/flex/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/grid/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/stack/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/sticky/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/layout/separator/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/layer-styles.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts`
- **Summary:** Layout primitives are largely faithful. Box/Flex/Grid/Stack/Sticky/Separator are pure token pass-throughs with no hardcoded atom values, so they inherit the 4px grid + radii tokens cleanly. Card is the strongest atom: its 1px border.subtle (gray.200 = --rule) + 10px radius + overflow-hidden + mono paper-2 header strip (bg.canvas = gray.50 = --paper-2, padding 12px 18px) exactly match spec .frame + .eyebrow-row, and this matches the East Card type's own documented 'bsys Frame: 10px radius' intent. ScrollArea thumb resolves to overlay.scrollThumb rgba(17,27,34,0.30) — an exact match to the spec note. Findings: (1) Card body paddingX is 20px while header/footer are 18px — a 2px horizontal misalignment (renderer minor); (2) a spec-internal inconsistency — the unused 'card' layer-style (6px radius / padding 4) and the tokens.ts radii comment (cards=lg/8px) both contradict how Card actually renders (10px via .frame); the renderer is correct, the spec/theme metadata is stale; (3) the Splitter ResizeTrigger handle is unstyled and falls back to Chakra's default grey pill rather than the cool-ink rule/brand palette (no spec atom exists to pin it); (4) chip-rail separator colour falls back to inherit/border.subtle rather than the spec's --rule-strong divider weight when no separatorColor is set (nit). The brief's expectation that Card uses the 'card' layer-style (6px/padding16) is itself based on the stale layer-style — Card is intentionally the 10px Frame shape.
- **Confirmed aligned:** Card root (1px border.subtle + 10px radius + overflow hidden = spec .frame), Card header eyebrow strip (12px/18px padding, bottom hairline, bg.canvas paper-2, mono 11px), scroll-area thumb (overlay.scrollThumb rgba(17,27,34,0.30), thumb radius 4px, 8px rail / 2px track padding), chip-rail middle-dot separator with density-driven gap on the 4px grid, Box/Flex/Grid/Stack token pass-through, Sticky position/zIndex layering, Separator orientation/variant pass-through.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| card · body horizontal padding (paddingX) | 18px (header/footer use 18px; spec .eyebrow-row + frame content blocks use 18px, e.g. .pad-18) | 20px (Card.Body paddingX="20px" while Card.Header/Footer paddingX="18px") | renderer-drift | 🟡 minor | Renderer: change Card.Body paddingX from "20px" to "18px" so body, header and footer share the same 18px horizontal inset (matches spec .eyebrow-row / .pad-18). |
| card layer-style vs Card renderer (radius + padding) | layer-styles.ts 'card' = borderRadius {radii.md}=6px, padding 4 (16px), border.subtle; tokens.ts radii comment marks lg(8px)='cards' | Card renderer uses borderRadius 10px (the .frame shape), bespoke header/body/footer padding — never references layerStyle="card" | spec-internal-inconsistency | 🟡 minor | Spec/theme side: either delete or repurpose the unused 'card' layer-style (6px) so it stops contradicting the 10px Card shape, and correct the tokens.ts radii comment (cards render at 10px via .frame, not lg/8px). No renderer change. |
| splitter · handle (ResizeTrigger) colour/width | no dedicated .splitter atom in spec.css; nearest foundation = border.subtle/strong rule lines (gray.200/gray.300) for gutter dividers | ChakraSplitter.ResizeTrigger rendered with zero inline styling — inherits Chakra's default splitter recipe (rounded grey pill handle + default gutter), see snapshot Layout__layout_splitter.png | renderer-drift | 🟡 minor | Either add a splitter slot-recipe / inline borderColor=border.subtle + brand handle on the ResizeTrigger, or add a .splitter atom to spec.css to define the canonical handle width/colour, then match. Visual verify after. |
| chip-rail · middle-dot separator colour fallback | rail/inline divider colour = --rule-strong (gray.300 #cbd5d5) per .meta .sep / .eyebrow-row .sep usage | middle-dot box color falls back to "inherit" when style.separatorColor unset (chip-rail/index.tsx:74); line separator borderColor falls back to undefined (Chakra Separator default border.subtle gray.200) | renderer-drift | ⚪ nit | Renderer: default the middle-dot color and line borderColor to a muted token (e.g. fg.subtle / border.strong) instead of "inherit" / undefined so unstyled chip rails match the spec divider weight. |
| scroll-area · scrollbar track default | spec has no scroll-track atom; thumb only (overlay.scrollThumb rgba(17,27,34,0.30)) | Scrollbar background = trackColor (only when style.trackColor set), otherwise transparent; rail width/height 8px, track padding 2px | intentional-deviation (intentional) | ⚪ nit | No change required; optionally add a scroll-area thumb/track spec atom (8px rail, 4px thumb, overlay.scrollThumb) to make the rail dimensions a documented atom rather than renderer-only. |

### `component:slice-composites`  — minor-drift
> 📋 Open — no slice-composite edits landed: sliceFrame.root still 4px radius (vs .frame 10px), chip neutral/brand/more text still one step too dark (chip.ts color `fg` and brand.800 vs spec brand.700), the eyebrow pills still reuse generic .chip at 12px/500, cohort dot/breakdown padding/range label/summary vocab all unchanged. The chip `fg` color now resolves to brand.900 via the foundation-ink fix but spec wants brand.700, so it remains drift. See `00-STATUS.md`.
- **Spec anchors:** design/spec.css:463-490 (.chip / .chip.brand / .chip.dashed / .chip.compact) · design/spec.css:298-305 (.frame, radius 10px) · design/spec.css:870-877 (.cap-eyebrow) · design/slice.html:100-141 (.filter-chip / .add-chip / .more-chip / .cohort-pill) · design/slice.html:93-99 (.search compact pill) · design/slice.html:827-859 (Slice.Legend rail) · design/slice.html:677-727 (Slice.Range pill + picker) · design/slice.html:729-788 (Slice.Breakdown) · design/slice.html:861-879 (Slice.Summary)
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/slice/range/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/slice/legend/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/slice/summary/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/slice/filter/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/slice/cohort/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/slice/search/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/slice/breakdown/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/chip.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/slot-recipes/sliceFrame.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/slot-recipes/sliceEdit.ts`
- **Summary:** Slice composites are structurally faithful to design/slice.html and largely match the foundation tokens; verdict is minor-drift with one major item. The shared chip recipe matches spec .chip exactly on size/padding/radius/border/gap, but two colour tokens drift one step too dark: neutral chip text uses fg=gray.900 #1a2626 where spec .chip wants --ink-2/brand.700 #2b4b55, and brand+more chip text use brand.800 #1f363d where spec wants --brand-dd/brand.700 #2b4b55 (chip.ts:21,42,45). The one major issue: the focused affordance card (sliceFrame.root) uses a 4px corner while every focused density in slice.html wraps in spec .frame (10px) — and the Summary renderer itself uses 10px, so the two card paths disagree (sliceFrame.ts:38 vs summary/index.tsx:36). The compact eyebrow pills (filter/add/more/cohort) reuse the generic .chip at 12px/weight-500 rather than the slice-specific .filter-chip/.add-chip/.more-chip/.cohort-pill atoms (mono 10.5–11px, weight 600–700, brand.600 border) — a reasonable consolidation but it renders the eyebrow pills visibly larger/lighter than slice.html draws; this is the main spec-vs-renderer item to reconcile (pick one chip vocabulary). Cohort compact pill substitutes an 8px series-colour dot for the spec fa-users glyph and uses fg.muted count colour vs spec --brand-d. Smaller items: focused breakdown resulting-series chips are hand-rolled (paddingY 6px → ~2px taller than the chip recipe and the dimension chips beside them); the Range picker 'COMPARE WITH' label reuses the 9.5px clauseConj slot instead of the 10px .cap-eyebrow; and Summary says 'narrowings' where spec literal is 'filters'. Legend rail, sliceEdit popover anatomy, and the compact search pill match spec essentially pixel-for-pixel. Documented deviations (border.strong chassis border) are recorded as intentional. spacing.1.5 used in sliceFrame resolves to Chakra default 6px (createSystem merges defaultConfig), so no broken token.
- **Confirmed aligned:** chip base font-size 12px / weight 500 / padding 4px-10px / radius 4px / gap 6px / border.strong, chip dashed tone fg.subtle, chip brand bg.brand.subtle, chip compact 11.5px / 3px-8px, Summary radius 10px + 18px/14px padding + bg.surface, Summary result-count mono tabular-nums / divider fg.muted / clear-all link, Legend swatch 14x3px radius 1px mono 10.5px 16px gap, Legend Others dashed pill, Search compact pill padding 10px/5px radius 4px mono 11px + kbd hint 9px, Range pill mono chip with calendar+chevron, sliceEdit popover head/body/foot anatomy + sm 320px / lg 380px widths.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| sliceFrame.ts · root border-radius (focused filter/breakdown/search card) | .frame radius 10px (spec.css:300); slice.html focused densities wrap in `<div class="frame">` | borderRadius 4px (sliceFrame.ts:38) | renderer-drift | 🟠 major | Renderer: sliceFrame.ts `root.borderRadius` should be 10px (== spec .frame), matching the Summary renderer and slice.html .frame usage. |
| chip.ts · base color (neutral chip text) | --ink-2 #2b4b55 (brand.700) — spec .chip color: var(--ink-2) | fg (gray.900 #1a2626) | renderer-drift | 🟡 minor | Renderer: chip.ts base `color` should be `{colors.brand.700}` (== --ink-2 #2b4b55) instead of `fg`. |
| chip.ts · brand tone color + more tone color | --brand-dd #2b4b55 (brand.700) — spec .chip.brand color, .more-chip color | {colors.brand.800} #1f363d | renderer-drift | 🟡 minor | Renderer: chip.ts brand tone and more tone `color` should be `{colors.brand.700}` to match --brand-dd #2b4b55. |
| filter compact chip · font-size / weight / border (vs slice.html .filter-chip) | .filter-chip: mono 10.5px, font-weight 600, border 1px --brand-d (brand.600 #3a7780), padding 4px 9px 4px 11px (slice.html:100-108) | chip({tone:brand,numeric:true,shape:pill}): mono 12px, weight 500, border border.brand (brand.500 #488e97), padding 4px/10px (filter/index.tsx:86, chip.ts base) | renderer-drift | 🟡 minor | Spec-vs-renderer consolidation: either add a chip `eyebrow`/compact-pill variant (mono 10.5px, weight 600, brand.600 border) or accept .chip as the single source and update slice.html .filter-chip to match. Prefer one chip vocabulary. |
| more/add chip · font-size / weight (vs slice.html .more-chip / .add-chip) | .more-chip mono 10.5px weight 700; .add-chip mono 10.5px weight 600, dashed border --rule-strong (slice.html:110-119) | chip more tone: mono 12px weight 700 (bold) brand.500 border; dashed tone: mono 12px weight 500 (filter/index.tsx:106,153) | renderer-drift | 🟡 minor | Same consolidation as filter chip — add a compact eyebrow chip size (10.5px) and lift add-chip weight to 600, or align slice.html to .chip. |
| cohort compact pill · structure (swatch dot vs spec users icon) | .cohort-pill: fa-users glyph + name + .count (--brand-d) + fa-pen; border --brand-d, radius 4px, mono 11px, weight 600 (slice.html:131-140, 798-805) | chip({tone,numeric}) with a leading 8px round series-color dot + name + ·count(fg.muted) + fa-pen; radius 4px, mono 12px (cohort/index.tsx:166-171) | renderer-drift | 🟡 minor | Decide spec direction: if the series dot is intended, update slice.html .cohort-pill to show the dot; otherwise renderer should use the fa-users glyph. Count colour should be brand.600 (--brand-d) per spec, and font 11px. |
| breakdown focused · resulting-series chip padding (off-grid spec vs chip recipe) | spec uses .chip atom (padding 4px 10px) for resulting-series chips (slice.html:759-766) | bespoke inline chip paddingX 10px paddingY 6px, radius {radii.sm} 4px, body xs 12px (breakdown/index.tsx:142-147) | renderer-drift | 🟡 minor | Renderer: build the resulting-series chip from the chip recipe (chip({numeric:true})) with an 8px dot, instead of inline paddingY 6px, so it matches the dimension chips and spec .chip. |
| range picker · COMPARE WITH label uses clauseConj (9.5px) vs spec .cap-eyebrow (10px) | spec picker section labels use .cap-eyebrow mono 10px weight 600 ls 0.18em (slice.html:702 'Compare with'; spec.css:870-877) | edit.clauseConj: mono 9.5px ls 0.16em fg.subtle, textAlign left (range/index.tsx:144, sliceEdit.ts:115-121) | renderer-drift | 🟡 minor | Renderer: range picker section labels should use a cap-eyebrow style (10px, ls 0.18em) — e.g. sliceFrame footerLabel or a dedicated eyebrow slot — not clauseConj. |
| sliceFrame.ts · root border-color | .frame border 1px var(--rule) gray.200 #e2e8e8 (spec.css:300) | border.strong (gray.300 #cbd5d5) (sliceFrame.ts:37) | intentional-deviation (intentional) | ⚪ nit | No change required (documented). For consistency, decide one border weight for both Summary (border.subtle) and sliceFrame.root (border.strong). |
| summary · narrowing vocabulary | spec Slice.Summary reads 'N results · M filters · clear all' (slice.html:865-877) | 'N results · M narrowings · clear all' (summary/index.tsx:52) | renderer-drift | ⚪ nit | Align one side: either update spec slice.html to 'narrowings' (if that is the chosen vocabulary) or revert renderer to 'filters'. |
| search compact pill · gap (spec 6px vs renderer spacing.1.5 6px) and kbd radius | .search gap 6px, border-radius 4px; kbd hint border 1px --rule, radius 2px, 9px (slice.html:93-99, 632) | searchPill gap {spacing.1.5}=6px, radius 4px; searchKbd radius 2px, 9px, border.subtle (gray.200) (sliceFrame.ts:219-276) | renderer-drift | ⚪ nit | No change — search compact pill matches spec. |

### `component:typography-extras`  — minor-drift
> 📋 Open — no typography-extras edits landed: List default disc marker still inherits text colour (not the brand-teal .dotlist dot), Numeric default mono.lg still has no weight (vs spec .num 500), Mark/Highlight still default to Chakra warm-yellow, and standalone Code still leans on the host recipe. The markdown body-size and th letter-spacing items remain intentional/nit. See `00-STATUS.md`.
- **Spec anchors:** spec.css:334-335 .mono / .num · spec.css:524-546 .dotlist + .dotlist li::before · spec.css:868 .text-r · spec.css:323-333 .kbd · colors_and_type.css:168-173 .p/.lead/.small/.caption/.code · text-styles.ts:120-153 body.md/lead/small/caption · text-styles.ts:246-296 mono.* + mono.kpi
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/text/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/numeric/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/list/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/highlight/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/mark/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/code/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/typography/markdown/markdown.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/text-styles.ts`
- **Summary:** Typography-extras (text, numeric, list, highlight, mark, code, markdown) largely matches the spec: Numeric resolves to JetBrains Mono tabular (mono.lg) with correct pos/neg/neutral sentiment hexes and a semibold mono-kpi hero; markdown maps headings to the DM Sans display ladder, inline code to mono/0.92em/bg.muted/r-sm, blockquote to a 3px brand.500 border, and rules/tables to gray.200 hairlines; Text is a faithful style pass-through. Snapshots in dist-examples confirm all seven render. The notable drift is List: the signature spec atom is .dotlist with 5px brand-teal (#488e97) dot bullets, but the renderer's default unordered marker is a native grey disc inheriting text colour (markerColor defaults to none) — wrong colour family for the canonical list look (major). Secondary drifts: spec .num pins weight 500 but the Numeric default tier mono.lg carries no weight (renders 400); Mark/Highlight default to Chakra warm-yellow rather than the cool brand-tint (#e8f6f7) used elsewhere, and there is no spec .mark/.code atom to anchor against (spec gap); standalone Code leans on the host Code recipe rather than the spec .code metric; markdown paragraphs use the 14px product body (body.md) vs spec .p 16px/1.625, which is a documented intentional deviation. Recommend: spec adds canonical .mark/.code atoms; renderer defaults disc bullets to brand.500 and mono.lg to weight 500.
- **Confirmed aligned:** Numeric default run mono.lg JetBrains Mono 14px tabular-nums + tnum, Numeric explicit fontVariantNumeric tabular-nums on ChakraText, Numeric KPI hero mono-kpi 24px semibold lineHeight 1, Numeric sentiment colours pos→fg.success #2f7a5b / neg→fg.danger #b85a4a / neutral→fg.muted #4a5f5f, markdown inline + block inline code mono / 0.92em / bg.muted / radius sm, markdown headings h1-h4 → display.lg/md/sm/xs DM Sans, markdown blockquote 3px brand.500 left border, markdown hr/table borders border.subtle gray.200, Text renderer pure style pass-through, List custom check/dash markers as real FA SVGs with aria-label + markerColor tint.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| list · unordered marker color (default dot) | .dotlist li::before background: var(--brand) #488e97, a 5px round brand-teal dot | native list-style disc inheriting the item text colour (ink/gray); markerColor defaults to undefined so no brand tint (list/index.tsx:96-97,122; toChakraList markerColor=none by default) | renderer-drift | 🟠 major | Renderer side: default markerColor to brand.500 (#488e97) for disc/circle/square markers when the author hasn't set one, OR set the List.Root marker `_marker={{ color: 'brand.500' }}` so native bullets render in brand-teal to match .dotlist. |
| list · li font-size / line-height / padding | .dotlist li: font-size 13.5px, line-height 1.55, padding 3px 0 3px 18px; ::before at left 4px / top 11px / 5x5px (spec.css:530-546) | li content is dispatched through EastChakraComponent (whatever Text the author supplies); the List.Root only sets ps='5' (20px padding-start) and gap from style; no enforced 13.5px/1.55 li typography (list/index.tsx:96,144-157) | renderer-drift | 🟡 minor | No code change required if item typography is intentionally author-controlled; otherwise document that .dotlist rhythm (13.5px/1.55, 18px indent) must be set on items. Optionally change ps default from '5'(20px) toward the spec 18px indent. |
| mark · default background / highlight tint | No dedicated spec `<mark>` atom; the system's highlight idiom is brand-tint #e8f6f7 (bg.brand.subtle) or status-tinted chips. Highlight emphasis in the design is cool brand/status tints, not warm yellow. | Mark with no style falls through to Chakra's default Mark recipe = warm yellow (yellow.subtle) background; markCustomColor and Highlight default to yellow (mark/index.tsx:22-53 passes only author-set bg; Chakra default fills yellow) | spec-internal-inconsistency | 🟡 minor | Spec side: add a canonical .mark / highlight atom (brand-tint bg #e8f6f7, ink text) so there's a law to match. Renderer side (optional): default Mark/Highlight bg to bg.brand.subtle instead of inheriting Chakra yellow. |
| code · standalone Code component size default | .code / code = font-mono / 0.92em / bg var(--bg-tertiary) #f1f5f5 / padding 1px 5px / r-sm (colors_and_type.css:173) | EastChakraCode passes through size/variant/colorPalette to Chakra's Code recipe; with no style it uses Chakra Code defaults (size 'sm'), not a pinned 0.92em/bg.muted/1px-5px (code/index.tsx:27-55) | spec-internal-inconsistency | 🟡 minor | Verify the host Code recipe resolves to mono/0.92em/bg.muted(#f1f5f5)/1px-5px/r-sm; if not, align the Code recipe defaults to spec .code, or have toChakraCode default size to match 0.92em. |
| spec · .num vs renderer numeric default weight | .mono = font-mono + tnum (no weight); .num = font-mono + tnum + font-weight 500 (spec.css:334-335) | mono.lg (Numeric default) sets family+tabular but NO font-weight (text-styles.ts:254-261), so default Numeric is weight 400, whereas spec .num pins weight 500 | renderer-drift | 🟡 minor | Renderer side: add fontWeight 'medium' (500) to the mono.lg tier used as the Numeric default, or have Numeric default fontWeight to 500 to match spec .num. |
| markdown · paragraph body size (body.md) | .p / p = 16px (--fs-md) / line-height 1.625 (--lh-relaxed), colors_and_type.css:168 | markdown p uses textStyle body.md = 14px (fontSizes.sm) / line-height 1.5 (normal) (markdown.tsx:119; text-styles.ts:120-125) | intentional-deviation (intentional) | ⚪ nit | None. If a markdown block must match marketing .p (16/1.625), the author selects a body.lg-mapped context; the 14px product default is by design. |
| markdown · table header caption (textStyle caption letter-spacing) | .caption letter-spacing 0.02em (colors_and_type.css:171); th adds textTransform uppercase + letterSpacing 'wider' | th uses textStyle='caption' (12px) then overrides letterSpacing='wider' = 0.06em (markdown.tsx:184-196; tokens.ts:209) | renderer-drift | ⚪ nit | None needed; if strict caption fidelity is wanted, drop the letterSpacing='wider' override on th and let caption's 0.02em stand, or use a label-tier token. |

### `component:display-extras`  — no-spec-analog
> 🟡 Partial — the foundation-ink fix (fg.DEFAULT → brand.900) resolves the default icon-glyph ink nit (currentColor now #111b22 = spec --ink); still open: no Avatar recipe encodes the .avatar mono/600/ink-2/paper-3 atom, the icon colorPalette still resolves the raw `-500` shade (brand-500 not brand.700 ink), and the +N overflow chip still omits size/borderColor. See `00-STATUS.md`.
- **Spec anchors:** spec.css:807-822 (.avatar — 22px, mono 10px/600, bg --paper-3, border --rule, color --ink-2) · spec.css:1741-1747 (.mx-avatar — 24px, bg --brand-d, color --paper, mono 9px/700, letter-spacing 0.05em) · spec.css:365 (.dot.ring — inset ring pattern, only ring atom in spec) · global-css.ts:78-80 (.svg-inline--fa { verticalAlign: 0 } FA reset) · colors_and_type.css :root --ink #111b22 / --ink-2 #2b4b55 / --brand #488e97 · semantic-tokens.ts:72-86 (fg.DEFAULT=gray.900, fg.success/danger/warning/info=status.*)
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/display/avatar-group/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/display/avatar/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/display/icon/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/global-css.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts`
- **Summary:** display-extras (avatar-group + icon) has NO dedicated spec atom — spec.css only defines `.avatar` (22px mono/600, ink-2 on paper-3 with a rule border) and the filled `.mx-avatar` variant, plus the FA `vertical-align:0` reset. The icon renderer is largely faithful: it relies on the documented global FA vertical-align reset, defaults to currentColor, and passes the FA size scale straight through (md→1em, no off-grid rounding). Two icon issues: colorPalette resolves to a hardcoded raw `-500` shade (fine for status palettes whose -500 equals the spec ink, but brand-500 #488e97 is lighter/lower-contrast than the spec's brand.700 ink for branded glyphs), and the global fg=gray.900 (#1a2626) vs spec --ink=brand.900 (#111b22) is a known token-vs-spec mismatch affecting default icon colour. Avatar-group is built on raw Chakra defaults with two documented intentional overrides (spaceX -1.5 for a clean 6px stack, borderColor bg.surface for the white ring) — both confirmed good in the snapshot. The real drift is structural: no recipe encodes the `.avatar` atom's mono font / 600 weight / --ink-2 / --paper-3+--rule treatment, so fallbacks render in Chakra's default sans-serif gray; and the +N overflow Root omits `size`/`borderColor`, so it can render off-size from the stacked members. Snapshots present and Read (Display__display_avatar-group.png, Display__display_icon.png) confirm the rendered output.
- **Confirmed aligned:** icon FA vertical-align reset (global-css.ts:78-80 zeroes .svg-inline--fa { vertical-align }), icon default color currentColor (= fg), icon status colorPalette `<palette>-500` resolving to spec status inks (#2f7a5b/#b85a4a/#b8862d/#3a7780), icon size scale pass-through (md→1em FA default), avatar-group spaceX -1.5 (-6px) overlap as documented intentional override, avatar-group borderColor bg.surface (#ffffff) white separator ring.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| avatar-group/avatar · fallback typography + palette (whole avatar atom) | .avatar: 22px, font-family --font-mono, font-size 10px, font-weight 600, color --ink-2 (#2b4b55), background --paper-3 (#f1f5f5), border 1px solid --rule (#e2e8e8), border-radius 50%. .mx-avatar variant: 24px, bg --brand-d (#3a7780), color --paper (#fff), mono 9px/700, letter-spacing 0.05em | Chakra built-in Avatar/AvatarGroup with NO recipe override: fallback uses Chakra's default body/heading font (not --font-mono), default fallback weight/size for the size token, and Chakra's default gray fallback bg/fg — no border ring on individual avatars, border-radius from Chakra full. Size driven only by style.size?.type (defaults 'xs') | renderer-drift | 🟠 major | Add an Avatar slot-recipe (or layerStyle) encoding fontFamily 'mono', fontWeight 600, color brand.700 (=--ink-2), bg gray.100 (=--paper-3), border 1px border.subtle, and a brand-d filled variant for the mx-avatar look; apply it in avatar/index.tsx so AvatarGroup members inherit it. Renderer side changes. |
| icon · colorPalette resolution shade (brand & non-status palettes) | Icon used as a text glyph should take an ink role: emphasized brand text = --brand-dd/--ink-2 = brand.700 (#2b4b55); body ink = --ink (#111b22) | icon/index.tsx:54 `color: color ?? (colorPalette ? \`var(--chakra-colors-${colorPalette}-500)\` : undefined)` — hardcodes the raw `-500` shade. For colorPalette 'brand' this is brand.500 = #488e97 (the mid-tone focus/border brand), not the readable brand.700 ink the spec uses for branded text/glyphs. | renderer-drift | 🟡 minor | Resolve colorPalette through the semantic fg role instead of the raw scale: use `{colorPalette}.fg` / colorPalette + color='colorPalette.fg', or for status map to fg.success/danger/warning/info. Renderer side change in toFontAwesomeIcon. |
| avatar-group · overflow +N chip styling | No spec atom for an avatar-overflow chip; nearest ink/surface roles would be --ink-2 text on a --paper-3/--rule chip matching the .avatar atom | Overflow rendered as a plain `<ChakraAvatar.Root><ChakraAvatar.Fallback>+{n}` with NO size, NO borderColor and NO spaceX-aware sizing — it falls back to Chakra's default avatar size (not the group `size`/`xs`) and default fallback typography | renderer-drift | 🟡 minor | Pass `size={size ?? 'xs'}` and `borderColor={borderColor ?? 'bg.surface'}` to the overflow `<ChakraAvatar.Root>` so the +N chip matches the visible members; apply the same fallback typography. Renderer side change in avatar-group/index.tsx. |
| icon · default glyph ink (currentColor → fg vs spec --ink) | --ink = #111b22 (brand.900) is the spec's primary ink | undefined color ⇒ inherits currentColor ⇒ host fg.DEFAULT = gray.900 = #1a2626 (semantic-tokens.ts:73) | spec-vs-tokens-conflict | ⚪ nit | System-level: align fg.DEFAULT to brand.900 (#111b22) to match spec --ink, or accept the documented gray.900 ink. No change inside icon/index.tsx itself. |

### `component:forms-composites`  — minor-drift
> 🟡 Partial — the foundation-ink fix resolves the date/time segment text-colour nit (color `fg` now #111b22 = spec --ink); still open: segment digits still render in body Inter Tight (not JetBrains Mono), range field shells still round 6px / border `border` (gray.200) / pad 12-6px / focus brand.500, tags-input chips still raw Chakra, the selected radio-card still lacks brand-tint fill + inset ring, and TimeScaleControl's variant is still a no-op. See `00-STATUS.md`.
- **Spec anchors:** spec.css .param-row .p-input input/select L1595-1644 (font-mono, font-size 13px, border 1px --rule-strong, border-radius 4px, padding 7px 10px, focus border --brand-d + 3px ring) · spec.css .chip L463-490 (radius 4px, border --rule-strong, 12px body, brand-tint variant) · spec.css :root L12-25 (--ink #111b22, --rule #e2e8e8, --rule-strong #cbd5d5, --brand-d #3a7780, --brand-tint #e8f6f7) · spec.css .choice-card.selected L1294 (border-color --brand-d, box-shadow inset 0 0 0 1px --brand-d, background --brand-tint) · spec.css .mx-bar .seg / segmented-control (no dedicated TimeScale atom) · colors_and_type.css --r-sm 4px / --r-md 6px L113-114, --font-mono JetBrains Mono L72
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/forms/date-range-input/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/forms/time-range-input/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/forms/time-scale-control/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/forms/tags-input/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/forms/radio-card-group/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/forms/input/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/forms/input/date/CompoundDateField.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/forms/input/date/TimeField.tsx`
- **Summary:** Forms-composites (date-range-input, time-range-input, time-scale-control, tags-input, radio-card-group, input/date fields) are structurally faithful — correct dual-field range layout, segmented time-scale, interactive-state pattern, and documented focus-border-only model — but carry several minor pixel drifts against the spec input atom. The range field shells round to 6px (md) instead of the spec 4px input radius, default to the lighter gray.200 border (--rule) where the spec .param-row input uses --rule-strong (gray.300), pad on a 4px grid (12/6px) vs the spec off-grid 10/7px, and focus to brand.500 vs spec --brand-d (brand.600). The one major visible issue is that date/time segment digits render in the body font (Inter Tight) rather than JetBrains Mono — the spec sets all numeric form-input values in --font-mono, so the calendar/time fields lose tabular mono. tags-input chips use raw Chakra item chrome rather than the .chip recipe; the default selected radio-card omits the spec's brand-tint fill + inset 1px ring; and TimeScaleControl's variant prop is a no-op data attribute (Chakra SegmentGroup has no variant), so the SubtleVariant example is indistinguishable from default. A systemic fg (gray.900 #1a2626) vs spec --ink (#111b22) drift also touches segment text. Most fixes are renderer-side; the fg/--ink and the input-border-weight (--rule vs --rule-strong) items are system-wide reconciliations rather than per-composite bugs.
- **Confirmed aligned:** dual-field range layout (two field shells + en-dash separator fg.muted), focus-border-only model via _focusWithin borderColor, time-scale-control SegmentGroup colorPalette brand + Indicator with brand-tint selected fill, interactive-state pattern (useState + useEffect([value]) + queueMicrotask) across all five composites, disabled state (opacity 0.6 + italic dimmed read-only segments), radio-card selected border in brand family (brand.500/600).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| date/time segments · font-family | --font-mono (JetBrains Mono) — .param-row .p-input input font-family: var(--font-mono) | No fontFamily set on DateInput/TimeInput Box or DateSegment Box → inherits body (Inter Tight) | renderer-drift | 🟠 major | Renderer: add fontFamily "mono" (and fontVariantNumeric tabular-nums) to the DateSegment/TimeSegment Box in CompoundDateField.tsx + TimeField.tsx so segment digits render in JetBrains Mono like the spec param input. |
| date-range-input / time-range-input · field shell border-radius | 4px (.param-row .p-input input border-radius: 4px → --r-sm) | borderRadius: "md" = 6px (radii.md) | renderer-drift | 🟡 minor | Renderer: change fieldShell borderRadius from "md" to "sm" (4px) in both date-range-input/index.tsx L142 and time-range-input/index.tsx L94 to match the input recipe and spec --r-sm. |
| date-range-input / time-range-input · field shell border-color | --rule-strong #cbd5d5 (gray.300) — .param-row .p-input input border | borderColor ?? "border" → bare "border" token has no DEFAULT in semantic-tokens.ts, falling back to Chakra default border = gray.200 (#e2e8e8 / --rule) | renderer-drift | 🟡 minor | Renderer: change the fallback from "border" to "border.strong" (gray.300) in fieldShell of both range composites — or, if the gray.200 input border is the intended system default, lift it to a spec-vs-tokens reconciliation (the spec .param-row input should then read --rule). |
| date-range-input / time-range-input · field shell padding | padding: 7px 10px (.param-row .p-input input) — deliberate off-4px-grid | px=3 / py=1.5 = 12px / 6px (md size); sm = 8px/4px; lg = 16px/10px | renderer-drift | 🟡 minor | Renderer: tune fieldShell px/py to approximate spec 10px/7px (e.g. px=2.5, py=1.5) — or accept grid-snapping as an intentional tokens.ts policy and note it as such. |
| date-range-input / time-range-input · field shell focus border colour | --brand-d #3a7780 (brand.600) — .param-row input:focus border-color | _focusWithin borderColor "brand.500" = #488e97 | renderer-drift | 🟡 minor | Renderer: set _focusWithin borderColor to "{colors.brand.600}" to match spec --brand-d, or reconcile spec to brand.500 if 500 is the system focus hue. |
| tags-input · chip (Item) styling | .chip — border-radius 4px, 1px border --rule-strong (gray.300), 12px Inter Tight, brand-tint optional; or chip recipe (theme/recipes/chip.ts) | Raw Chakra ChakraTagsInput.Item/ItemPreview with no chip recipe applied — Chakra default tag chrome (default radius/border/colorPalette brand) | renderer-drift | 🟡 minor | Renderer: style ChakraTagsInput.ItemPreview to the chip recipe values (borderRadius 4px, borderColor border.strong, fontSize 12px body, color fg, × in fg.subtle) — or route the item through the elara-chip recipe so tags-input shares the canonical chip vocabulary. |
| radio-card-group · selected card fill + inset ring | .choice-card.selected — border-color --brand-d, box-shadow inset 0 0 0 1px --brand-d, background --brand-tint #e8f6f7 | Chakra RadioCard default (colorPalette brand): brand border on selected, but NO brand-tint fill and no inset 1px ring unless selectedCardBackground escape-hatch is passed (Basic example renders selected 'Team' card with white fill) | renderer-drift | 🟡 minor | Renderer: default selectedCardBackground to bg.brand.subtle (--brand-tint) and add an inset 1px brand ring on the selected Item when no override is given, matching .choice-card.selected. |
| date/time segments · text colour | --ink #111b22 (brand.900) — spec primary input text | color "fg" = fg.DEFAULT = gray.900 #1a2626 | renderer-drift | ⚪ nit | System-level: either re-point fg.DEFAULT to brand.900 (#111b22) to match spec --ink, or accept gray.900 as the intended body ink and update the spec. Not a per-composite fix. |
| time-scale-control · variant (subtle/outline/solid) | No dedicated TimeScale atom; segmented control should visibly differentiate emphasis variants | variantTag passed only as data-variant on SegmentGroup.Root (L79) — no visual effect; subtle vs default render identically in the PNG (timeScaleControlSubtleVariant looks the same chrome as Basic) | renderer-drift (intentional) | ⚪ nit | Renderer: either implement the variant via a theme-level SegmentGroup slot-recipe keyed on data-variant, or drop the variant style option from TimeScaleControl so the example doesn't imply a difference that isn't rendered. |

### `component:collections-inline`  — major-drift
> 🟡 Partial — Matrix off-palette drifts are resolved (segment fallback brand.solid, selection bg.brand.subtle + fg.default 2px outline offset -2px, hover bg.muted/hoverHighlightToken, emphasis fg.danger / 2px outline); still open: matrix legend/header text still literal gray.700 (not a semantic token), and the table pinned-column border still carries the wrong hex fallbacks (#e2e8f0 / #fafafa). The pagination/table-footer items remain unchanged but minor/nit. See `00-STATUS.md`.
- **Spec anchors:** spec.css:570-619 (.cell label/value/sub + .spark) · spec.css:3617-3727 (.cgrid heat-grid + .cgrid-bar + .cgrid-legend — Collections.Grid colour law) · spec.css:3741-3990 (.evt / .planner-cell.selected / .planner-now) · configure.html:24-124 (.lhs-headers / .lhs-cells / .col-i — Planner & Gantt left side, .gantt-deps)
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/collections/matrix/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/collections/table/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/collections/shared/column-pinning.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/collections/gantt/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/collections/planner/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/collections/tree-view/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/collections/data-list/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/collections/pagination/index.tsx`
- **Summary:** collections-inline is mostly clean — table, gantt, planner, tree-view, data-list and pagination drive colour/spacing through Chakra semantic tokens or East style props with no off-palette inline hex. The one serious offender is matrix/index.tsx, whose hardcoded defaults are the WRONG colour family: segment fill blue.400, selection blue.200/blue.700, hover yellow.50 + inline yellow.100, emphasis blue.500, where the spec's Collections.Grid/Matrix law is teal --brand-d (#3a7780) bars, --brand-tint (#e8f6f7) selection fill, a 2px --brand-d outline, brand-tint hover, and amber/ink secondary segments — never blue or yellow. Snapshot comparison (dist-examples Collections__collections_matrix.png vs dist-design configure__pattern__valuematrix.png) visually confirms blue renderer bars against teal spec bars, so this is real, highly visible drift (rated critical). Secondary issues: the matrix selection ring is also 3px inset vs the spec's 2px outline, legend/header text uses literal gray.700 instead of the --ink-2/fg semantic token, segment-label/cell-bg use raw "white" literals, and the table pinned-column border carries wrong hardcoded hex fallbacks (#e2e8f0/#fafafa vs --rule #e2e8e8 / --paper-2 #f8fafa). The table resize-handle gray.300 is the documented border.strong lift (intentional). All fixes are renderer-side: replace blue/yellow defaults with brand/brand-tint tokens and correct the hex fallbacks.
- **Confirmed aligned:** data-list/index.tsx fully style-prop driven (no inline hex/off-token px), pagination/index.tsx ghost ButtonGroup + IconButton all colour/state forwarded + 0-based↔1-based conversion, tree-view/index.tsx item/hover/selected/caret/connector all token-driven, table/index.tsx column widths via CSS vars + density-recipe padding + style-prop header chrome + resize-handle border.strong (gray.300), matrix grid lines default gray.200 (=--rule) + cellBorderRadius 2px (=.cgrid-bar/.evt) + headerBackground gray.50 (=--paper-2), planner/gantt chrome sticky header bg.panel / hover bg.muted / EventAxis border.muted (token-driven, no inline hex).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| matrix · segment fill default color (index.tsx:430) | --brand-d #3a7780 (teal); cgrid-bar seg.c-1 = var(--brand-d), multi-segment uses brand-d / warn / ink-derived tints — palette is teal+amber, never blue | getSomeorUndefined(seg.color) ?? colorByCategory.get(category) ?? "blue.400" (Chakra blue, a hue the spec never uses) | renderer-drift | 🔴 critical | Renderer: change the matrix segment fallback from "blue.400" to a brand token — "brand.600" (#3a7780 = --brand-d) to match .cgrid-bar .seg.c-1. |
| matrix · selectedBackground default (index.tsx:76) | .cgrid td.selected / .planner-cell.selected → background var(--brand-tint) #e8f6f7 with a var(--brand-d) outline | "blue.200" (Chakra blue, wrong family) | renderer-drift | 🔴 critical | Renderer: default selectedBackground to "bg.brand.subtle" / brandTint (#e8f6f7). |
| matrix · selectedBorderColor default (index.tsx:77) | .cgrid td.selected outline: 2px solid var(--brand-d) #3a7780 (and .planner-cell.selected outline 2px --brand-d) | "blue.700" applied as boxShadow inset 0 0 0 3px (index.tsx:420) | renderer-drift | 🔴 critical | Renderer: default selectedBorderColor to brand.600 (#3a7780); reduce the ring from inset 3px to a 2px outline with outlineOffset -2px to match .cgrid/.planner-cell.selected. |
| matrix · hoverHighlightColor default + inline cell hover (index.tsx:78, 400) | Spec has no yellow anywhere; .planner-cell.empty:hover = var(--brand-tint); heat hover stays within the brand-d tint ramp | default "yellow.50" for header/row hover (line 78) and a hardcoded "yellow.100" for cell-body hover (line 400) | renderer-drift | 🔴 critical | Renderer: default hoverHighlightColor to bg.brand.subtle (brand-tint #e8f6f7) or bg.muted (#f1f5f5); replace the hardcoded "yellow.100" at line 400 with the resolved hoverHighlightToken. |
| matrix · emphasis ring default (index.tsx:94) | Emphasis/ring colour in spec is var(--brand-d) #3a7780 (or status hues --neg/--warn for risk); never blue | emphasisDefault ?? "blue.500" → outline 3px solid (index.tsx:415) | renderer-drift | 🟠 major | Renderer: default the emphasis ring token to brand.600 (#3a7780). |
| matrix · legend label text colour (index.tsx:281, 684) | .cgrid-legend text color var(--ink-2) #2b4b55; header text uses --ink/--ink-4 | headerFg default "gray.700" and legend `<Text color="gray.700">` | renderer-drift | 🟡 minor | Renderer: use a semantic token (fg.muted for legend, fg/brand.fg for headers) instead of literal gray.700. |
| table · pinned-column border hex fallback (table/index.tsx:649,655 & shared/column-pinning.tsx:20) | --rule #e2e8e8 (border.subtle/gray.200) for the pin divider; --paper-2 #f8fafa for pinned bg | '2px solid var(--chakra-colors-border, #e2e8f0)' and 'var(--chakra-colors-bg-canvas, #fafafa)' | renderer-drift | 🟡 minor | Renderer: drop the literal hex fallbacks or set them to #e2e8e8 / #f8fafa to match the resolved semantic tokens. |
| matrix · segmentLabelColor default (index.tsx:79) | .cgrid-bar .seg color var(--paper) #ffffff — label-on-fill is white | "white" | renderer-drift | ⚪ nit | Renderer: prefer the white/bg.surface token for consistency; no visual change. |
| matrix · cell background default (index.tsx:401) | .cgrid / Matrix cell background var(--paper) #ffffff = bg.surface | cellBackground ?? "white" | renderer-drift | ⚪ nit | Renderer: use bg.surface token; no visual change. |
| table · column-resize handle bar colour (table/index.tsx:873) | No direct spec atom for a TanStack resize affordance; spec dividers use --rule #e2e8e8 / --rule-strong #cbd5d5 | _before bg: 'gray.300' (#cbd5d5 = border.strong) at 0.4 opacity | intentional-deviation (intentional) | ⚪ nit | None — matches the documented border.strong divider convention. |
| table · footer pager button radius (table/index.tsx:1252,1271) | radii.sm = 4px (spec uses 4px for .wb-pin etc.) | borderRadius: "4px" (raw px, inline `<button>` with border:none, transparent bg) | renderer-drift | ⚪ nit | Optional: render via the shared IconButton ghost recipe (as pagination/index.tsx does) so focus-ring and hover come from the button recipe rather than ad-hoc inline styles. |

### `component:overlays-inline`  — major-drift
> 🟡 Partial — dialog + drawer backdrops now use `{colors.overlay.backdrop}` (0.40 scrim) and toggle-tip now consumes the tooltip recipe (dark ink chip + arrow); still open: command-palette still bypasses its recipe (raw ChakraDialog, list maxH 400px, item 14px, body-font group labels), dialog eyebrow still inline 10px/0.18em, dialog boxShadow still `lg` (not xl), popover radius still 6px, and the menu/popover/hover-card inline-duplicate-recipe items. Dialog content radius 10px matches the frame card radius (intentional), and popover radius vs menu is fine. See `00-STATUS.md`.
- **Spec anchors:** spec.css:298-305 .frame (surface/modal radius 10px, border --rule, overflow hidden) · spec.css:155-202 .popover / .popover-head / .popover-body / .popover-foot (radius 4px, border --rule-strong, box-shadow var(--shadow-md)) · colors_and_type.css:121-125 --shadow-xs..xl ladder · tokens.ts:98-102 colors.overlay.backdrop = rgba(17,27,34,0.40) · tokens.ts:122-128 radii sm4/md6/lg8/xl12/2xl16 · tokens.ts:154-162 shadows xs..xl + focus · text-styles.ts:188-198 caption.eyebrow (mono 11px, semibold, 0.12em, uppercase, fg.muted) · slot-recipes/dialog.ts:17-83 · slot-recipes/popover.ts:13-52 · slot-recipes/menu.ts:17-64 · slot-recipes/hoverCard.ts:13-45 · slot-recipes/commandPalette.ts:14-76 · slot-recipes/tooltip.ts:18-38 · slot-recipes/actionBar.ts:15-32
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/overlays/dialog/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/overlays/drawer/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/overlays/popover/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/overlays/menu/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/overlays/hover-card/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/overlays/command-palette/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/overlays/toggle-tip/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/overlays/tooltip/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/overlays/action-bar/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/slot-recipes/dialog.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/slot-recipes/drawer.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/slot-recipes/popover.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/slot-recipes/commandPalette.ts`
- **Summary:** Inline-style audit of the overlays group. Tooltip and ActionBar are clean (recipe-only, no inline bypass). The real problems: (1) command-palette/index.tsx ignores its own theme-registered commandPaletteSlotRecipe entirely — it hand-rolls a ChakraDialog with ad-hoc Boxes, so the palette inherits dialog chrome and diverges on list maxHeight (400 vs 320px), item font (14 vs 13px), and group-label typography (12px body/medium/0.02em vs mono 11px caption.eyebrow). (2) toggle-tip renders a white Popover surface with p=1/width=auto instead of the dark tooltip chip its purpose implies. (3) dialog + drawer backdrops are hardcoded rgba(17,27,34,0.04) in their slot recipes while tokens.ts defines colors.overlay.backdrop = rgba(17,27,34,0.40) 'dialog backdrop' (and the commandPalette recipe correctly uses it) — a 10x scrim inconsistency, spec-internal. (4) dialog content radius (10px) and shadow (lg) disagree with the review brief's 2xl/xl targets, and 10px is not even a radii token; popover radius is three-way inconsistent (brief lg/8, spec.css .popover 4, recipe 6). (5) dialog eyebrow is hand-styled 10px/0.18em instead of caption.eyebrow (11px/0.12em). (6) popover/menu/hover-card renderers re-state their recipe's padding/minW/maxW/fontSize inline; hover-card's inline padding actively kills its own size variant. Snapshots only show closed triggers, so open-state visual confirmation of backdrops/radii/shadows is recommended.
- **Confirmed aligned:** tooltip/index.tsx recipe-only no inline overrides (delegates to tooltipSlotRecipe), action-bar/index.tsx no inline shadow/radius/padding (relies on actionBarSlotRecipe + Button outline/sm), menu inline Content props (minW 220px, padding 4px, fontSize 13px) equal menuSlotRecipe.base.content, popover inline Content props (padding 14px 16px, minW 240px, maxW 360px, fontSize 13px) equal popoverSlotRecipe.base.content, dialog backdrop rendered via `<ChakraDialog.Backdrop />` with no inline override.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| command-palette · slot-recipe bypass (uses ChakraDialog, never CommandPalette recipe) | commandPaletteSlotRecipe (registered as theme.slotRecipes.commandPalette) defines content radius lg/8px + shadow xl + backdrop overlay.backdrop, input mono/md, list maxHeight 320px, item radius sm + caption.eyebrow group labels | command-palette/index.tsx renders a raw `<ChakraDialog.Root size="md">` + hand-rolled `<Box>` list; the commandPaletteSlotRecipe is never applied. Chrome therefore comes from dialogSlotRecipe (radius 10px, shadow lg) + ad-hoc inline Boxes. | renderer-drift | 🟠 major | Renderer side: rebuild the palette on CommandPalette.* slots (or at minimum apply the commandPalette recipe class), or delete the unused recipe. Prefer wiring the recipe so radius/shadow/maxHeight/item styling come from one place. |
| command-palette · group label typography | commandPaletteSlotRecipe.groupLabel = textStyle caption.eyebrow (mono 11px, semibold, 0.12em widest, uppercase, fg.muted) | command-palette/index.tsx:184-194 `<Text fontSize="xs"(12px) fontWeight="medium"(500) letterSpacing="wide"(0.02em) textTransform uppercase color fg.muted>` — body font, not mono | renderer-drift | 🟠 major | Renderer: apply textStyle="caption.eyebrow" to group labels (drop the inline font props). |
| dialog · backdrop opacity | colors.overlay.backdrop = rgba(17,27,34,0.40) (the dedicated dialog-backdrop token); commandPaletteSlotRecipe.backdrop uses {colors.overlay.backdrop} | dialogSlotRecipe.backdrop.background = rgba(17,27,34,0.04) | spec-internal-inconsistency | 🟠 major | Recipe: dialogSlotRecipe.backdrop.background → {colors.overlay.backdrop}. (If 4% is the intended soft scrim, then the overlay.backdrop token's '0.40' is wrong and should be lowered — reconcile the two.) |
| drawer · backdrop opacity | colors.overlay.backdrop = rgba(17,27,34,0.40) | drawerSlotRecipe.backdrop.background = rgba(17,27,34,0.04) | spec-internal-inconsistency | 🟠 major | Recipe: drawerSlotRecipe.backdrop.background → {colors.overlay.backdrop}, consistent with the token's stated purpose. |
| toggle-tip · uses Popover chrome, not the dark tooltip chip | tooltipSlotRecipe = dark ink chip (bg fg.default, color bg.surface, mono 11px, radius sm, shadow sm). ToggleTip is documented as a click-activated tooltip. | toggle-tip/index.tsx:43-58 renders ChakraPopover.Content with inline p="1" width="auto" — a white popover surface (popoverSlotRecipe content: white bg, gray.300 border, shadow md), not the dark tooltip chip | renderer-drift | 🟠 major | Renderer: style the ToggleTip content to mirror the tooltip chip (dark bg fg.default, color bg.surface, mono 11px, radius sm, shadow sm) rather than a default white popover; remove the ad-hoc p=1/width=auto. |
| command-palette · list maxHeight | commandPaletteSlotRecipe.list.maxHeight = 320px | command-palette/index.tsx:174 maxH="400px" | renderer-drift | 🟡 minor | Renderer: change maxH to 320px (or consume recipe list.maxHeight). |
| command-palette · item label font-size | commandPaletteSlotRecipe.item.fontSize = 13px | command-palette/index.tsx:214 `<Text fontSize="sm">` = 14px (tokens.ts fontSizes.sm) | renderer-drift | 🟡 minor | Renderer: set item label fontSize to 13px. |
| command-palette · item radius / vertical padding | commandPaletteSlotRecipe.item: borderRadius radii.sm(4px), paddingX spacing.4(16px) paddingY spacing.2(8px), marginX spacing.2 | command-palette/index.tsx:204-206 px="3"(12px) py="2"(8px) borderRadius="sm"(4px); no marginX inset | renderer-drift | 🟡 minor | Renderer: px=4 + marginX=2 to match recipe (or render via recipe slot). |
| dialog · eyebrow inline typography vs caption.eyebrow | caption.eyebrow text-style = mono 11px, semibold, letterSpacing 0.12em (widest), uppercase, fg.muted | dialog/index.tsx:150-159 inline `<ChakraBox fontFamily="mono" fontSize="10px" fontWeight="600" letterSpacing="0.18em" textTransform uppercase color fg.muted>` | renderer-drift | 🟡 minor | Renderer: replace the inline eyebrow box with textStyle="caption.eyebrow" (11px / 0.12em / fg.muted). |
| dialog · content border-radius (recipe) | brief target dialog radius = 2xl (16px); spec .frame surface radius = 10px (off-token) | dialogSlotRecipe.content.borderRadius = 10px (matches .frame, NOT the 2xl/16px the review brief calls for) | spec-vs-tokens-conflict | 🟡 minor | Decide: if dialogs follow .frame, change brief target to 10px and document; if they follow the radii ladder, set recipe borderRadius to {radii.2xl} (16px). Currently neither tokens nor brief agree with the recipe. |
| dialog · content box-shadow (recipe) | brief target dialog shadow = token xl (0 20px 25px… ) | dialogSlotRecipe.content.boxShadow = lg (0 10px 15px… ) | renderer-drift | 🟡 minor | Recipe: set dialog content boxShadow to xl (matches commandPalette recipe which already uses xl). |
| command-palette · item label font-size (note) — covered above | — | — | renderer-drift | 🟡 minor | (see command-palette item label font-size) |
| popover · content border-radius (recipe) | brief target popover radius = lg (8px); spec .popover (slice.html:160) = 4px; .frame = 10px | popoverSlotRecipe.content.borderRadius = 6px (md) | spec-internal-inconsistency | 🟡 minor | Reconcile: pick one popover radius. spec.css .popover uses 4px (radii.sm) with --rule-strong border + shadow-md; aligning recipe to {radii.sm}(4px) would match the canonical HTML, but the brief asks for lg. Decide and unify brief + spec.css + recipe. |
| hover-card · inline maxW overrides recipe size variant | hoverCardSlotRecipe has size variants (xs/sm/md/lg) controlling padding; content maxWidth from base = 320px | hover-card/index.tsx:65 inline padding="14px 16px" minW="240px" maxW="320px" hardcoded on Content, while the renderer ALSO passes size={size} to Root | renderer-drift | 🟡 minor | Renderer: drop the inline padding (and minW/maxW) so the hoverCard size variant actually drives padding. |
| command-palette · item radius (note) — covered above | — | — | renderer-drift | 🟡 minor | (see command-palette item radius / vertical padding) |
| toggle-tip · content padding off-token | tooltip chip paddingX spacing.2(8px) paddingY spacing.1(4px) | toggle-tip/index.tsx:54 p="1" (4px all sides) width="auto" | renderer-drift | 🟡 minor | Renderer: paddingX=2 paddingY=1 (8px/4px) to match the tooltip chip. |
| dialog · inline maxW duplicates/overrides recipe size variants | dialogSlotRecipe.variants.size.{xs..full}.content.maxWidth (e.g. md=480px) is the single source for dialog width | dialog/index.tsx:16-24,148 a parallel DIALOG_SIZE_MAX map sets maxW inline on Content, duplicating the recipe variant table | renderer-drift | ⚪ nit | Renderer: pass size to ChakraDialog.Root (already done) and drop the inline maxW + DIALOG_SIZE_MAX so the recipe variant governs width. |
| drawer · fullscreen IconButton offset uses bare scale, not token | spacing tokens 4px-grid; close/expand chrome should sit on the grid | drawer/index.tsx:129 `<HStack position="absolute" top="2" right="2" gap="1">` (8px/8px/4px — these ARE grid values via Chakra scale) | intentional-deviation (intentional) | ⚪ nit | None — on-grid via scale. |
| popover · inline Content props duplicate recipe (dead override) | popoverSlotRecipe.base.content already sets padding 14px 16px, minWidth 240px, maxWidth 360px, fontSize 13px | popover/index.tsx:57 inline padding="14px 16px" minW="240px" maxW="360px" fontSize="13px" repeats the recipe verbatim | renderer-drift | ⚪ nit | Renderer: remove the inline padding/minW/maxW/fontSize; let popoverSlotRecipe govern. |
| popover · content border colour vs spec | spec.css .popover:159 border 1px solid var(--rule-strong) = gray.300 (#cbd5d5) | popoverSlotRecipe.content.borderColor = {colors.gray.300} ✓ | renderer-drift (intentional) | ⚪ nit | None. |
| menu · inline Content props duplicate recipe | menuSlotRecipe.base.content sets minWidth 220px, padding 4px, fontSize 13px | menu/index.tsx:46 inline minW="220px" padding="4px" fontSize="13px" repeats the recipe | renderer-drift | ⚪ nit | Renderer: remove inline minW/padding/fontSize; rely on menuSlotRecipe. |

> 📸 Visual verify: `make east-ui-examples-html-overlays` then compare the dist-examples PNG to the dist-design spec PNG.

### `component:navigation-deep`  — minor-drift
> ↪ Superseded by `06-nav-implemented-to-spec.md` — the active-item affordance is now the spec inset brand-tint pill (navList.ts: marginInline 8px, radii.sm, brandTint bg, brand.700, fontWeight bold), resolving the major left-rule drift and the active-weight minor; breadcrumb already matched. Still open: groupLabel section eyebrow remains 9.5px (spec 10.5px), no nested sub-item render path, and the per-item badge / gray.500 role / text-style-vs-recipe nits. See `00-STATUS.md`.
- **Spec anchors:** design/index.html:519-557 (Header recipe — Row 1 breadcrumb mono 11px/0.06em, Row 2 surface title brand 24/700/-0.015em, state eyebrow mono 10.5/0.14em) · design/index.html:644-716 (Sidebar recipe — 240px panel, paper-2 bg, item height 36px, item mono 12px/600/0.12em uppercase ink-3, ACTIVE = brand-tint pill 4px radius + 8px side inset + weight 700 brand-dd, section eyebrow mono 10.5px/600/0.18em ink-4, icon col 16px gap 10px) · design/index.html:2239-2245 (Breadcrumb rules — separator mono / in ink-4 never coloured, current page non-link ink weight 600, trailing run anchor = vertical rule + run #N) · spec.css/colors_and_type.css :root (--brand-d #3a7780, --brand-dd #2b4b55, --ink #111b22, --ink-3 #4a5f5f, --ink-4 #6b8080, --paper-2 #f8fafa, --rule #e2e8e8, --brand-tint #e8f6f7)
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/navigation/nav-list/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/navigation/breadcrumb/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/slot-recipes/navList.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/slot-recipes/breadcrumb.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/text-styles.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/layer-styles.ts`
- **Summary:** Breadcrumb matches the CURRENT Header recipe spec essentially 1:1 — mono 11px / 0.06em links in brand.600 (#3a7780 == --brand-d), mono '/' separator in gray.500 (#6b8080 == --ink-4), non-link current page in brand.900 (#111b22 == --ink) at weight 600, and a faithful trailing run-anchor (12px-inset vertical rule in --rule, 'run' in ink-4, #N in --ink/600). No breadcrumb drift. NavList (the bsys Sidebar recipe) matches the spec on type ramp, resting colour, card chrome, item height, icon column and gap, but lags the CURRENT spec on the active-item affordance: spec calls for a brand-tint PILL (4px radius, 8px side inset, weight 700), whereas the renderer paints a full-width brand-tint band with a 3px brand.600 LEFT RULE at weight 600 — the renderer's own comment describes this older look (one major + one minor drift). Section eyebrow is 9.5px vs the current spec's 10.5px (the nav.eyebrow text-style JSDoc still cites the stale 9.5px) — minor. Lesser items: an out-of-spec per-item badge (spec forbids notifications in the sidebar), no nested sub-item render path (spec defines one), a hard-coded {colors.gray.500} where fg.muted would be more dark-mode-correct (same light hex), and a spec-internal inconsistency between the 'breadcrumb' text-style token (fg.muted/500) and the breadcrumb slot recipe (brand.600/900) the renderer actually uses. All hex resolutions verified against tokens.ts/colors_and_type.css; snapshots Navigation__navigation_breadcrumb.png and Navigation__navigation_nav-list.png confirm the rendered output.
- **Confirmed aligned:** breadcrumb link brand.600 (=--brand-d), separator gray.500 mono (=ink-4), currentLink brand.900 weight 600 (=--ink), list/link mono 11px / 0.06em, run-anchor (vertical rule gray.200 / 12px inset / 'run' ink-4 / #N brand.900 wt 600), nav-list item mono 12px / 600 / 0.12em uppercase, resting item gray.600 (=ink-3), item height 36px / icon col 16px / gap 10px, active fill brandTint + active color brand.700 (=brand-dd), nav-list card surface paper-2/gray.50 + gray.200 1px border + radius lg 8px + paddingBlock 12px, groupLabel 0.18em uppercase gray.500 (=ink-4).
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| nav-list · active item — accent style (left rule vs pill) | Active = brand-tint pill: background --brand-tint, border-radius 4px, 8px side inset (margin 0 8px), NO left accent rule (index.html:656 + Dimensions line 716 'brand-tint fill 4px radius with 8px side inset (pill)') | Active = full-width brand-tint band with a 3px brand.600 left border (borderInlineStartColor on item), square corners, no side inset, no 4px radius (navList.ts:46-48,65-69) | renderer-drift | 🟠 major | Renderer (navList.ts item base + active): drop borderInlineStart* (or keep transparent), and on '&[aria-current=page],&[data-active]' set marginInline '8px', borderRadius '{radii.sm}' (4px), keep background brandTint; recompute paddingInline so total left inset reads as the spec pill. |
| nav-list · active item — font weight | Active label weight 700 (bold) — spec active anchor index.html:656 'font-weight: 700' | fontWeight semibold/600 for both resting and active (navList.ts:53, no weight bump on aria-current=page) | renderer-drift | 🟡 minor | Renderer: add fontWeight '{fontWeights.bold}' to the '&[aria-current=page],&[data-active]' block in navList.ts. |
| nav-list · groupLabel (section eyebrow) font-size | mono 10.5px (index.html:652 expanded section eyebrow + 662 Scope eyebrow, both 'font-size: 10.5px') | fontSize 9.5px (navList.ts:27); text-style nav.eyebrow also 9.5px (text-styles.ts:491) | renderer-drift | 🟡 minor | Bump groupLabel fontSize to 10.5px in navList.ts (and nav.eyebrow text-style to 10.5px) to match index.html:652/662; update the nav.eyebrow JSDoc from 9.5px to 10.5px. |
| nav-list · nested sub-item type | Spec defines a 'nested sub-item' item type — indented 36px, em-dash prefix, drops the uppercase styling, weight 500, 11px, color brand-d/ink-3 (index.html:658-659,684) | No nested/sub-item rendering path — NavList renders flat sections of uniform uppercase 12px items only (nav-list/index.tsx:44-83) | renderer-drift | 🟡 minor | If the IR supports nested items, add a sub-item render branch: paddingInlineStart ~36px, textTransform none, fontWeight medium/500, fontSize 11px, em-dash prefix, color brand.600/gray.600. If the IR does not model nesting, this is a spec-vs-IR scope gap to record, not a renderer bug. |
| nav-list · groupLabel color role | section eyebrow color --ink-4 #6b8080 (index.html:652) | color {colors.gray.500} #6b8080 (navList.ts:30) — exact hex match, but hard-codes the gray scale ref instead of a semantic role | renderer-drift | ⚪ nit | Optional: swap {colors.gray.500} -> fg.muted in groupLabel for dark-mode correctness; no light-mode pixel change. |
| nav-list · per-item badge | No per-item badge in the current Sidebar recipe; recipe explicitly Forbids 'notifications' inside the sidebar (index.html:686). Section eyebrow / divider / nested sub-item are the only non-link item types. | badge slot rendered trailing each item: mono 10px / weight 600 / letterSpacing 0.04em / color gray.500 (navList.ts:86-93); shown as '3' / '12' in the snapshot | renderer-drift | ⚪ nit | No code change required; if strict-spec conformance is wanted, document the badge as an intentional extension to the Sidebar recipe in index.html, or gate it off for the sidebar surface variant. |
| breadcrumb · text-style 'breadcrumb' weight vs slot recipe | Header Row 1 breadcrumb links carry no explicit weight in the spec mockup (default); current page weight 600 (index.html:527, 2244) | text-styles.ts breadcrumb token = fontWeight medium/500 + color fg.muted, but the renderer uses the breadcrumb SLOT recipe (link brand.600, currentLink brand.900/600), NOT the breadcrumb text-style — the two define different colours/weights for the same element name | spec-internal-inconsistency | ⚪ nit | Consolidate: have the app-shell header breadcrumb consume the same colour roles as the breadcrumb slot recipe (brand.600 link / brand.900 current), or document that text-styles.breadcrumb is the muted app-shell variant distinct from the in-content slot recipe. |

### `component:buttons-extras`  — major-drift
> 📋 Open — no buttons-extras edits landed: the East ButtonVariantType (solid/subtle/outline/ghost/plain) and the buttonRecipe variants (solid/ink/outline/ghost/danger/commit) still disagree (subtle/plain fall through to Chakra), Toggle still has no recipe-level pressed paint, CopyButton's icon-only default still renders a solid fill, no closeButton recipe is registered, and the commit-tracking 0.12em-vs-0.14em nit remains. See `00-STATUS.md`.
- **Spec anchors:** spec.css:413-446 (.btn family: 12.5px / weight 500 / padding 6px 12px / radius 6px / border 1px rule-strong / paper / --ink) · spec.css:431-437 (.btn.primary = brand-d fill, weight 600) · spec.css:438-443 (.btn.ghost = transparent, --ink-3) · spec.css:446 (.btn.compact = padding 4px 8px / 11.5px) · spec.css:1234-1240 (.diff-row .x-btn = 26x26 outline box, border 1px rule-strong, radius 4px, paper bg, --ink-3 glyph, 13px) · spec.css:1890-1902 (.lib-seg / .lib-seg-btn.active = brand-tint bg + brand-dd ink = the canonical pressed/selected segment treatment) · theme/recipes/button.ts:74-159 (recipe variants solid/ink/outline/ghost/danger/commit/commit-primary; sizes xs26/sm28/md32/lg40) · theme/recipes/icon-button.ts:33-48 (variants ghost/outline/solid/ink/subtle; defaultVariants ghost/md) · theme/slot-recipes/segmentGroup.ts:130-150 (brand-tint/_checked = brand.100 + brand.800 — the spec-aligned pressed treatment)
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/buttons/toggle/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/buttons/button-group/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/buttons/close-button/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/buttons/copy-button/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/button.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/recipes/icon-button.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui/src/buttons/toggle/types.ts` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui/src/buttons/button/types.ts`
- **Summary:** buttons-extras are recipe-routed composites: Toggle, CopyButton, CloseButton forward variant/size/colour straight to the button/iconButton recipes, and ButtonGroup correctly drives Chakra's attached `<Group>` (joined borders, radius only on ends — verified in the snapshot). Two major issues: (1) the East ButtonVariantType enum (solid/subtle/outline/ghost/plain) and the renderer buttonRecipe variant set (solid/ink/outline/ghost/danger/commit/commit-primary) DISAGREE — 'subtle'/'plain' have no Elara recipe variant so they silently fall through to Chakra's built-in defaults (the flat gray fill on the demoed toggles is Chakra's, not the canonical recipe), while ink/danger/commit are recipe-only and unreachable from East. (2) Toggle has NO recipe-level pressed paint — pressed visuals come only from per-example hex (Auto-refresh uses off-palette #eef2ff instead of brand-tint, and a pressed toggle with no hex is indistinguishable from unpressed), whereas the spec's canonical selected/pressed segment is brand-tint + brand-dd ink. Also: CopyButton's icon-only default renders a solid dark fill instead of the iconButton recipe's ghost default (recipe-merge issue), no closeButton recipe is registered so the × is unmediated Chakra default rather than spec .x-btn, and un-sized toggle/copy default to 14px vs spec .btn 12.5px. One spec-vs-tokens nit on the shared recipe: commit tracking is 0.12em (widest token) vs spec 0.14em. The Deploy button's blue is an intentional example colorPalette override, not drift.
- **Confirmed aligned:** button-group attached joined borders (radius only on end segments), button-group direct-child dispatch (data-first/last/between land on real DOM button), button-group Deploy blue is an intentional colorPalette example override, toggle interactive-state pattern (useState + useEffect([value.pressed]) + queueMicrotask), toggle/copy/close forward variant/size/bg/color/borderColor straight to the registered recipes.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| toggle · variant vocabulary (subtle/plain have no recipe analog) | buttonRecipe variants = solid/ink/outline/ghost/danger/commit/commit-primary (button.ts:74-152) | Toggle (and Button) East ButtonVariantType enum = solid/subtle/outline/ghost/plain (button/types.ts:35-44). 'subtle' and 'plain' are valid East inputs with NO matching recipe variant; 'ink','danger','commit','commit-primary' are recipe-only and unreachable from East. | spec-internal-inconsistency | 🟠 major | Reconcile the two variant vocabularies: either add 'subtle' and 'plain' variants to buttonRecipe (button.ts) mapping to spec values (subtle ~ bg.subtle fill / fg; plain ~ transparent no-border like .btn.ghost minus hover-bg), and drop or expose ink/danger/commit in ButtonVariantType — so every East-allowed variant resolves to an Elara recipe variant rather than Chakra's built-in fallback. |
| toggle · pressed-state styling (no recipe-level _pressed/aria-pressed paint) | .lib-seg-btn.active / segmentGroup brand-tint _checked = brand-tint bg (#e8f6f7→brand.100) + brand-dd/brand.800 ink (spec.css:1902; segmentGroup.ts:130-141) — the canonical pressed/selected treatment | toggle/index.tsx:65-70 applies pressed visuals ONLY from value.style.pressedBackground/pressedColor (per-example hex). The renderer sets data-pressed/aria-pressed (lines 79-80) but no recipe selector keys off it, so with no style hex the pressed state is visually identical to unpressed. | renderer-drift | 🟠 major | Add a recipe-level pressed treatment keyed on &[data-pressed]/[aria-pressed=true] (e.g. background: bg.brand.subtle / brand.muted, color: brand.fg) in buttonRecipe so Toggle has a canonical pressed paint without per-example hex; keep style.pressedBackground/pressedColor as escape hatches only. Then change the example off-palette '#eef2ff' to the brand-tint token. |
| copy-button · icon-only default fill (ghost recipe default not applied) | iconButton recipe defaultVariants.variant = ghost = transparent bg / fg.muted (icon-button.ts:35,48) | copyButtonBasic (CopyButton.Root with no style) renders a SOLID dark-teal/slate filled square in Buttons__buttons_copy-button.png, not a transparent ghost icon. | renderer-drift | 🟠 major | Ensure the registered iconButton recipe's defaultVariants actually apply (verify createSystem merge isn't shadowed by Chakra's defaultConfig IconButton), or have copy-button/index.tsx pass variant="ghost" explicitly when style.variant is absent so the icon-only copy button is transparent per spec. |
| toggle/copy-button · default font-size (recipe md vs spec .btn) | .btn font-size 12.5px (spec.css:415); .btn.compact 11.5px (spec.css:446) | buttonRecipe size md = fontSizes.sm = 14px (button.ts:156; tokens.ts:186). Toggle/CopyButton default to size md when no size is given, so an un-sized toggle/copy button renders 14px text vs spec 12.5px. | renderer-drift | 🟡 minor | Set buttonRecipe size md fontSize to 12.5px (the .btn base) rather than fontSizes.sm/14px, OR give Toggle/CopyButton a default size that lands on 12.5px; height 32px can stay. |
| close-button · no closeButton recipe registered (× chrome ≠ spec .x-btn) | .diff-row .x-btn = 26x26 box, border 1px rule-strong (#cbd5d5), border-radius 4px, background paper (#fff), color --ink-3 (#4a5f5f), font-size 13px (spec.css:1234-1240) | close-button/index.tsx renders Chakra's `<CloseButton>` with NO Elara closeButton recipe registered (theme/index.ts recipes block has no closeButton). The default basic close button (Buttons__buttons_close-button.png) is a borderless ghost × with no box/border/paper bg. | renderer-drift | 🟡 minor | Either register a closeButton recipe (or reuse iconButton with an outline-26px default) mapping to spec .x-btn (26x26, border 1px border.strong, radius sm/4px, bg.surface, color fg.muted, 13px), or document close-button as an intentional ghost-dismiss deviation in layer/text-styles so it is not unmediated Chakra default. |
| button recipe · commit variant letterSpacing token (adjacent, spec-vs-tokens) | .btn.commit-mono letter-spacing 0.14em (spec.css:451); .commit-bar draft 0.14em (spec.css:687) | buttonRecipe commit/commit-primary use letterSpacings.widest = 0.12em (button.ts:127,143; tokens.ts:210) | spec-vs-tokens-conflict | ⚪ nit | Add a letterSpacing token for 0.14em (e.g. widest at 0.14 or a dedicated 'commit' tracking) and reference it in the commit/commit-primary recipe variants, or accept 0.12em as the consolidated value and update the spec .btn.commit-mono to 0.12em. |

### `component:feedback-extras`  — minor-drift
> 🟡 Partial — the foundation-ink fix resolves the banner-title default-ink nit (title now inherits fg.DEFAULT = brand.900 #111b22 = spec --ink); still open: partial/info banner still pairs slate bg with a teal border+icon, description body still fg.muted (not primary ink), change-banner border/icon still too light, the split radius/padding/title-size drifts, and the three-overlapping-banner-systems spec-internal inconsistency. See `00-STATUS.md`.
- **Spec anchors:** spec.css:723-744 (.banner dashed + .banner .icon + .banner.solid + .banner .actions) · spec.css:1648-1661 (.banner guardrail family: guard/stale/partial/change/error + .banner-title) · spec.css:2332-2357 (.bn family: .bn / .bn-stale / .bn-partial / .bn-change / .bn-icon / .bn-body / .bn-actions) · colors_and_type.css token crosswalk (--brand #488e97, --brand-d #3a7780, --brand-dd #2b4b55, --info #3a7780, --ink #111b22, --warn #b8862d)
- **Renderer:** `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/feedback/banner/index.tsx` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/layer-styles.ts:180-250` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/semantic-tokens.ts:44-116` · `/home/crambelsoupy/src/east-workspace/libs/east-ui/packages/east-ui-components/src/theme/tokens.ts:33-120`
- **Summary:** Banner renderer (feedback/banner/index.tsx + layer-styles banner.*) correctly achieves the headline goal: full 1px coloured surround, NO 4px left-accent stripe, warn/error palettes match spec hex, dashed-stale uses top/bottom-only dashed borders, actions/dismiss align right. The drift is in colour-pairing and spacing. MAJOR: (1) partial/info banners pair the correct slate-blue bg (rgba(91,110,135,0.08)) with a TEAL border+icon (fg.info #3a7780) instead of the spec's slate #5b6e87 — a mismatch the spec never has; (2) description body text is fg.muted #4a5f5f vs spec --ink #111b22 (body should be primary ink, not muted). MINOR: change-banner border (#488e97 vs spec --brand-d #3a7780) and icon (#488e97 vs spec --brand-dd #2b4b55) are 1-2 steps too light; border-radius is a flat 4px vs spec's split 2px (.bn) / 4px (guardrail); surround padding 10px vs spec .bn 12px vertical; dashed.stale 20px vs spec 18px horizontal; title 14px vs spec 13px. The underlying SPEC-INTERNAL-INCONSISTENCY (major): spec carries three overlapping banner systems (.banner dashed 723, .banner guardrail 1648, .bn family 2332) with conflicting radii (none/2px/4px) and paddings for the same variants — the renderer can't match all three, so the spec should be consolidated into one canonical banner atom (and gain a .banner.ok for the renderer's success variant). No left-stripe regression. progress/status/empty-state/skeleton out of scope per task.
- **Confirmed aligned:** layout structure (flex, align-items flex-start, gap 12px, 1px full coloured border, NO 4px left-accent stripe), warning/guard icon fg.warning (status.warn #b8862d), warning/guard background bg.warning.subtle warnSubtle rgba(184,134,45,0.08), stale variant banner.dashed.stale (dashed top/bottom only on bg.surface with border.strong), icon font-size md 16px + flex-shrink 0, error variant bg.danger.subtle rgba(184,90,74,0.06) + fg.danger #b85a4a border, actions/dismiss right-aligned via flex-1 body + flexShrink HStack.
- **Discrepancies:**

| Property | Spec | Renderer | Dir | Sev | Fix |
|---|---|---|---|---|---|
| banner · partial/info border-color + icon colour | .bn.bn-partial border-color:#5b6e87 and .bn-partial .bn-icon color:#5b6e87 (slate-blue), paired with rgba(91,110,135,0.08) bg | banner.partial borderColor: fg.info = status.info #3a7780 (teal), STATUS_TO_FG.info/neutral = fg.info #3a7780 icon, paired with bg.info.subtle rgba(91,110,135,0.08) | renderer-drift | 🟠 major | Renderer side: add a dedicated slate semantic token (e.g. fg.partial / status.partial = #5b6e87) and point banner.partial.borderColor + STATUS_TO_FG.{info,neutral,partial} at it, OR change infoSubtle/fg.info both to the slate family. Do not leave bg on slate while border/icon are on teal. |
| banner · description (body) text colour | .bn-body color:var(--ink) = #111b22 (spec.css:2348) | description ChakraBox color="fg.muted" = gray.600 = #4a5f5f (banner/index.tsx:137) | renderer-drift | 🟠 major | Renderer: description colour should be the body ink (fg.DEFAULT / brand.900) not fg.muted; reserve fg.muted for secondary meta lines. |
| spec · duplicate/conflicting .banner definitions | three separate .banner rule blocks: dashed (723-744), guardrail surround (1648-1661), plus the .bn family (2332-2357) — three radii (none/4px/2px) and two paddings (12/18, 10/14, 12/14) for conceptually the same component | renderer consolidates into banner.* layer styles, having to pick one value per property | spec-internal-inconsistency | 🟠 major | Spec side: consolidate .banner + .bn into one canonical banner atom with a single radius and padding scale per variant; the renderer can then map 1:1. |
| banner · change border-color | .bn.bn-change border-color:var(--brand-d) = #3a7780 (brand.600) | banner.change borderColor: border.brand = brand.500 = #488e97 (lighter teal) | renderer-drift | 🟡 minor | Renderer: set banner.change borderColor to brand.600 / a 'border.brand.strong' (#3a7780) to match --brand-d. |
| banner · change icon colour | .bn.bn-change .bn-icon color:var(--brand-dd) = #2b4b55 (brand.700) | STATUS_TO_FG.change = border.brand = brand.500 = #488e97 | renderer-drift | 🟡 minor | Renderer: STATUS_TO_FG.change should resolve to brand.700/#2b4b55 (e.g. brand.fg) not border.brand. |
| banner · border-radius | .bn border-radius:2px (spec.css:2335); guardrail .banner border-radius:4px (spec.css:1652) | all surround layer styles (banner.stale/partial/change/error/ok/guard) borderRadius:4px | renderer-drift | 🟡 minor | Renderer: set the .bn-derived layer styles (partial/change + the .bn-stale tint) borderRadius to 2px; keep guard at 4px. OR consolidate the spec to a single radius and document it. |
| banner · vertical padding (surround variants) | .bn padding:12px 14px (spec.css:2335) | banner.{stale,partial,change,error,ok,guard} padding:10px 14px (layer-styles.ts:190 etc.) | renderer-drift | 🟡 minor | Renderer: bump padding to 12px 14px for the .bn-derived variants (partial/change/stale-tint), keep 10px 14px only for guard if matching the guardrail spec. OR consolidate spec padding. |
| banner.dashed.stale · horizontal padding | .banner (dashed) padding:12px 18px (spec.css:727) | banner.dashed.stale padding:12px 20px (layer-styles.ts:248) | renderer-drift | 🟡 minor | Renderer: change banner.dashed.stale padding to 12px 18px. |
| banner · title font-size & weight | .bn-body / .banner-title: title inline in 13px body, strong = font-weight:600 (spec.css:2348-2349,1661) | title ChakraBox fontSize="sm" (14px) fontWeight="semibold" lineHeight 1.4 (banner/index.tsx:130) | renderer-drift | 🟡 minor | Renderer: set title fontSize to 13px to match the .bn-body line; keep semibold. |
| banner · primary ink token (title default) | .banner-title / .bn-body strong color:var(--ink) = #111b22 | title inherits fg.DEFAULT = gray.900 = #1a2626 (no explicit color) | spec-vs-tokens-conflict (intentional) | ⚪ nit | None for banner; resolve globally if the team wants fg.DEFAULT pinned to brand.900 #111b22. |
| banner.ok / success variant | no .bn-ok / .banner.ok variant exists in spec (the .banner family is guard/stale/partial/change/error) | banner.ok = bg.success.subtle rgba(47,122,91,0.06) + fg.success #2f7a5b border (layer-styles.ts:220) | renderer-drift (intentional) | ⚪ nit | Spec side: add a .banner.ok / .bn-ok atom (bg rgba(47,122,91,0.06), border --pos) so the renderer variant has a canonical source. |