Inline mono key/value fact — headers, meta rows, rails.

```jsx
<Tag k="horizon" v="14 d" />
<Tag k="scenario" v="B · draft" />
```

Key is auto-uppercased mono; value is bold tabular mono. Compose several in a flex row with 16px gap.
