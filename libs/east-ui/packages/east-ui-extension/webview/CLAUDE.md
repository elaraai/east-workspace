# east-ui-extension webview

React app bundled into the VS Code extension's webview panel. Built
with Vite; consumed by the extension's webview-creation code in
`../src/`.

This is an internal build-time split of `east-ui-extension`, not a
published package.

## See also

- [`../CLAUDE.md`](../CLAUDE.md) — extension overview (CSP, font
  hosting, build pipeline).
